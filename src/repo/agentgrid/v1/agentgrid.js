/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * Tool
 * Runtime Tool(Code Interpreter) 管理与调用相关接口
 *
 * OpenAPI spec version: v1
 * Contact:
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

require('../../../lib/node_loader')
var JDCloud = require('../../../lib/core')
var Service = JDCloud.Service
var serviceId = 'agentgrid'
Service._services[serviceId] = true

/**
 * agentgrid service.
 * @version 1.0.1
 */

class AGENTGRID extends Service {
  constructor (options = {}) {
    options._defaultEndpoint = {}
    options._defaultEndpoint.protocol =
      options._defaultEndpoint.protocol || 'https'
    options._defaultEndpoint.host =
      options._defaultEndpoint.host || 'agentgrid.jdcloud-api.com'
    options.basePath = '/v1' // 默认要设为空""
    super(serviceId, options)
  }

  /**
      *  查询 Browser Tool 列表。

## 接口说明
- 支持按 Browser Tool ID 批量查询。

      * @param {Object} opts - parameters
      * @param {integer} [opts.pageSize] - 每页返回的数据条数。取值范围 10～100，默认值为 10。  optional
      * @param {integer} [opts.pageNumber] - 页码。默认值为 1。  optional
      * @param {filter} [opts.filters] - 过滤条件。支持的键如下：
- browserToolId：按 Browser Tool ID 精确匹配，支持多个值。
- name：按 Browser Tool 名称模糊匹配，仅支持单个值。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param browserTool browserTools
      * @param integer total  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeBrowserTools (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeBrowserTools"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeBrowserTools with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建一个 Browser Tool 资源。

## 接口说明
- 支持通过 &#x60;clientToken&#x60; 保证请求幂等性。
- 创建成功后由服务端自动生成 &#x60;apiKey&#x60;。
- Browser Tool 状态包括：&#x60;pending&#x60;（已受理，等待资源准备）、&#x60;ready&#x60;（可创建 Session）、&#x60;failed&#x60;（处理失败）、&#x60;deleting&#x60;（删除中）、&#x60;deleted&#x60;（已删除）。
- 本接口为异步接口。请求受理成功后，资源通常先进入 &#x60;pending&#x60;，待底层资源准备完成后进入 &#x60;ready&#x60;；如创建失败则进入 &#x60;failed&#x60;。建议通过“查询 Browser Tool 详情”或“查询 Browser Tool 列表”轮询确认最终状态。

      * @param {Object} opts - parameters
      * @param {string} opts.name - Browser Tool 名称。长度为 1~32 个字符，可包含中文、数字、大小写字母、下划线、中划线或点。
      * @param {string} [opts.description] - Browser Tool 描述信息，长度不超过 256 个字符。  optional
      * @param {string} [opts.authenticationType] - Browser Tool 鉴权方式，当前仅支持 &#x60;APIKey&#x60;  optional
      * @param {string} opts.networkConfiguration - Code Interpreter 网络配置。支持 public｜sandbox 两种。默认为sandbox。
      * @param {string} [opts.clientToken] - 用于保证请求幂等性。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string browserToolId  Browser Tool ID。
      */

  createBrowserTool (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createBrowserTool"
      )
    }

    opts = opts || {}

    if (opts.name === undefined || opts.name === null) {
      throw new Error(
        "Missing the required parameter 'opts.name' when calling createBrowserTool"
      )
    }
    if (
      opts.networkConfiguration === undefined ||
      opts.networkConfiguration === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.networkConfiguration' when calling createBrowserTool"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (
      opts.authenticationType !== undefined &&
      opts.authenticationType !== null
    ) {
      postBody['authenticationType'] = opts.authenticationType
    }
    if (
      opts.networkConfiguration !== undefined &&
      opts.networkConfiguration !== null
    ) {
      postBody['networkConfiguration'] = opts.networkConfiguration
    }
    if (opts.clientToken !== undefined && opts.clientToken !== null) {
      postBody['clientToken'] = opts.clientToken
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createBrowserTool with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Browser Tool 的详细信息。

      * @param {Object} opts - parameters
      * @param {string} opts.browserToolId - Browser Tool ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param browserTool browserTool
      */

  describeBrowserTool (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeBrowserTool"
      )
    }

    opts = opts || {}

    if (opts.browserToolId === undefined || opts.browserToolId === null) {
      throw new Error(
        "Missing the required parameter 'opts.browserToolId' when calling describeBrowserTool"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      browserToolId: opts.browserToolId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeBrowserTool with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools/{browserToolId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除指定 Browser Tool。

## 接口说明
- 删除时会级联停止并删除该 Browser Tool 下的所有 Session。
- 删除请求受理后，Browser Tool 状态会变为 deleting，删除完成后变为 deleted；deleting 期间不可继续创建新的 Session

      * @param {Object} opts - parameters
      * @param {string} opts.browserToolId - Browser Tool ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteBrowserTool (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteBrowserTool"
      )
    }

    opts = opts || {}

    if (opts.browserToolId === undefined || opts.browserToolId === null) {
      throw new Error(
        "Missing the required parameter 'opts.browserToolId' when calling deleteBrowserTool"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      browserToolId: opts.browserToolId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteBrowserTool with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools/{browserToolId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Browser Tool 的 Session 列表。

## 接口说明
- 支持按 Session 状态过滤。
- 未指定 &#x60;state&#x60; 时，默认返回该 Browser Tool 下的全部 Session。
- 如仅需获取可直接接入的 Session，建议筛选 &#x60;state&#x3D;ready&#x60;。

      * @param {Object} opts - parameters
      * @param {string} opts.browserToolId - Browser Tool ID
      * @param {integer} [opts.pageSize] - 每页返回的数据条数。取值范围 10～100，默认值为 10。  optional
      * @param {integer} [opts.pageNumber] - 页码。默认值为 1。  optional
      * @param {filter} [opts.filters] - 过滤条件。支持的键如下：
- sessionId：按 session ID 精确匹配，支持多个值。
- state：按 Session 状态精确匹配，支持单个值。可选值：&#x60;pending&#x60;、&#x60;creating&#x60;、&#x60;ready&#x60;、&#x60;deleting&#x60;、&#x60;deleted&#x60;、&#x60;failed&#x60;
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param browserToolSession browserToolSessions
      * @param integer total  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeBrowserToolSessions (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeBrowserToolSessions"
      )
    }

    opts = opts || {}

    if (opts.browserToolId === undefined || opts.browserToolId === null) {
      throw new Error(
        "Missing the required parameter 'opts.browserToolId' when calling describeBrowserToolSessions"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      browserToolId: opts.browserToolId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeBrowserToolSessions with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools/{browserToolId}/sessions',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建并初始化 Browser Tool Session。

## 接口说明
- 支持通过 &#x60;clientToken&#x60; 保证请求幂等性。
- Session 状态包括：&#x60;pending&#x60;（已受理，等待资源准备）、&#x60;creating&#x60;（初始化中）、&#x60;ready&#x60;（可连接使用）、&#x60;deleting&#x60;（删除中）、&#x60;deleted&#x60;（已删除）、&#x60;failed&#x60;（初始化失败）。
- 本接口为异步接口。请求受理成功后返回 &#x60;sessionId&#x60;，Session 通常会依次经历 &#x60;pending&#x60;、&#x60;creating&#x60;，最终进入 &#x60;ready&#x60; 或 &#x60;failed&#x60;。建议通过“查询 Browser Tool Session 详情”轮询确认状态；仅当 &#x60;state&#x60; 为 &#x60;ready&#x60; 时，&#x60;cdpEndpoint&#x60;、&#x60;vncEndpoint&#x60; 才可用于接入。

      * @param {Object} opts - parameters
      * @param {string} opts.browserToolId - Browser Tool ID
      * @param {string} [opts.name] - Session 名称。  optional
      * @param {integer} [opts.maxLifeTime] - Session 最大存活时长（秒）。取值范围 60～28800，默认值为 28800。  optional
      * @param {string} [opts.clientToken] - 用于保证请求幂等性。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string sessionId  Session ID。
      */

  createBrowserToolSession (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createBrowserToolSession"
      )
    }

    opts = opts || {}

    if (opts.browserToolId === undefined || opts.browserToolId === null) {
      throw new Error(
        "Missing the required parameter 'opts.browserToolId' when calling createBrowserToolSession"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.maxLifeTime !== undefined && opts.maxLifeTime !== null) {
      postBody['maxLifeTime'] = opts.maxLifeTime
    }
    if (opts.clientToken !== undefined && opts.clientToken !== null) {
      postBody['clientToken'] = opts.clientToken
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      browserToolId: opts.browserToolId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createBrowserToolSession with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools/{browserToolId}/sessions',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Browser Tool Session 的详细信息。

      * @param {Object} opts - parameters
      * @param {string} opts.browserToolId - Browser Tool ID
      * @param {string} opts.sessionId - Session ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param browserToolSession browserToolSession
      */

  describeBrowserToolSession (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeBrowserToolSession"
      )
    }

    opts = opts || {}

    if (opts.browserToolId === undefined || opts.browserToolId === null) {
      throw new Error(
        "Missing the required parameter 'opts.browserToolId' when calling describeBrowserToolSession"
      )
    }
    if (opts.sessionId === undefined || opts.sessionId === null) {
      throw new Error(
        "Missing the required parameter 'opts.sessionId' when calling describeBrowserToolSession"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      browserToolId: opts.browserToolId,
      sessionId: opts.sessionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeBrowserToolSession with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools/{browserToolId}/sessions/{sessionId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止指定 Browser Tool Session。

## 接口说明
- 删除请求受理后，Session 状态会进入 &#x60;deleting&#x60;，删除完成后进入 &#x60;deleted&#x60;。
- &#x60;deleted&#x60; 状态的 Session 不可恢复；如需继续使用，请重新创建新的 Session。

      * @param {Object} opts - parameters
      * @param {string} opts.browserToolId - Browser Tool ID
      * @param {string} opts.sessionId - Session ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteBrowserToolSession (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteBrowserToolSession"
      )
    }

    opts = opts || {}

    if (opts.browserToolId === undefined || opts.browserToolId === null) {
      throw new Error(
        "Missing the required parameter 'opts.browserToolId' when calling deleteBrowserToolSession"
      )
    }
    if (opts.sessionId === undefined || opts.sessionId === null) {
      throw new Error(
        "Missing the required parameter 'opts.sessionId' when calling deleteBrowserToolSession"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      browserToolId: opts.browserToolId,
      sessionId: opts.sessionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteBrowserToolSession with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/browserTools/{browserToolId}/sessions/{sessionId}:delete',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  为指定 Runtime 创建 Endpoint 访问地址。

## 接口说明
- 调用成功后将返回该 Runtime 的公网与私网访问地址。
- Endpoint 地址可用于后续 Runtime 的调用与接入配置。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string publicEndpoint  公网访问地址。
      * @param string internalEndpoint  私网访问地址。
      */

  createRuntimeEndpoint (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createRuntimeEndpoint"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling createRuntimeEndpoint"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createRuntimeEndpoint with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}:createEndpoint',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Runtime 的 Endpoint 详细信息。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param runtimeEndpointInfo runtimeEndpoint
      */

  describeRuntimeEndpoint (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeRuntimeEndpoint"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling describeRuntimeEndpoint"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeRuntimeEndpoint with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}:describeEndpoint',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  批量查询多个 Runtime 的 Endpoint 信息。

      * @param {Object} opts - parameters
      * @param {string} [opts.runtimeIds] - Runtime ID 列表。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param runtimeEndpointInfo runtimeEndpoints
      */

  describeRuntimeEndpoints (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeRuntimeEndpoints"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.runtimeIds, 'runtimeIds')
    )

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeRuntimeEndpoints with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/endpoints',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除指定 Runtime 的 Endpoint 配置。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteRuntimeEndpoint (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteRuntimeEndpoint"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling deleteRuntimeEndpoint"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteRuntimeEndpoint with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}:deleteEndpoint',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询 Runtime 列表，默认返回当前生效版本的信息。

## 接口说明
- 支持分页、排序、条件过滤与标签过滤。
- Runtime 状态包括：&#x60;pending&#x60;、&#x60;creating&#x60;、&#x60;running&#x60;、&#x60;updating&#x60;、&#x60;failed&#x60;、&#x60;deleted&#x60;。
- 返回结果中的 &#x60;state&#x60; 表示 Runtime 当前整体状态；&#x60;versionView.state&#x60; 表示本次返回版本的状态。

      * @param {Object} opts - parameters
      * @param {integer} [opts.pageSize] - 每页返回的数据条数。取值范围 10～100，默认值为 10。  optional
      * @param {integer} [opts.pageNumber] - 页码。默认值为 1。  optional
      * @param {filter} [opts.filters] - 过滤条件。支持的键如下：
- runtimeId：按 Runtime ID 精确匹配，支持多个值。
- runtimeName：按 Runtime 名称模糊匹配，仅支持单个值。
- state：按 Runtime 状态过滤，支持多个值。可选值：&#x60;pending&#x60;、&#x60;creating&#x60;、&#x60;running&#x60;、&#x60;updating&#x60;、&#x60;failed&#x60;、&#x60;deleted&#x60;
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param integer total  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      * @param runtimeView runtimes
      */

  describeRuntimes (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeRuntimes"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeRuntimes with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建一个 Runtime 资源，并生成首个 Runtime 版本。

## 接口说明
- 支持通过 &#x60;clientToken&#x60; 保证请求幂等性。
- 创建时需提供制品、鉴权方式、网络等核心配置信息。
- Runtime 状态包括：&#x60;pending&#x60;（已受理，等待资源准备）、&#x60;creating&#x60;（创建中）、&#x60;running&#x60;（可正常调用）、&#x60;updating&#x60;（版本或配置更新中）、&#x60;failed&#x60;（处理失败）、&#x60;deleted&#x60;（已删除，不可继续使用）。
- Runtime Version 状态包括：&#x60;pending&#x60;（已受理，等待资源准备）、&#x60;creating&#x60;（创建中）、&#x60;running&#x60;（可正常使用）、&#x60;failed&#x60;（处理失败）、&#x60;deleted&#x60;（已删除）。
- 本接口为异步接口，请求受理成功后会返回 &#x60;requestId&#x60;、&#x60;runtimeId&#x60; 和初始 &#x60;runtimeVersion&#x60;。创建初期，Runtime 与首个 Runtime Version 通常先处于 &#x60;pending&#x60;，随后进入 &#x60;creating&#x60;，最终进入 &#x60;running&#x60; 或 &#x60;failed&#x60;。
- 建议在创建成功后，通过“查询 Runtime 详情”或“查询 Runtime 版本列表”轮询状态；当 &#x60;state&#x3D;running&#x60; 且 &#x60;versionView.state&#x3D;running&#x60; 后，再进行调用或版本相关操作。

      * @param {Object} opts - parameters
      * @param {string} opts.name - Runtime 名称。长度为 1~32 个字符，只允许中文、数字、大小写字母、英文下划线（\_）、连字符（-）及点（.）。
      * @param {artifactConfiguration} opts.artifactConfiguration - Runtime 制品配置，当前支持通过镜像导入
      * @param {integer} [opts.warmSession] - Runtime 预热 Session 数量，进行预热后，将会提前生成对应数量的 Session 实例，可以大幅减少会话初次启动时间。取值范围1～10，默认值为 1，且应小于等于 &#x60;maxSession&#x60;。  optional
      * @param {integer} [opts.maxSession] - Runtime 可承载的最大 Session 数量，取值范围1～10，默认值为 10。  optional
      * @param {string} [opts.description] - Runtime 描述信息。  optional
      * @param {authorizerConfiguration} opts.authorizerConfiguration - Runtime 鉴权配置。
      * @param {lifecycleConfiguration} [opts.lifecycleConfiguration] - Runtime 生命周期配置。  optional
      * @param {networkConfiguration} opts.networkConfiguration - Runtime 网络配置。
      * @param {array} [opts.envs] - Runtime 环境变量配置，最多 50 组。  optional
      * @param {string} [opts.protocol] - Runtime 支持的协议类型。当前支持 HTTP 。  optional
      * @param {string} [opts.clientToken] - 用于保证请求幂等性。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string runtimeId  Runtime ID。
      * @param string runtimeVersion  Runtime 版本号。
      */

  createRuntime (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createRuntime"
      )
    }

    opts = opts || {}

    if (opts.name === undefined || opts.name === null) {
      throw new Error(
        "Missing the required parameter 'opts.name' when calling createRuntime"
      )
    }
    if (
      opts.artifactConfiguration === undefined ||
      opts.artifactConfiguration === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.artifactConfiguration' when calling createRuntime"
      )
    }
    if (
      opts.authorizerConfiguration === undefined ||
      opts.authorizerConfiguration === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.authorizerConfiguration' when calling createRuntime"
      )
    }
    if (
      opts.networkConfiguration === undefined ||
      opts.networkConfiguration === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.networkConfiguration' when calling createRuntime"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (
      opts.artifactConfiguration !== undefined &&
      opts.artifactConfiguration !== null
    ) {
      postBody['artifactConfiguration'] = opts.artifactConfiguration
    }
    if (opts.warmSession !== undefined && opts.warmSession !== null) {
      postBody['warmSession'] = opts.warmSession
    }
    if (opts.maxSession !== undefined && opts.maxSession !== null) {
      postBody['maxSession'] = opts.maxSession
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (
      opts.authorizerConfiguration !== undefined &&
      opts.authorizerConfiguration !== null
    ) {
      postBody['authorizerConfiguration'] = opts.authorizerConfiguration
    }
    if (
      opts.lifecycleConfiguration !== undefined &&
      opts.lifecycleConfiguration !== null
    ) {
      postBody['lifecycleConfiguration'] = opts.lifecycleConfiguration
    }
    if (
      opts.networkConfiguration !== undefined &&
      opts.networkConfiguration !== null
    ) {
      postBody['networkConfiguration'] = opts.networkConfiguration
    }
    if (opts.envs !== undefined && opts.envs !== null) {
      postBody['envs'] = opts.envs
    }
    if (opts.protocol !== undefined && opts.protocol !== null) {
      postBody['protocol'] = opts.protocol
    }
    if (opts.clientToken !== undefined && opts.clientToken !== null) {
      postBody['clientToken'] = opts.clientToken
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createRuntime with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Runtime 的版本详情信息。

## 接口说明
- 不传 &#x60;version&#x60; 时，返回 Runtime 当前生效版本的信息。
- 返回结果中的顶层 &#x60;state&#x60; 表示 Runtime 当前整体状态；&#x60;versionView.state&#x60; 表示指定版本或当前生效版本的状态。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} [opts.version] - Runtime 版本号。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param runtimeView runtimeView
      */

  describeRuntime (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeRuntime"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling describeRuntime"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.version !== undefined && opts.version !== null) {
      queryParams['version'] = opts.version
    }

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeRuntime with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除指定 Runtime 及其所有版本信息。

## 接口说明
- 仅当 Runtime 当前状态为 &#x60;running&#x60; 或 &#x60;failed&#x60; 时允许删除。
- 删除后数据不可恢复，请谨慎操作。
- 删除完成后，Runtime 最终状态为 &#x60;deleted&#x60;；&#x60;deleted&#x60; 状态的 Runtime 不可恢复。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteRuntime (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteRuntime"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling deleteRuntime"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteRuntime with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新 Runtime 模板配置，并生成一个新版本。

## 接口说明
- 该操作用于更新 Runtime 的配置信息。
- Runtime 在处理更新请求期间，其状态通常会变为 &#x60;updating&#x60;；本次新生成的 Runtime Version 状态通常会依次经历 &#x60;pending&#x60;、&#x60;creating&#x60;，最终进入 &#x60;running&#x60; 或 &#x60;failed&#x60;。
- 本接口响应仅返回 &#x60;requestId&#x60;，不会直接返回新版本号。请求受理后，建议调用“查询 Runtime 版本列表”确认最新版本号及其状态。
- 新版本创建成功后不会自动成为生效版本；如需切换，请继续调用“选择 Runtime 模版版本”接口。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} [opts.description] - Runtime 新版本的描述信息。  optional
      * @param {authorizerConfiguration} [opts.authorizerConfiguration] - Runtime 鉴权配置。  optional
      * @param {artifactConfiguration} [opts.artifactConfiguration] - Runtime 制品配置，支持更换镜像  optional
      * @param {networkConfiguration} [opts.networkConfiguration] - Runtime 网络配置。  optional
      * @param {integer} [opts.warmSession] - Runtime 预热 Session 数量，进行预热后，将会提前生成对应数量的 Session 实例，可以大幅减少会话初次启动时间。取值范围1～10，默认值为 1，且应小于等于 &#x60;maxSession&#x60;。  optional
      * @param {integer} [opts.maxSession] - Runtime 可承载的最大 Session 数量，取值范围1～10，默认值为 10。  optional
      * @param {array} [opts.envs] - Runtime 环境变量配置，以 &#x60;key-value&#x60; 键值对形式指定，单次请求最多支持 50 组。

## 参数说明
- 不传该参数时，保留当前生效版本中的环境变量配置。
- 当 &#x60;key&#x60; 以连字符 &#x60;-&#x60; 结尾时，表示删除旧版本中对应 &#x60;key&#x60; 的环境变量。
- 当 &#x60;key&#x60; 不以连字符 &#x60;-&#x60; 结尾时，表示新增或更新该环境变量。
- 本次调用后，环境变量不能超过50组
- &#x60;key&#x60; 长度上限为 256 个字符。
  optional
      * @param {string} [opts.protocol] - Runtime 支持的协议类型，当前仅支持 HTTP。  optional
      * @param {lifecycleConfiguration} [opts.lifecycleConfiguration] - Runtime 生命周期配置。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateRuntime (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateRuntime"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling updateRuntime"
      )
    }

    let postBody = {}
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (
      opts.authorizerConfiguration !== undefined &&
      opts.authorizerConfiguration !== null
    ) {
      postBody['authorizerConfiguration'] = opts.authorizerConfiguration
    }
    if (
      opts.artifactConfiguration !== undefined &&
      opts.artifactConfiguration !== null
    ) {
      postBody['artifactConfiguration'] = opts.artifactConfiguration
    }
    if (
      opts.networkConfiguration !== undefined &&
      opts.networkConfiguration !== null
    ) {
      postBody['networkConfiguration'] = opts.networkConfiguration
    }
    if (opts.warmSession !== undefined && opts.warmSession !== null) {
      postBody['warmSession'] = opts.warmSession
    }
    if (opts.maxSession !== undefined && opts.maxSession !== null) {
      postBody['maxSession'] = opts.maxSession
    }
    if (opts.envs !== undefined && opts.envs !== null) {
      postBody['envs'] = opts.envs
    }
    if (opts.protocol !== undefined && opts.protocol !== null) {
      postBody['protocol'] = opts.protocol
    }
    if (
      opts.lifecycleConfiguration !== undefined &&
      opts.lifecycleConfiguration !== null
    ) {
      postBody['lifecycleConfiguration'] = opts.lifecycleConfiguration
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateRuntime with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}:updateRuntime',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新 Runtime 基础信息

## 接口说明
- 该操作仅更新 Runtime 的基础元信息，不会生成新的版本，也不会改变当前生效版本。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} [opts.description] - Runtime 描述信息。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateRuntimeAttributes (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateRuntimeAttributes"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling updateRuntimeAttributes"
      )
    }

    let postBody = {}
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateRuntimeAttributes with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}:updateRuntimeAttributes',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  将指定版本设置为 Runtime 的生效版本。

## 接口说明
- 调用前建议先通过“查询 Runtime 的版本列表”确认目标版本 &#x60;state&#x3D;running&#x60;。
- 切换成功后，后续未显式指定版本的 Runtime 调用将默认命中该生效版本。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} opts.version - Runtime 版本号
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  modifyRuntimeVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  modifyRuntimeVersion"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling modifyRuntimeVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling modifyRuntimeVersion"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call modifyRuntimeVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}/versions/{version}:modifyRuntimeVersion',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询 Runtime 的版本列表信息。

## 接口说明
- 每个版本的 &#x60;state&#x60; 可选值为：&#x60;pending&#x60;、&#x60;creating&#x60;、&#x60;running&#x60;、&#x60;failed&#x60;、&#x60;deleted&#x60;。
- 建议在创建 Runtime 或更新 Runtime 后，通过本接口确认版本是否已进入 &#x60;running&#x60;。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string runtimeId  Runtime ID
      * @param string name  Runtime 名称
      * @param runtimeVersionView runtimeVersionViews
      */

  describeRuntimeVersions (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeRuntimeVersions"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling describeRuntimeVersions"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeRuntimeVersions with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}/versions',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Runtime 当前运行中的 Session 数量。

## 接口说明
- 返回值为查询时刻的快照统计，其中 &#x60;versionSessions&#x60; 用于展示各版本当前生效的 Session 数量。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string runtimeId  Runtime ID。
      * @param integer totalSession  当前 Runtime 下处于运行状态的 Session 数量。
      * @param versionSessionView versionSessions
      */

  getRuntimeSessions (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getRuntimeSessions"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling getRuntimeSessions"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getRuntimeSessions with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}/sessions',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除指定 Runtime 的目标版本信息。

## 接口说明
- 删除后该版本不可恢复，请谨慎操作。
- 删除完成后，该版本状态最终为 &#x60;deleted&#x60;。

      * @param {Object} opts - parameters
      * @param {string} opts.runtimeId - Runtime ID
      * @param {string} opts.version - Runtime 版本号
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteRuntimeVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteRuntimeVersion"
      )
    }

    opts = opts || {}

    if (opts.runtimeId === undefined || opts.runtimeId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runtimeId' when calling deleteRuntimeVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling deleteRuntimeVersion"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      runtimeId: opts.runtimeId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteRuntimeVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/runtimes/{runtimeId}/versions/{version}:deleteRuntimeVersion',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询 Code Interpreter 列表。

## 接口说明
- 支持按 Code Interpreter ID 批量查询。

      * @param {Object} opts - parameters
      * @param {integer} [opts.pageSize] - 每页返回的数据条数。取值范围 10～100，默认值为 10。  optional
      * @param {integer} [opts.pageNumber] - 页码。默认值为 1。  optional
      * @param {filter} [opts.filters] - 过滤条件。支持的键如下：
- codeInterpreterId：按 Code Interpreter ID 精确匹配，支持多个值。
- name：按 Code Interpreter 名称模糊匹配，仅支持单个值。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param integer total  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      * @param codeInterpreter codeInterpreters
      */

  describeCodeInterpreters (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeCodeInterpreters"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeCodeInterpreters with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建一个 Code Interpreter 工具实例。

## 接口说明
- 支持通过 &#x60;clientToken&#x60; 保证请求幂等性。
- 创建成功后由服务端自动生成 &#x60;apiKey&#x60;。
- Code Interpreter 状态包括：&#x60;pending&#x60;（已受理，等待资源准备）、&#x60;ready&#x60;（可创建 Session）、&#x60;failed&#x60;（处理失败）、&#x60;deleting&#x60;（删除中）、&#x60;deleted&#x60;（已删除）。
- 本接口为异步接口。请求受理成功后，资源通常先进入 &#x60;pending&#x60;，待底层资源准备完成后进入 &#x60;ready&#x60;；如创建失败则进入 &#x60;failed&#x60;。建议通过“查询 Code Interpreter 详情”或“查询 Code Interpreter 列表”轮询确认最终状态。

      * @param {Object} opts - parameters
      * @param {string} opts.name - Code Interpreter 名称，全局唯一。
      * @param {string} [opts.description] - Code Interpreter 描述信息。  optional
      * @param {string} [opts.authenticationType] - Code Interpreter 鉴权方式，当前仅支持 &#x60;APIKey&#x60;  optional
      * @param {string} [opts.clientToken] - 用于保证请求幂等性。  optional
      * @param {string} opts.networkConfiguration - Code Interpreter 网络配置类型。当前支持 &#x60;Public&#x60; 和 &#x60;SandBox&#x60;，建议显式传值。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string codeInterpreterId  Code Interpreter ID。
      */

  createCodeInterpreter (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createCodeInterpreter"
      )
    }

    opts = opts || {}

    if (opts.name === undefined || opts.name === null) {
      throw new Error(
        "Missing the required parameter 'opts.name' when calling createCodeInterpreter"
      )
    }
    if (
      opts.networkConfiguration === undefined ||
      opts.networkConfiguration === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.networkConfiguration' when calling createCodeInterpreter"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (
      opts.authenticationType !== undefined &&
      opts.authenticationType !== null
    ) {
      postBody['authenticationType'] = opts.authenticationType
    }
    if (opts.clientToken !== undefined && opts.clientToken !== null) {
      postBody['clientToken'] = opts.clientToken
    }
    if (
      opts.networkConfiguration !== undefined &&
      opts.networkConfiguration !== null
    ) {
      postBody['networkConfiguration'] = opts.networkConfiguration
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createCodeInterpreter with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Code Interpreter 的详细信息。

      * @param {Object} opts - parameters
      * @param {string} opts.codeInterpreterId - Code Interpreter ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param codeInterpreter codeInterpreter
      */

  describeCodeInterpreter (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeCodeInterpreter"
      )
    }

    opts = opts || {}

    if (
      opts.codeInterpreterId === undefined ||
      opts.codeInterpreterId === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.codeInterpreterId' when calling describeCodeInterpreter"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      codeInterpreterId: opts.codeInterpreterId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeCodeInterpreter with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters/{codeInterpreterId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除指定 Code Interpreter。

## 接口说明
- 删除请求受理后，Code Interpreter 状态会变为 &#x60;deleting&#x60;，删除完成后变为 &#x60;deleted&#x60;。

      * @param {Object} opts - parameters
      * @param {string} opts.codeInterpreterId - Code Interpreter ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteCodeInterpreter (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteCodeInterpreter"
      )
    }

    opts = opts || {}

    if (
      opts.codeInterpreterId === undefined ||
      opts.codeInterpreterId === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.codeInterpreterId' when calling deleteCodeInterpreter"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      codeInterpreterId: opts.codeInterpreterId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteCodeInterpreter with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters/{codeInterpreterId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Code Interpreter 的 Session 列表。

## 接口说明
- 支持按 Session 状态过滤。
- 未指定 &#x60;state&#x60; 时，默认返回该 Code Interpreter 下的全部 Session。
- 如仅需获取可继续执行工具调用的 Session，建议筛选 &#x60;state&#x3D;ready&#x60;。

      * @param {Object} opts - parameters
      * @param {string} opts.codeInterpreterId - Code Interpreter ID
      * @param {integer} [opts.pageSize] - 每页返回的数据条数。取值范围 10～100，默认值为 10。  optional
      * @param {integer} [opts.pageNumber] - 页码。默认值为 1。  optional
      * @param {filter} [opts.filters] - 过滤条件。支持的键如下：
- state：按 Session 状态精确匹配，支持单个值。可选值：&#x60;pending&#x60;、&#x60;creating&#x60;、&#x60;ready&#x60;、&#x60;deleting&#x60;、&#x60;deleted&#x60;、&#x60;failed&#x60;
- sessionId：按 session ID 精确匹配，支持多个值。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param integer total  session 总数量。
      * @param codeInterpreterSession codeInterpreterSessions
      */

  describeCodeInterpreterSessions (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeCodeInterpreterSessions"
      )
    }

    opts = opts || {}

    if (
      opts.codeInterpreterId === undefined ||
      opts.codeInterpreterId === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.codeInterpreterId' when calling describeCodeInterpreterSessions"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      codeInterpreterId: opts.codeInterpreterId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeCodeInterpreterSessions with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters/{codeInterpreterId}/sessions',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建并初始化 Code Interpreter Session。

## 接口说明
- Session 状态包括：&#x60;pending&#x60;（已受理，等待资源准备）、&#x60;creating&#x60;（初始化中）、&#x60;ready&#x60;（可执行工具调用）、&#x60;deleting&#x60;（删除中）、&#x60;deleted&#x60;（已删除）、&#x60;failed&#x60;（初始化失败）。
- 本接口为异步接口。请求受理成功后返回 &#x60;sessionId&#x60;，Session 通常会依次经历 &#x60;pending&#x60;、&#x60;creating&#x60;，最终进入 &#x60;ready&#x60; 或 &#x60;failed&#x60;。建议通过“查询 Code Interpreter Session 详情”轮询确认状态，并在 &#x60;state&#x3D;ready&#x60; 后再发起后续调用。

      * @param {Object} opts - parameters
      * @param {string} opts.codeInterpreterId - Code Interpreter ID
      * @param {string} [opts.name] - Session 名称。长度为 1~32 个字符，可包含中文、数字、大小写字母、下划线、中划线或点。  optional
      * @param {integer} [opts.maxLifeTime] - Session 存活时间（秒）。取值范围为60～28800，默认为900秒（15分钟）。  optional
      * @param {string} [opts.clientToken] - 用于保证请求幂等性。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string sessionId  创建的 Session ID。
      */

  createCodeInterpreterSession (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createCodeInterpreterSession"
      )
    }

    opts = opts || {}

    if (
      opts.codeInterpreterId === undefined ||
      opts.codeInterpreterId === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.codeInterpreterId' when calling createCodeInterpreterSession"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.maxLifeTime !== undefined && opts.maxLifeTime !== null) {
      postBody['maxLifeTime'] = opts.maxLifeTime
    }
    if (opts.clientToken !== undefined && opts.clientToken !== null) {
      postBody['clientToken'] = opts.clientToken
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      codeInterpreterId: opts.codeInterpreterId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createCodeInterpreterSession with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters/{codeInterpreterId}/sessions',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定 Code Interpreter Session 详情。

      * @param {Object} opts - parameters
      * @param {string} opts.codeInterpreterId - Code Interpreter ID
      * @param {string} opts.sessionId - Session ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param codeInterpreterSession codeInterpreterSession
      */

  describeCodeInterpreterSession (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeCodeInterpreterSession"
      )
    }

    opts = opts || {}

    if (
      opts.codeInterpreterId === undefined ||
      opts.codeInterpreterId === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.codeInterpreterId' when calling describeCodeInterpreterSession"
      )
    }
    if (opts.sessionId === undefined || opts.sessionId === null) {
      throw new Error(
        "Missing the required parameter 'opts.sessionId' when calling describeCodeInterpreterSession"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      codeInterpreterId: opts.codeInterpreterId,
      sessionId: opts.sessionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeCodeInterpreterSession with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters/{codeInterpreterId}/sessions/{sessionId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止指定 Code Interpreter Session。

## 接口说明
- 删除请求受理后，Session 状态会进入 &#x60;deleting&#x60;，删除完成后进入 &#x60;deleted&#x60;。
- &#x60;deleted&#x60; 状态的 Session 不可恢复；如需继续使用，请重新创建新的 Session。

      * @param {Object} opts - parameters
      * @param {string} opts.codeInterpreterId - Code Interpreter ID
      * @param {string} opts.sessionId - Session ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteCodeInterpreterSession (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteCodeInterpreterSession"
      )
    }

    opts = opts || {}

    if (
      opts.codeInterpreterId === undefined ||
      opts.codeInterpreterId === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.codeInterpreterId' when calling deleteCodeInterpreterSession"
      )
    }
    if (opts.sessionId === undefined || opts.sessionId === null) {
      throw new Error(
        "Missing the required parameter 'opts.sessionId' when calling deleteCodeInterpreterSession"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      codeInterpreterId: opts.codeInterpreterId,
      sessionId: opts.sessionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  agentgrid/1.0.1'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteCodeInterpreterSession with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/codeInterpreters/{codeInterpreterId}/sessions/{sessionId}:delete',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }
}
module.exports = AGENTGRID
