/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * Workspace
 * 工作空间相关的操作
 *
 * OpenAPI spec version: v1
 * Contact:
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

require('../../../lib/node_loader')
var JDCloud = require('../../../lib/core')
var Service = JDCloud.Service
var serviceId = 'jdaip'
Service._services[serviceId] = true

/**
 * jdaip service.
 * @version 1.0.2
 */

class JDAIP extends Service {
  constructor (options = {}) {
    options._defaultEndpoint = {}
    options._defaultEndpoint.protocol =
      options._defaultEndpoint.protocol || 'https'
    options._defaultEndpoint.host =
      options._defaultEndpoint.host || 'jdaip.jdcloud-api.com'
    options.basePath = '/v1' // 默认要设为空""
    super(serviceId, options)
  }

  /**
      *  管理段-查询主账号下所有绑定空间的子账号
      * @param {Object} opts - parameters
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;pin&#x60;: 主账号pin， 精确比配， 必填
&#x60;memberUser&#x60;: 子账号名称，模糊匹配
&#x60;memberPin&#x60;: 子账号pin，精确匹配，支持多个
&#x60;regionId&#x60;: 区域，精确匹配，支持多个
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param accountMember members
      * @param integer totalCount  成员个数
      */

  adminDescribeAccountMembers (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeAccountMembers"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeAccountMembers with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/api-regions/{regionId}/member:admin',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询微调实验列表。

分页查询工作空间下的微调实验列表，支持多种过滤条件。

## 过滤条件

支持 &#x60;filters&#x60; 参数进行过滤：
- &#x60;experimentId&#x60;：实验ID，精确匹配，支持多个
- &#x60;name&#x60;：实验名称，模糊匹配，支持单个
- &#x60;modelSeries&#x60;：模型系列，精确匹配，支持多个
- &#x60;createUser&#x60;：创建人，模糊匹配
- &#x60;permission&#x60;：权限类型，精确匹配（public/private）
- &#x60;ownerUserPin&#x60;：所有者pin，精确匹配
- &#x60;ownerUser&#x60;：所有者名称，模糊匹配

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;experimentId&#x60;: 微调实验ID，精确匹配，支持多个。
&#x60;name&#x60;: 微调实验名称，模糊匹配，支持单个。
&#x60;modelSeries&#x60;: 模型系列，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人，模糊匹配，支持单个。
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param experimentListInfo experimentListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeExperiments (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeExperiments"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeExperiments"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeExperiments with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建微调实验。

创建一个新的微调实验，指定基础模型、模型系列和模板等参数。

## 核心配置

- **基本信息**：实验名称、描述
- **模型配置**：模型系列、模型模板、基础模型ID和版本
- **权限配置**：工作空间中的资源归属权限

## 创建成功后

- 返回唯一的微调实验ID（experimentId）
- 可以在该实验下创建多个微调运行
- 可通过查询接口查看实验详情

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {experimentParam} opts.experimentParam - 微调实验参数。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string experimentId  微调实验ID。
      */

  createExperiment (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createExperiment"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createExperiment"
      )
    }
    if (opts.experimentParam === undefined || opts.experimentParam === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentParam' when calling createExperiment"
      )
    }

    let postBody = {}
    if (opts.experimentParam !== undefined && opts.experimentParam !== null) {
      postBody['experimentParam'] = opts.experimentParam
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createExperiment with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询微调实验详情。

获取微调实验的完整信息，包括模型配置、权限设置、创建信息等。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param experimentDetailInfo experimentDetailInfo  微调实验详情。
      */

  describeExperiment (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeExperiment"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeExperiment"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling describeExperiment"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeExperiment with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新微调实验。

更新微调实验的元数据属性，不影响已有的微调运行。

## 使用场景

- 修改实验名称以便更好地识别和管理
- 更新实验描述信息
- 调整资源的可见性权限
- 转移实验归属给其他用户

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {string} [opts.name] - 微调实验名称。1~32字符，仅支持中文、大小写字母、数字、英文中划线 &quot;-&quot;和英文下划线&quot;_&quot;。  optional
      * @param {string} [opts.description] - 微调实验的描述信息，不超过256个字符。  optional
      * @param {string} [opts.permission] - 工作空间中的资源归属权限，支持（public,private）。  optional
      * @param {string} [opts.ownerUserPin] - 归属用户pin。
可配置任务所属主账号pin，或者工作空间其他成员的子账号pin
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateExperiment (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateExperiment"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateExperiment"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling updateExperiment"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (opts.permission !== undefined && opts.permission !== null) {
      postBody['permission'] = opts.permission
    }
    if (opts.ownerUserPin !== undefined && opts.ownerUserPin !== null) {
      postBody['ownerUserPin'] = opts.ownerUserPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateExperiment with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除微调实验。

删除微调实验将级联删除该实验下的所有微调运行，请谨慎操作。

## 注意事项

- **私有队列**：直接删除该实验下的所有运行
- **公共队列**：会检查运行状态是否满足删除条件，不满足则返回错误
- 删除操作不可恢复，请确保已备份重要数据

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteExperiment (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteExperiment"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteExperiment"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling deleteExperiment"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteExperiment with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询 pin 下有权限的工作空间下的训练任务列表。

管理员接口，可跨工作空间查询训练任务列表。

## 过滤条件

- &#x60;jobId&#x60;：任务ID，精确匹配，支持多个
- &#x60;state&#x60;：任务状态，精确匹配，支持多个
- &#x60;workspaceId&#x60;：工作空间ID，精确匹配，支持多个
- &#x60;permission&#x60;：权限类型，精确匹配
- &#x60;ownerUserPin&#x60;：所有者pin，精确匹配
- &#x60;ownerUser&#x60;：所有者名称，模糊匹配

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;jobId&#x60;: 训练任务ID，精确匹配，支持多个。
&#x60;state&#x60;: 训练任务的状态，精确匹配，支持多个。
&#x60;workspaceId&#x60;: 工作空间ID，精确匹配，支持多个。
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminJobListInfo jobListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  adminDescribeJobs (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeJobs"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeJobs"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeJobs with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/api/{apiType}/jobs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询 pin 下所有的资源，按训练任务状态统计数量。

管理员接口，用于快速了解当前账号下各状态的资源数量分布。

## 使用场景

- 资源概览监控
- 任务状态分布分析
- 资源使用统计

## 过滤条件

- &#x60;workspaceId&#x60;：工作空间ID，精确匹配
- &#x60;state&#x60;：任务状态，精确匹配，支持多个
- &#x60;permission&#x60;：权限类型，精确匹配
- &#x60;ownerUserPin&#x60;：所有者pin，精确匹配
- &#x60;ownerUser&#x60;：所有者名称，模糊匹配

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;workspaceId&#x60;: 工作空间ID，精确匹配，支持单个。
&#x60;state&#x60;: 训练任务的状态，精确匹配，支持多个。
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminJobStateCount stateCount
      */

  adminDescribeJobsCount (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeJobsCount"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeJobsCount"
      )
    }

    let postBody = null
    let queryParams = {}
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeJobsCount with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/api/{apiType}/jobs:count',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询当前主账号下所有的训练任务列表。

管理员接口，可跨工作空间查询主账号下所有训练任务。

## 过滤条件

- &#x60;jobId&#x60;：任务ID，精确匹配，支持多个
- &#x60;permission&#x60;：权限类型，精确匹配
- &#x60;ownerUserPin&#x60;：所有者pin，精确匹配
- &#x60;ownerUser&#x60;：所有者名称，模糊匹配

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;jobId&#x60;: 训练任务ID，精确匹配，支持多个。
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminJobListInfo jobListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  adminDescribeJobList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeJobList"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeJobList"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeJobList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/api/{apiType}/jobList',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询训练任务类型列表。

获取当前系统支持的所有训练任务类型。不同类型的训练任务对应不同的底层调度框架和资源管理方式。

**支持的训练任务类型：**
- &#x60;pytorch&#x60;：PyTorch 框架，适用于深度学习训练、分布式训练
- &#x60;ray&#x60;：Ray 框架，适用于分布式计算、强化学习、超参调优

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string jobTypes  训练任务类型。
      */

  describeJobTypes (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeJobTypes"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeJobTypes"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeJobTypes with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobTypes',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询训练任务列表。

分页查询工作空间下的训练任务列表，支持多种过滤条件。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;jobId&#x60;: 训练任务ID，精确匹配，支持多个。
&#x60;name&#x60;: 训练任务名称，模糊匹配，支持单个。
&#x60;jobType&#x60;: 训练任务类型，精确匹配，支持多个。
&#x60;state&#x60;: 训练任务的状态，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个。
&#x60;queueId&#x60;: 训练任务所属的队列ID，精确匹配，支持多个。
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param jobListInfoForJob jobListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeJobs (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeJobs"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeJobs"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeJobs with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建训练任务。

创建一个新的训练任务，用于执行机器学习模型的训练工作。支持多种数据源、模型和计算资源配置。

## 核心配置项

- **基本信息**：任务名称、描述、框架类型
- **镜像配置**：镜像可见性、镜像ID、镜像地址
- **启动命令**：训练脚本执行命令和环境变量
- **资源配置**：队列、GPU/CPU/内存、节点数量
- **存储配置**：OSS/CFS/JPFS 存储挂载
- **数据与模型**：数据集、模型、代码仓库配置
- **高级配置**：重启策略（仅异构节点池+PyTorch）、健康检测

## 创建流程

1. 提交创建请求，系统进行参数校验
2. 返回任务ID，任务进入 **排队中(queuing)** 状态
3. 资源就绪后自动进入 **启动中(pending)** 状态
4. 启动成功后进入 **运行中(running)** 状态开始训练

## 注意事项

- &#x60;resource&#x60; 参数已废弃
- Ray 任务必须使用 &#x60;roleResource&#x60; 配置 Head 和 Worker 角色
- **重启策略仅适用于异构节点池的 PyTorch 任务**，云主机资源池和 Ray 任务不支持

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {jobParam} opts.jobParam - 训练任务参数。
      * @param {logCollectConfig} [opts.logCollectConfig] - 日志采集配置。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string jobId  训练任务ID。
      */

  createJob (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createJob"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createJob"
      )
    }
    if (opts.jobParam === undefined || opts.jobParam === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobParam' when calling createJob"
      )
    }

    let postBody = {}
    if (opts.jobParam !== undefined && opts.jobParam !== null) {
      postBody['jobParam'] = opts.jobParam
    }
    if (opts.logCollectConfig !== undefined && opts.logCollectConfig !== null) {
      postBody['logCollectConfig'] = opts.logCollectConfig
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createJob with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询训练任务详情。

获取训练任务的完整配置信息和运行状态，包括基本信息、镜像配置、资源配置、存储配置、运行配置、Pod信息等。

## 使用场景

- 查看任务的完整配置信息
- 排查任务运行问题
- 获取 Pod 终端访问地址

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param jobDetailInfoForJob jobDetailInfo  训练任务详情。
      */

  describeJob (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeJob"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeJob"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling describeJob"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeJob with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新训练任务属性信息。

仅支持更新任务的元数据属性（名称、描述、权限、归属），不影响正在运行的训练任务。

## 使用场景

- 修改任务名称以便更好地识别和管理
- 更新任务描述信息
- 调整资源的可见性权限
- 转移任务归属给其他用户

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {string} [opts.name] - 训练任务名称。1~32字符，仅支持中文、大小写字母、数字、英文中划线 &quot;-&quot;和英文下划线&quot;_&quot;。  optional
      * @param {string} [opts.description] - 训练任务的描述信息，不超过256个字符。  optional
      * @param {string} [opts.permission] - 工作空间中的资源归属权限，支持(public,private)，默认为public。  optional
      * @param {string} [opts.ownerUserPin] - 归属用户pin。
可配置任务所属主账号pin，或者工作空间其他成员的子账号pin
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateJob (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateJob"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateJob"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling updateJob"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (opts.permission !== undefined && opts.permission !== null) {
      postBody['permission'] = opts.permission
    }
    if (opts.ownerUserPin !== undefined && opts.ownerUserPin !== null) {
      postBody['ownerUserPin'] = opts.ownerUserPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateJob with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除训练任务。

删除训练任务将释放所有相关资源，删除后无法恢复。

## 可删除的任务状态

- ✅ queuing、pending、running、success、failed、stopped
- ❌ stopping、deleting、rolling-back（需等待操作完成）

## 注意事项

- 删除操作不可恢复，请确保已备份重要数据
- 删除任务不会删除关联的存储数据、数据集和模型

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteJob (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteJob"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteJob"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling deleteJob"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteJob with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止训练任务。

手动停止正在运行或等待中的训练任务。停止后任务将释放所有计算资源，且无法再次启动。

## 可停止的任务状态

- ✅ queuing、pending、running
- ❌ 已停止、已成功、已失败的任务无需停止

## 注意事项

- 停止操作**不可逆**，停止后无法恢复或重新启动任务
- 所有计算资源将被释放，存储数据不会被删除
- 如需保存训练进度，请确保代码支持检查点保存

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  stopJob (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  stopJob"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling stopJob"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling stopJob"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call stopJob with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}:stop',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取训练任务事件。

查询训练任务执行过程中的各类事件记录，用于监控任务状态变化和问题排查。

## 常见事件

- &#x60;Scheduled&#x60;：Pod 已调度到节点
- &#x60;Pulled/Pulling&#x60;：镜像拉取
- &#x60;Started/Created&#x60;：容器启动
- &#x60;FailedScheduling&#x60;：调度失败（资源不足等）
- &#x60;FailedMount&#x60;：存储挂载失败
- &#x60;BackOff&#x60;：容器启动失败重试中

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {string} [opts.instanceName] - 实例名称。  optional
      * @param {string} [opts.startTime] - 事件开始时间。  optional
      * @param {string} [opts.endTime] - 事件结束时间。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param eventInfoListForJob eventInfoList
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  getJobEvents (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getJobEvents"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getJobEvents"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling getJobEvents"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.instanceName !== undefined && opts.instanceName !== null) {
      queryParams['instanceName'] = opts.instanceName
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      queryParams['startTime'] = opts.startTime
    }
    if (opts.endTime !== undefined && opts.endTime !== null) {
      queryParams['endTime'] = opts.endTime
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getJobEvents with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}:events',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取训练任务重启历史记录。

查询训练任务的所有重启记录，包括每次重启的触发条件、时间和原因。用于分析任务稳定性和问题排查。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param restartHistoryListForJob restartHistoryList
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  getJobRestartHistory (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getJobRestartHistory"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getJobRestartHistory"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling getJobRestartHistory"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getJobRestartHistory with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}:restartHistory',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取训练任务关联的 Pod 日志（流式返回）。

通过 SSE（Server-Sent Events）技术实时推送训练日志，适用于实时监控训练进度。

## 特点

- **实时性**：日志实时推送，无需轮询
- **持续连接**：保持长连接，适合长时间训练任务
- **自动重连**：客户端断开后可自动重连

## 使用场景

- 实时监控训练进度
- 调试训练脚本
- 观察训练指标变化

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {string} opts.podName - pod名称
      * @param {integer} [opts.tailLines] - 从最后多少条开始返回日志，默认100条。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  getPodLogs (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getPodLogs"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getPodLogs"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling getPodLogs"
      )
    }
    if (opts.podName === undefined || opts.podName === null) {
      throw new Error(
        "Missing the required parameter 'opts.podName' when calling getPodLogs"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.tailLines !== undefined && opts.tailLines !== null) {
      queryParams['tailLines'] = opts.tailLines
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId,
      podName: opts.podName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getPodLogs with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}/pods/{podName}:logs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询训练任务实例列表。

获取训练任务下所有运行实例（Pod）的详细信息，包括状态、IP、运行时长等。

## 使用场景

- 查看各实例的运行状态
- 获取实例终端访问地址
- 排查分布式训练中的节点问题

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {boolean} [opts.anomalyFirst] - 异常状态数据优先展示；默认为false  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;name&#x60;: 实例名称，模糊匹配，支持单个。
&#x60;type&#x60;: 实例类型，模糊匹配，支持单个。
&#x60;state&#x60;: 实例状态，精确匹配，支持多个，用&quot;,&quot;隔开。
&#x60;ip&#x60;: 实例Ip，精确匹配，支持多个，用&quot;,&quot;隔开。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param instanceListInfoForJob podListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeInstances (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeInstances"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeInstances"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling describeInstances"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.anomalyFirst !== undefined && opts.anomalyFirst !== null) {
      queryParams['anomalyFirst'] = opts.anomalyFirst
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeInstances with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}:instances',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取训练任务实例事件。

查询单个训练实例（Pod）的事件记录，用于深入排查特定实例的问题。

## 使用场景

- 排查特定实例启动失败原因
- 分析实例异常行为
- 定位存储挂载问题
- 诊断资源不足问题

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.jobId - 训练任务ID
      * @param {string} opts.instanceName - 实例名称
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {string} [opts.startTime] - 事件开始时间。  optional
      * @param {string} [opts.endTime] - 事件结束时间。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param eventInfoListForInstance eventInfoList
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  getInstanceEvents (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getInstanceEvents"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getInstanceEvents"
      )
    }
    if (opts.jobId === undefined || opts.jobId === null) {
      throw new Error(
        "Missing the required parameter 'opts.jobId' when calling getInstanceEvents"
      )
    }
    if (opts.instanceName === undefined || opts.instanceName === null) {
      throw new Error(
        "Missing the required parameter 'opts.instanceName' when calling getInstanceEvents"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      queryParams['startTime'] = opts.startTime
    }
    if (opts.endTime !== undefined && opts.endTime !== null) {
      queryParams['endTime'] = opts.endTime
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      jobId: opts.jobId,
      instanceName: opts.instanceName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getInstanceEvents with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/jobs/{jobId}/instances/{instanceName}:events',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询 pin 下有权限的工作空间下的微调运行列表。

管理员接口，可跨工作空间查询微调运行列表。

## 过滤条件

- &#x60;runId&#x60;：运行ID，精确匹配，支持多个
- &#x60;state&#x60;：运行状态，精确匹配，支持多个

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;runId&#x60;: 微调运行ID，精确匹配，支持多个。
&#x60;state&#x60;: 微调运行的状态，精确匹配，支持多个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminRunListInfo runListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  adminDescribeRuns (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeRuns"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeRuns"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeRuns with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/api/{apiType}/runs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询 pin 下所有的资源，按微调运行状态统计数量。

管理员接口，用于快速了解当前账号下各状态的资源数量分布。

## 使用场景

- 资源概览监控
- 运行状态分布分析
- 资源使用统计

## 过滤条件

- &#x60;workspaceId&#x60;：工作空间ID，精确匹配
- &#x60;state&#x60;：运行状态，精确匹配，支持多个

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;workspaceId&#x60;: 工作空间ID，精确匹配，支持单个。
&#x60;state&#x60;: 微调运行的状态，精确匹配，支持多个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminRunStateCount stateCount
      */

  adminDescribeRunsCount (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeRunsCount"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeRunsCount"
      )
    }

    let postBody = null
    let queryParams = {}
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeRunsCount with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/api/{apiType}/runs:count',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询当前主账号下所有的微调运行列表。

管理员接口，可跨工作空间查询主账号下所有微调运行。

## 过滤条件

- &#x60;runId&#x60;：运行ID，精确匹配，支持多个

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;runId&#x60;: 微调运行ID，精确匹配，支持多个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminRunListInfo runListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  adminDescribeRunList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeRunList"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeRunList"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeRunList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/api/{apiType}/runList',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询微调运行配置参数列表。

获取指定模型模板支持的训练配置参数，包括训练阶段、微调方法和详细参数列表。

## 使用场景

- 创建微调运行前获取支持的配置选项
- 了解各参数的默认值和取值范围
- 根据模型模板获取定制化的参数配置

## 返回内容

- &#x60;stage&#x60;：支持的训练阶段列表
- &#x60;finetuningType&#x60;：各阶段支持的微调方法
- &#x60;extras&#x60;：各微调方法支持的详细参数配置

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} [opts.modelTemplate] - 模型模版，严格区分大小写。
| 模型名                                                             | 参数量                            | modelTemplate       |
| ----------------------------------------------------------------- | -------------------------------- | ------------------- |
| [Llama 3-3.3](https://huggingface.co/meta-llama)                  | 1B/3B/8B/70B                     | llama3              |
| [Qwen (1-2.5) (Code/Math/MoE/QwQ)](https://huggingface.co/Qwen)   | 0.5B/1.5B/3B/7B/14B/32B/72B/110B | qwen                |
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param object runConfigInfo  微调运行配置参数。
      */

  describeRunConfigs (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeRunConfigs"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeRunConfigs"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.modelTemplate !== undefined && opts.modelTemplate !== null) {
      queryParams['modelTemplate'] = opts.modelTemplate
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeRunConfigs with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/runConfigs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询微调运行列表。

分页查询指定实验下的微调运行列表，支持多种过滤条件。

## 过滤条件

支持 &#x60;filters&#x60; 参数进行过滤：
- &#x60;runId&#x60;：运行ID，精确匹配，支持多个
- &#x60;name&#x60;：运行名称，模糊匹配，支持单个
- &#x60;state&#x60;：运行状态，精确匹配，支持多个
- &#x60;createUser&#x60;：创建人，模糊匹配

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;runId&#x60;: 微调运行ID，精确匹配，支持多个。
&#x60;name&#x60;: 微调运行名称，模糊匹配，支持单个。
&#x60;state&#x60;: 微调运行的状态，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人，模糊匹配，支持单个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param runListInfoForExperiment runListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeRuns (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeRuns"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeRuns"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling describeRuns"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeRuns with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}/runs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建微调运行。

在指定的微调实验下创建一个新的微调运行任务。

## 核心配置

- **基本信息**：运行名称、描述
- **微调配置**：训练阶段、微调方法、训练参数
- **数据集配置**：训练数据集、验证数据集
- **资源配置**：队列、GPU/CPU/内存
- **存储配置**：保存模型和训练输出

## 创建流程

1. 提交创建请求，系统进行参数校验
2. 返回运行ID，运行进入 **排队中(queuing)** 状态
3. 资源就绪后自动进入 **启动中(pending)** 状态
4. 启动成功后进入 **运行中(running)** 状态开始训练

## 注意事项

- 训练数据集为必填项
- 验证数据集可通过拆分训练集或单独指定
- 存储空间用于保存训练后的模型文件

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {runParamForExperiment} opts.runParam - 微调运行参数。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string runId  微调运行ID。
      */

  createRun (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createRun"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createRun"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling createRun"
      )
    }
    if (opts.runParam === undefined || opts.runParam === null) {
      throw new Error(
        "Missing the required parameter 'opts.runParam' when calling createRun"
      )
    }

    let postBody = {}
    if (opts.runParam !== undefined && opts.runParam !== null) {
      postBody['runParam'] = opts.runParam
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createRun with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}/runs',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除微调运行。

删除微调运行将释放相关资源，删除后无法恢复。

## 可删除条件

- **私有队列**：任何状态都可以删除
- **公共队列**：仅 queuing、pending、success、failed、stopped 状态可删除

## 注意事项

- 删除操作不可恢复
- 训练中的运行需要先停止再删除

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {string} opts.runId - 微调运行ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteRun (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteRun"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteRun"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling deleteRun"
      )
    }
    if (opts.runId === undefined || opts.runId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runId' when calling deleteRun"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId,
      runId: opts.runId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteRun with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}/runs/{runId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止微调运行。

手动停止正在运行或等待中的微调运行。停止后运行将释放计算资源，且无法再次启动。

## 可停止条件

- **私有队列**：任何状态都可以停止
- **公共队列**：仅 queuing、pending 状态可停止

## 注意事项

- 停止操作不可逆
- 如需保存训练进度，请确保代码支持检查点保存

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {string} opts.runId - 微调运行ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  stopRun (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  stopRun"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling stopRun"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling stopRun"
      )
    }
    if (opts.runId === undefined || opts.runId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runId' when calling stopRun"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId,
      runId: opts.runId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call stopRun with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}/runs/{runId}:stop',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取微调运行事件。

查询微调运行执行过程中的各类事件记录，用于监控运行状态变化和问题排查。

## 使用场景

- 监控训练进度
- 排查启动失败问题
- 分析资源调度情况

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {string} opts.runId - 微调运行ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {string} [opts.startTime] - 事件开始时间。  optional
      * @param {string} [opts.endTime] - 事件结束时间。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param eventInfoListForExperiment runEventInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  getRunEvents (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getRunEvents"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getRunEvents"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling getRunEvents"
      )
    }
    if (opts.runId === undefined || opts.runId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runId' when calling getRunEvents"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      queryParams['startTime'] = opts.startTime
    }
    if (opts.endTime !== undefined && opts.endTime !== null) {
      queryParams['endTime'] = opts.endTime
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId,
      runId: opts.runId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getRunEvents with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}/runs/{runId}:events',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取微调运行关联的 Pod 日志（流式返回）。

通过 SSE（Server-Sent Events）技术实时推送训练日志，适用于实时监控训练进度。

## 特点

- **实时性**：日志实时推送，无需轮询
- **持续连接**：保持长连接，适合长时间训练任务
- **自动重连**：客户端断开后可自动重连

## 使用场景

- 实时监控训练进度
- 调试训练参数
- 观察训练指标变化

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.experimentId - 微调实验ID
      * @param {string} opts.runId - 微调运行ID
      * @param {string} opts.podName - pod名称
      * @param {integer} [opts.tailLines] - 从最后多少条开始返回日志，默认100条。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  getPodLogs (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getPodLogs"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getPodLogs"
      )
    }
    if (opts.experimentId === undefined || opts.experimentId === null) {
      throw new Error(
        "Missing the required parameter 'opts.experimentId' when calling getPodLogs"
      )
    }
    if (opts.runId === undefined || opts.runId === null) {
      throw new Error(
        "Missing the required parameter 'opts.runId' when calling getPodLogs"
      )
    }
    if (opts.podName === undefined || opts.podName === null) {
      throw new Error(
        "Missing the required parameter 'opts.podName' when calling getPodLogs"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.tailLines !== undefined && opts.tailLines !== null) {
      queryParams['tailLines'] = opts.tailLines
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      experimentId: opts.experimentId,
      runId: opts.runId,
      podName: opts.podName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getPodLogs with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/train/{regionId}/workspaces/{workspaceId}/experiments/{experimentId}/runs/{runId}/pods/{podName}:logs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据集列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;datasetName&#x60;: 数据集名称，模糊匹配，支持单个
&#x60;description&#x60;: 数据集描述，模糊匹配，支持单个
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个
&#x60;datasetTypes&#x60;: 数据集类型数组，支持多个，取值范围：[&#x60;text&#x60;, &#x60;custom&#x60;]
&#x60;datasetIds&#x60;: 数据集ID数组，支持多个
&#x60;taskTypes&#x60;: 任务类型数组，支持多个，取值范围：[&#x60;sft&#x60;, &#x60;dpo&#x60;, &#x60;cpt&#x60;, &#x60;custom&#x60;]
&#x60;labels&#x60;: 标签数组，支持多个
&#x60;cfsVpcIds&#x60;: cfsVpcId数组，支持多个
&#x60;states&#x60;: 资源状态数组，支持多个，取值范围：[&#x60;running&#x60;, &#x60;success&#x60;, &#x60;failed&#x60;]
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      * @param datasetInfo data
      */

  describeDatasets (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeDatasets"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeDatasets"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeDatasets with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建数据集/新版本
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {createDatasetParam} opts.datasetSpec - 创建数据集的参数
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string datasetId  数据集ID
      * @param string version  数据集版本
      */

  createDataset (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createDataset"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createDataset"
      )
    }
    if (opts.datasetSpec === undefined || opts.datasetSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetSpec' when calling createDataset"
      )
    }

    let postBody = {}
    if (opts.datasetSpec !== undefined && opts.datasetSpec !== null) {
      postBody['datasetSpec'] = opts.datasetSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createDataset with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据集详情
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.datasetId - 数据集ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param datasetInfo dataset
      */

  describeDataset (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeDataset"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeDataset"
      )
    }
    if (opts.datasetId === undefined || opts.datasetId === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetId' when calling describeDataset"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      datasetId: opts.datasetId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeDataset with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset/{datasetId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改数据集信息
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.datasetId - 数据集ID
      * @param {updateDatasetParam} opts.updateDatasetSpec - 修改数据集的参数
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateDataset (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateDataset"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateDataset"
      )
    }
    if (opts.datasetId === undefined || opts.datasetId === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetId' when calling updateDataset"
      )
    }
    if (
      opts.updateDatasetSpec === undefined ||
      opts.updateDatasetSpec === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.updateDatasetSpec' when calling updateDataset"
      )
    }

    let postBody = {}
    if (
      opts.updateDatasetSpec !== undefined &&
      opts.updateDatasetSpec !== null
    ) {
      postBody['updateDatasetSpec'] = opts.updateDatasetSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      datasetId: opts.datasetId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateDataset with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset/{datasetId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据数据集ID删除数据集
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.datasetId - 数据集ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteDataset (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteDataset"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteDataset"
      )
    }
    if (opts.datasetId === undefined || opts.datasetId === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetId' when calling deleteDataset"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      datasetId: opts.datasetId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteDataset with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset/{datasetId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据集的版本列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.datasetId - 数据集ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;versions&#x60;: 版本号 数组，支持多个
&#x60;taskTypes&#x60;: 任务类型数组，支持多个，取值范围：[&#x60;sft&#x60;, &#x60;dpo&#x60;, &#x60;cpt&#x60;, &#x60;custom&#x60;,&#x60;distill&#x60;]
&#x60;storageTypes&#x60;: 存储类型（数据来源）：oss\cfs\jpfs 数组，支持多个
&#x60;taskTypes&#x60;: 任务类型数组，支持多个，取值范围：[&#x60;sft&#x60;, &#x60;dpo&#x60;, &#x60;cpt&#x60;, &#x60;custom&#x60;,&#x60;distill&#x60;]
&#x60;labelsIn&#x60;: 标签，多个标签查询，有一个标签匹配上就行
&#x60;labelsEq&#x60;: 标签，多个标签查询，每一个标签都要匹配到
&#x60;cfsVpcIds&#x60;: cfsVpcId数组，支持多个
&#x60;states&#x60;: 资源状态数组，支持多个，取值范围：[&#x60;running&#x60;, &#x60;success&#x60;, &#x60;failed&#x60;]
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      * @param datasetVersionInfo data
      */

  describeDatasetVersions (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeDatasetVersions"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeDatasetVersions"
      )
    }
    if (opts.datasetId === undefined || opts.datasetId === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetId' when calling describeDatasetVersions"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      datasetId: opts.datasetId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeDatasetVersions with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset/{datasetId}/version',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据集版本详情
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.datasetId - 数据集ID
      * @param {string} opts.version - 数据集版本
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param datasetVersionInfo data
      */

  describeDatasetVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeDatasetVersion"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeDatasetVersion"
      )
    }
    if (opts.datasetId === undefined || opts.datasetId === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetId' when calling describeDatasetVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling describeDatasetVersion"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      datasetId: opts.datasetId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeDatasetVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset/{datasetId}/version/{version}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改数据集版本信息
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.datasetId - 数据集ID
      * @param {string} opts.version - 数据集版本
      * @param {updateDatasetVersionParam} opts.updateDatasetVersionSpec - 修改数据集版本的参数
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateDatasetVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateDatasetVersion"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateDatasetVersion"
      )
    }
    if (opts.datasetId === undefined || opts.datasetId === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetId' when calling updateDatasetVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling updateDatasetVersion"
      )
    }
    if (
      opts.updateDatasetVersionSpec === undefined ||
      opts.updateDatasetVersionSpec === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.updateDatasetVersionSpec' when calling updateDatasetVersion"
      )
    }

    let postBody = {}
    if (
      opts.updateDatasetVersionSpec !== undefined &&
      opts.updateDatasetVersionSpec !== null
    ) {
      postBody['updateDatasetVersionSpec'] = opts.updateDatasetVersionSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      datasetId: opts.datasetId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateDatasetVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset/{datasetId}/version/{version}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除数据集版本
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.datasetId - 数据集ID
      * @param {string} opts.version - 数据集版本
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteDatasetVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteDatasetVersion"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteDatasetVersion"
      )
    }
    if (opts.datasetId === undefined || opts.datasetId === null) {
      throw new Error(
        "Missing the required parameter 'opts.datasetId' when calling deleteDatasetVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling deleteDatasetVersion"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      datasetId: opts.datasetId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteDatasetVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ds-regions/{regionId}/workspace/{workspaceId}/dataset/{datasetId}/version/{version}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询pin下有权限的工作空间下的蒸馏任务列表。

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1, 500]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;taskId&#x60;: 蒸馏任务ID，精确匹配，支持多个。
&#x60;taskState&#x60;: 蒸馏任务的状态，精确匹配，支持多个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminDistillTaskListInfo runListInfo
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  adminDescribeDistillTasks (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeDistillTasks"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeDistillTasks"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeDistillTasks with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/dt-regions/{regionId}/api/{apiType}/dttasks',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取镜像列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;imageName&#x60;: 镜像名称，模糊匹配，支持单个
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个
&#x60;imageTypes&#x60;: 镜像类型，支持多个，取值范围：[&#x60;gpu&#x60;, &#x60;cpu&#x60;]
&#x60;imageIds&#x60;: 镜像ID数组，支持多个
&#x60;imageUsages&#x60;: 镜像用途，支持多个，取值范围：[notebook Notebook、training 开发训练、inference 在线服务]
&#x60;labels&#x60;: 标签数组，支持多个
&#x60;states&#x60;: 状态数组，支持多个，取值范围：[资源状态:queuing排队中、pending启动中、running构建中、success注册成功、failed构建失败、deleting删除中、deleted已删除]
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      * @param imageInfo data
      */

  describeImages (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeImages"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeImages"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeImages with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/img-regions/{regionId}/workspace/{workspaceId}/image',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建镜像
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {createImageParam} opts.createImageSpec - 创建镜像的参数
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string imageId  镜像ID
      */

  createImage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createImage"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createImage"
      )
    }
    if (opts.createImageSpec === undefined || opts.createImageSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.createImageSpec' when calling createImage"
      )
    }

    let postBody = {}
    if (opts.createImageSpec !== undefined && opts.createImageSpec !== null) {
      postBody['createImageSpec'] = opts.createImageSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createImage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/img-regions/{regionId}/workspace/{workspaceId}/image',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取镜像详情
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.imageId - 镜像ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param imageInfo data
      */

  describeImage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeImage"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeImage"
      )
    }
    if (opts.imageId === undefined || opts.imageId === null) {
      throw new Error(
        "Missing the required parameter 'opts.imageId' when calling describeImage"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      imageId: opts.imageId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeImage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/img-regions/{regionId}/workspace/{workspaceId}/image/{imageId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改镜像信息
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.imageId - 镜像ID
      * @param {updatePrivateImageParam} opts.updateImageSpec - 修改镜像的参数
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateImage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateImage"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateImage"
      )
    }
    if (opts.imageId === undefined || opts.imageId === null) {
      throw new Error(
        "Missing the required parameter 'opts.imageId' when calling updateImage"
      )
    }
    if (opts.updateImageSpec === undefined || opts.updateImageSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.updateImageSpec' when calling updateImage"
      )
    }

    let postBody = {}
    if (opts.updateImageSpec !== undefined && opts.updateImageSpec !== null) {
      postBody['updateImageSpec'] = opts.updateImageSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      imageId: opts.imageId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateImage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/img-regions/{regionId}/workspace/{workspaceId}/image/{imageId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据镜像ID删除镜像
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.imageId - 镜像ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteImage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteImage"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteImage"
      )
    }
    if (opts.imageId === undefined || opts.imageId === null) {
      throw new Error(
        "Missing the required parameter 'opts.imageId' when calling deleteImage"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      imageId: opts.imageId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteImage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/img-regions/{regionId}/workspace/{workspaceId}/image/{imageId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取构建镜像的pod日志，采用SSE流式返回。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.imageId - 镜像ID
      * @param {string} opts.podName - pod name
      * @param {integer} [opts.tailLines] - 从最后多少条开始返回日志，默认100条。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  getPodLogs (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getPodLogs"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getPodLogs"
      )
    }
    if (opts.imageId === undefined || opts.imageId === null) {
      throw new Error(
        "Missing the required parameter 'opts.imageId' when calling getPodLogs"
      )
    }
    if (opts.podName === undefined || opts.podName === null) {
      throw new Error(
        "Missing the required parameter 'opts.podName' when calling getPodLogs"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.tailLines !== undefined && opts.tailLines !== null) {
      queryParams['tailLines'] = opts.tailLines
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      imageId: opts.imageId,
      podName: opts.podName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getPodLogs with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/img-regions/{regionId}/workspace/{workspaceId}/image/{imageId}/pods/{podName}:logs',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取公共镜像列表
      * @param {Object} opts - parameters
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;imageName&#x60;: 镜像名称，模糊匹配，支持单个
&#x60;imageTypes&#x60;: 镜像类型，支持多个，取值范围：[&#x60;gpu&#x60;, &#x60;cpu&#x60;]
&#x60;imageIds&#x60;: 镜像ID数组，支持多个
&#x60;imageUsages&#x60;: 镜像用途，支持多个，取值范围：[notebook Notebook、training 开发训练、inference 在线服务]
&#x60;labelsIn&#x60;: 标签，多个标签查询，有一个标签匹配上就行
&#x60;labelsEq&#x60;: 标签，多个标签查询，每一个标签都要匹配到
&#x60;labelsLikeIn&#x60;: 标签(模糊查询)，多个标签查询，有一个标签匹配上就行
&#x60;labelsLikeEq&#x60;: 标签(模糊查询)，多个标签查询，每一个标签都要匹配到
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      * @param publicImageInfo data
      */

  describePublicImages (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describePublicImages"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describePublicImages with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/img-regions/{regionId}/publicImage',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  发起推理服务滚动更新
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} opts.inferenceId - 推理服务ID
      * @param {string} opts.workspaceId - workspaceID
      * @param {rolloutInferenceSpec} opts.rolloutInferenceSpec
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string rolloutId  滚动更新ID
      */

  rolloutInference (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  rolloutInference"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling rolloutInference"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling rolloutInference"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling rolloutInference"
      )
    }
    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling rolloutInference"
      )
    }
    if (
      opts.rolloutInferenceSpec === undefined ||
      opts.rolloutInferenceSpec === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.rolloutInferenceSpec' when calling rolloutInference"
      )
    }

    let postBody = {}
    if (
      opts.rolloutInferenceSpec !== undefined &&
      opts.rolloutInferenceSpec !== null
    ) {
      postBody['rolloutInferenceSpec'] = opts.rolloutInferenceSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId,
      inferenceId: opts.inferenceId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call rolloutInference with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/rollout',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  暂停推理服务滚动更新
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} opts.inferenceId - 推理服务ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  pauseRollout (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  pauseRollout"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling pauseRollout"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling pauseRollout"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling pauseRollout"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call pauseRollout with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/pause',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取推理服务滚动更新记录
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} opts.inferenceId - 推理服务ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1  optional
      * @param {integer} [opts.pageSize] - 分页大小；默认为20；取值范围[1,100]  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param rolloutInfo rollouts
      * @param integer total  总记录数
      */

  describeInferenceRollouts (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeInferenceRollouts"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeInferenceRollouts"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling describeInferenceRollouts"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling describeInferenceRollouts"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeInferenceRollouts with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/rollouts',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取推理服务列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为20；取值范围[1, 100]。  optional
      * @param {string} [opts.name] - 推理服务名称  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;inferenceId&#x60;: 版本ID，精确匹配，支持多个。
&#x60;version&#x60;: 版本号，精确匹配，支持多个。
&#x60;status&#x60;: 状态，精确匹配，支持多个。
&#x60;queueId&#x60;: 队列Id，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人，模糊匹配，支持单个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param inferenceData inferences
      * @param integer totalCount  推理服务总数
      */

  describeInferences (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeInferences"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeInferences"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.name !== undefined && opts.name !== null) {
      queryParams['name'] = opts.name
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeInferences with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建推理服务
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {inferenceSpec} opts.inferenceSpec - 创建推理服务的规格（与请求 JSON 字段 inferenceSpec 对应）
      * @param {logCollectConfig} [opts.logCollectConfig] - 日志采集配置  optional
      * @param {extendInferenceRequest} [opts.extend] - 推理服务扩展信息（如用户标签、资源组等）  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string inferenceId  Inference ID
      */

  createInference (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createInference"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createInference"
      )
    }
    if (opts.inferenceSpec === undefined || opts.inferenceSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceSpec' when calling createInference"
      )
    }

    let postBody = {}
    if (opts.inferenceSpec !== undefined && opts.inferenceSpec !== null) {
      postBody['inferenceSpec'] = opts.inferenceSpec
    }
    if (opts.logCollectConfig !== undefined && opts.logCollectConfig !== null) {
      postBody['logCollectConfig'] = opts.logCollectConfig
    }
    if (opts.extend !== undefined && opts.extend !== null) {
      postBody['extend'] = opts.extend
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createInference with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取推理服务详情
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string inferenceId  服务ID
      * @param string deployType  部署方式（自定义,或者大语言模型）
      * @param integer trafficPercent  流量占比
      * @param string status  服务状态
      * @param integer qps  推理服务限流阈值（QPS）
      * @param serviceInfo service  推理服务相关信息
      * @param metadata metadata  推理服务元数据（如：名字，ID等）
      * @param model models
      * @param code codes
      * @param string regionId  地域ID
      * @param roleSet roleSet  多角色部署配置
      * @param resource resource  推理服务所需资源（如：CPU， 内存等）
      * @param runtime runtime  推理服务运行环境（如：镜像，命令行，环境变量等）
      * @param charge charge  计费信息
      * @param logCollectConfigDetail logCollectConfig  日志采集配置
      * @param userTag userTags
      * @param schedulePriority schedulePriority  调度优先级,非必填，默认使用系统优先级：normal-priority(10000)
      * @param resourceGroupInfo resourceGroupInfo  资源组信息
      * @param rolloutInfo rolloutInfo  滚动更新参数
      */

  describeInference (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeInference"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeInference"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling describeInference"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeInference with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新推理服务描述
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {updateInfo} [opts.updateSpec]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      */

  updateInference (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateInference"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateInference"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling updateInference"
      )
    }

    let postBody = {}
    if (opts.updateSpec !== undefined && opts.updateSpec !== null) {
      postBody['updateSpec'] = opts.updateSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateInference with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除推理服务
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      */

  deleteInference (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteInference"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteInference"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling deleteInference"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteInference with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  启动推理服务
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      */

  startInference (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  startInference"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling startInference"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling startInference"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call startInference with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/start',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止推理服务
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      */

  stopInference (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  stopInference"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling stopInference"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling stopInference"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call stopInference with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/stop',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取推理服务事件
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为20；取值范围[1, 100]。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      * @param event events
      * @param integer totalCount  该推理服务下的事件总数
      */

  describeInferenceEvents (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeInferenceEvents"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeInferenceEvents"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling describeInferenceEvents"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeInferenceEvents with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/events',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取推理服务列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为20；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;serviceId&#x60;: 服务ID，精确匹配，支持多个。
&#x60;name&#x60;: 名称，模糊匹配，支持单个。
&#x60;deployType&#x60;: 部署方式，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人，模糊匹配，支持单个。
&#x60;permission&#x60;: 是否公开，public: 公开，private：私有
&#x60;ownerUser&#x60;: 所有者，模糊匹配，支持单个。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持单个
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param service services
      * @param integer totalCount  推理服务总数
      */

  describeServices (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeServices"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeServices"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeServices with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/services',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取推理服务详情
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.serviceName - 推理服务名称。
      * @param {string} opts.serviceName - 推理服务名称。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string createTime  服务创建时间
      * @param string deployType  部署类型（custom, llm)
      * @param string name  服务名称
      * @param integer running  该服务中运行的版本数
      * @param string serviceId  服务ID
      * @param string address  推理服务总的调用地址
      * @param string accessToken  访问服务的Bearer Token
      * @param string gatewayType  网关类型，shared:共享网关, dedicated:专属网关
      * @param integer qps  推理服务限流阈值（QPS）
      * @param boolean enableLimit  开启 true 关闭 false
      * @param integer total  该服务中总版本数
      * @param string ownerUser  服务的所有者
      * @param string ownerUserPin  服务的所有者PIN
      * @param string createUser  创建服务的用户
      * @param string createUserPin  创建服务的用户PIN
      */

  describeService (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeService"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeService"
      )
    }
    if (opts.serviceName === undefined || opts.serviceName === null) {
      throw new Error(
        "Missing the required parameter 'opts.serviceName' when calling describeService"
      )
    }
    if (opts.serviceName === undefined || opts.serviceName === null) {
      throw new Error(
        "Missing the required parameter 'opts.serviceName' when calling describeService"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      serviceName: opts.serviceName,
      serviceName: opts.serviceName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeService with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/services/{serviceName}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新推理服务
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.serviceName - 推理服务名称。
      * @param {updateInferenceServiceSpec} [opts.updateInferenceServiceSpec]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      */

  updateService (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateService"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateService"
      )
    }
    if (opts.serviceName === undefined || opts.serviceName === null) {
      throw new Error(
        "Missing the required parameter 'opts.serviceName' when calling updateService"
      )
    }

    let postBody = {}
    if (
      opts.updateInferenceServiceSpec !== undefined &&
      opts.updateInferenceServiceSpec !== null
    ) {
      postBody['updateInferenceServiceSpec'] = opts.updateInferenceServiceSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      serviceName: opts.serviceName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateService with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/services/{serviceName}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除推理服务
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.serviceName - 推理服务名称。
      * @param {string} opts.serviceName - 推理服务名称。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      */

  deleteService (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteService"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteService"
      )
    }
    if (opts.serviceName === undefined || opts.serviceName === null) {
      throw new Error(
        "Missing the required parameter 'opts.serviceName' when calling deleteService"
      )
    }
    if (opts.serviceName === undefined || opts.serviceName === null) {
      throw new Error(
        "Missing the required parameter 'opts.serviceName' when calling deleteService"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      serviceName: opts.serviceName,
      serviceName: opts.serviceName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteService with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/services/{serviceName}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新推理服务限流阈值
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.serviceName - 推理服务名称。
      * @param {updateServiceQpsSpec} opts.limit - qps配置
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateServiceQPS (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateServiceQPS"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateServiceQPS"
      )
    }
    if (opts.serviceName === undefined || opts.serviceName === null) {
      throw new Error(
        "Missing the required parameter 'opts.serviceName' when calling updateServiceQPS"
      )
    }
    if (opts.limit === undefined || opts.limit === null) {
      throw new Error(
        "Missing the required parameter 'opts.limit' when calling updateServiceQPS"
      )
    }

    let postBody = {}
    if (opts.limit !== undefined && opts.limit !== null) {
      postBody['limit'] = opts.limit
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      serviceName: opts.serviceName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateServiceQPS with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/services/{serviceName}/updateQPS',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取实例信息
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为20；取值范围[1, 100]。  optional
      * @param {boolean} [opts.failedFirst] - 是否开启异常优先的排序  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;name&#x60;: pod 名称，模糊匹配，支持单个。
&#x60;status&#x60;: 状态，精确匹配，支持多个。
&#x60;ip&#x60;: pod ip, 精确匹配，支持多个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      * @param integer totalCount  pod总数
      * @param podInfo pods
      */

  describeInferencePods (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeInferencePods"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeInferencePods"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling describeInferencePods"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.failedFirst !== undefined && opts.failedFirst !== null) {
      queryParams['failedFirst'] = opts.failedFirst
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeInferencePods with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/pods',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取指定推理服务的当前自动扩缩容规则配置
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      * @param string scaleType  scale type, auto:for autoScale, cron:for cronScale, none:no scale configured
      * @param autoScale autoScale
      * @param cronScale cronScale
      */

  describeInferenceScale (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeInferenceScale"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeInferenceScale"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling describeInferenceScale"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeInferenceScale with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/autoscale',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  为指定的推理服务创建自动扩缩容规则，包括基于资源使用率的自动扩缩容和基于时间的定时扩缩容
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {inferenceScaleSpec} opts.inferenceScaleSpec - 日志采集配置
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      * @param string inferenceId  推理服务ID
      */

  createInferenceScale (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createInferenceScale"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createInferenceScale"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling createInferenceScale"
      )
    }
    if (
      opts.inferenceScaleSpec === undefined ||
      opts.inferenceScaleSpec === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceScaleSpec' when calling createInferenceScale"
      )
    }

    let postBody = {}
    if (
      opts.inferenceScaleSpec !== undefined &&
      opts.inferenceScaleSpec !== null
    ) {
      postBody['inferenceScaleSpec'] = opts.inferenceScaleSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createInferenceScale with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/autoscale',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新指定推理服务的自动扩缩容规则配置
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {inferenceScaleSpec} opts.inferenceScaleSpec - 日志采集配置
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      * @param string inferenceId  推理服务ID
      */

  updateInferenceScale (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateInferenceScale"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateInferenceScale"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling updateInferenceScale"
      )
    }
    if (
      opts.inferenceScaleSpec === undefined ||
      opts.inferenceScaleSpec === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceScaleSpec' when calling updateInferenceScale"
      )
    }

    let postBody = {}
    if (
      opts.inferenceScaleSpec !== undefined &&
      opts.inferenceScaleSpec !== null
    ) {
      postBody['inferenceScaleSpec'] = opts.inferenceScaleSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateInferenceScale with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/autoscale',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除指定推理服务的自动扩缩容规则，停止自动扩缩容功能
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.inferenceId - 推理服务 ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string requestId  请求ID
      */

  deleteInferenceScale (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteInferenceScale"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteInferenceScale"
      )
    }
    if (opts.inferenceId === undefined || opts.inferenceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.inferenceId' when calling deleteInferenceScale"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      inferenceId: opts.inferenceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteInferenceScale with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/workspace/{workspaceId}/inferences/{inferenceId}/autoscale',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询pin下有权限的工作空间下的inference列表。

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;inferenceId&#x60;: 资源ID，精确匹配，支持多个。
&#x60;serviceId&#x60;: 服务ID, 精确匹配，支持多个。
&#x60;name&#x60;: 名称，模糊匹配，支持单个。
&#x60;state&#x60;: 状态，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个。
&#x60;createUserPin&#x60;: 创建人pin，精确匹配，支持多个。
&#x60;appType&#x60;: 应用类型，精确匹配，支持单个。
&#x60;queue&#x60;: 队列ID，精确匹配，支持单个。
&#x60;workspaceId&#x60;: 工作空间ID，精确匹配，支持单个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param adminInferenceInfo inferences
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  adminDescribeInferences (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeInferences"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeInferences"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeInferences with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/api/{apiType}/inferences',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询pin下所有的资源，按inference状态统计数量。

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;workspaceId&#x60;: 工作空间ID，精确匹配，支持单个。
&#x60;state&#x60;: 推理服务状态(queuing,deploying,failed,running,stopping,scaling,stopped,deleting)。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param inferenceStateCount stateCount
      */

  adminDescribeInferenceCount (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeInferenceCount"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeInferenceCount"
      )
    }

    let postBody = null
    let queryParams = {}
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeInferenceCount with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/infer-regions/{regionId}/api/{apiType}/inferenceCount',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询节点列表
      * @param {Object} opts - parameters
      * @param {integer} [opts.page] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为20；取值范围[10, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;status&#x60;: 节点调度状态；
&#x60;ready_status&#x60;: 节点就绪状态。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param nodeInstance nodeList
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeNodeInstances (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeNodeInstances"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.page !== undefined && opts.page !== null) {
      queryParams['page'] = opts.page
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeNodeInstances with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/nodes',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询指定监控维度的列表
      * @param {Object} opts - parameters
      * @param {string} opts.type - 监控维度
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param metrics data
      * @param integer total  记录总数
      */

  listMetrics (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  listMetrics"
      )
    }

    opts = opts || {}

    if (opts.type === undefined || opts.type === null) {
      throw new Error(
        "Missing the required parameter 'opts.type' when calling listMetrics"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.type !== undefined && opts.type !== null) {
      queryParams['type'] = opts.type
    }

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call listMetrics with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/metrics:list',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询私有模型列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;modelName&#x60;: 模型名称，模糊匹配，支持单个
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个
&#x60;modelIds&#x60;: 模型Id，精确匹配，支持多个
&#x60;labels&#x60;: 标签数组，精确匹配，支持多个
&#x60;cfsVpcIds&#x60;: cfsVpcId数组，精确匹配，支持多个
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param modelsPrivate modelsPrivate
      * @param integer totalCount  模型个数
      */

  describePrivateModels (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describePrivateModels"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describePrivateModels"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describePrivateModels with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建新模型/新版本。
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {modelSpecPrivate} opts.modelSpec - 模型配置。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string modelId  模型ID
      * @param string version  模型版本
      */

  createModel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createModel"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createModel"
      )
    }
    if (opts.modelSpec === undefined || opts.modelSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelSpec' when calling createModel"
      )
    }

    let postBody = {}
    if (opts.modelSpec !== undefined && opts.modelSpec !== null) {
      postBody['modelSpec'] = opts.modelSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createModel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改模型信息
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.modelId - 模型ID
      * @param {updateModelParm} opts.updateModelSpec - 修改模型配置。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateModel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateModel"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateModel"
      )
    }
    if (opts.modelId === undefined || opts.modelId === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelId' when calling updateModel"
      )
    }
    if (opts.updateModelSpec === undefined || opts.updateModelSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.updateModelSpec' when calling updateModel"
      )
    }

    let postBody = {}
    if (opts.updateModelSpec !== undefined && opts.updateModelSpec !== null) {
      postBody['updateModelSpec'] = opts.updateModelSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      modelId: opts.modelId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateModel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model/{modelId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据模型ID删除模型
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.modelId - 模型ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteModel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteModel"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteModel"
      )
    }
    if (opts.modelId === undefined || opts.modelId === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelId' when calling deleteModel"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      modelId: opts.modelId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteModel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model/{modelId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取模型的版本列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.modelId - 模型ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;versionName&#x60;: 版本号数组，精确匹配，支持多个
&#x60;labels&#x60;: 模型用途数组，精确匹配，支持多个
&#x60;sourceTypes&#x60;: 注册来源数组，精确匹配，支持多个
&#x60;cfsVpcIds&#x60;: cfsVpcId数组，精确匹配，支持多个
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param integer totalCount  版本数量
      * @param modelVersionInfo versionResult
      */

  describeModelVersions (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeModelVersions"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeModelVersions"
      )
    }
    if (opts.modelId === undefined || opts.modelId === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelId' when calling describeModelVersions"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      modelId: opts.modelId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeModelVersions with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model/{modelId}/version',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取模型版本的详情
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.modelId - 模型ID
      * @param {string} opts.version - 模型版本
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param modelVersionInfo versionResult  版本信息
      */

  describeModelVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeModelVersion"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeModelVersion"
      )
    }
    if (opts.modelId === undefined || opts.modelId === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelId' when calling describeModelVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling describeModelVersion"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      modelId: opts.modelId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeModelVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model/{modelId}/version/{version}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改模型版本信息
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.modelId - 模型ID
      * @param {string} opts.version - 模型版本
      * @param {updateModelVersionParm} opts.updateModelVersionSpec - 修改模型版本配置。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateModelVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateModelVersion"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateModelVersion"
      )
    }
    if (opts.modelId === undefined || opts.modelId === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelId' when calling updateModelVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling updateModelVersion"
      )
    }
    if (
      opts.updateModelVersionSpec === undefined ||
      opts.updateModelVersionSpec === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.updateModelVersionSpec' when calling updateModelVersion"
      )
    }

    let postBody = {}
    if (
      opts.updateModelVersionSpec !== undefined &&
      opts.updateModelVersionSpec !== null
    ) {
      postBody['updateModelVersionSpec'] = opts.updateModelVersionSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      modelId: opts.modelId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateModelVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model/{modelId}/version/{version}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除模型版本
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.modelId - 模型ID
      * @param {string} opts.version - 模型版本
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteModelVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteModelVersion"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteModelVersion"
      )
    }
    if (opts.modelId === undefined || opts.modelId === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelId' when calling deleteModelVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling deleteModelVersion"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      modelId: opts.modelId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteModelVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/workspace/{workspaceId}/model/{modelId}/version/{version}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询公共模型列表
      * @param {Object} opts - parameters
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;modelName&#x60;: 模型名称，模糊匹配，支持单个
&#x60;modelDescription&#x60;: 模型描述，模糊匹配，支持单个
&#x60;modelIds&#x60;: 模型Id，精确匹配，支持多个
&#x60;labelsIn&#x60;: 标签，多个标签查询，有一个标签匹配上就行
&#x60;labelsEq&#x60;: 标签，多个标签查询，每一个标签都要匹配到
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param modelsPublic models
      * @param integer totalCount  模型个数
      */

  describePublicModels (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describePublicModels"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describePublicModels with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/publicModel',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取公共模型版本详情
      * @param {Object} opts - parameters
      * @param {string} opts.modelId - 模型ID
      * @param {string} opts.version - 模型版本
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param publicModelVersionInfo versionResult
      */

  describePublicModelVersion (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describePublicModelVersion"
      )
    }

    opts = opts || {}

    if (opts.modelId === undefined || opts.modelId === null) {
      throw new Error(
        "Missing the required parameter 'opts.modelId' when calling describePublicModelVersion"
      )
    }
    if (opts.version === undefined || opts.version === null) {
      throw new Error(
        "Missing the required parameter 'opts.version' when calling describePublicModelVersion"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      modelId: opts.modelId,
      version: opts.version
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describePublicModelVersion with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/publicModel/{modelId}/version/{version}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  管理端查询公共模型列表
      * @param {Object} opts - parameters
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;modelName&#x60;: 模型名称，模糊匹配，支持单个
&#x60;modelDescription&#x60;: 模型描述，模糊匹配，支持单个
&#x60;modelIds&#x60;: 模型Id，精确匹配，支持多个
&#x60;labelsIn&#x60;: 标签，多个标签查询，有一个标签匹配上就行
&#x60;labelsEq&#x60;: 标签，多个标签查询，每一个标签都要匹配到
&#x60;online&#x60;: true上线、false下线
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param modelsPublic models
      * @param integer totalCount  模型个数
      */

  describeAdminPublicModels (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeAdminPublicModels"
      )
    }

    opts = opts || {}

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeAdminPublicModels with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/model-regions/{regionId}/adminPublicModel',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询工作空间下的Notebook实例列表，支持分页、过滤和多条件查询。

## 接口说明
- 返回当前工作空间下用户有权限查看的Notebook实例列表。
- 支持通过filters参数进行多条件过滤查询。
- 支持通过userTags按自定义标签筛选资源。
- 支持通过resourceGroupIds按资源组筛选资源。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;notebookId&#x60;: 资源ID，精确匹配，支持多个。
&#x60;name&#x60;: 名称，模糊匹配，支持单个。
&#x60;state&#x60;: 状态，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个。
&#x60;createUserPin&#x60;: 创建人pin，精确匹配，支持多个。
&#x60;appType&#x60;: 应用类型，精确匹配，支持单个。
&#x60;queue&#x60;: 队列ID，精确匹配，支持单个。
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param notebookInfo notebooks
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeNotebooks (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeNotebooks"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeNotebooks"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeNotebooks with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  在工作空间下创建一个Notebook实例，Notebook是基于Kubernetes的交互式开发环境，支持JupyterLab应用。

## 接口说明
- **资源队列**: 必须选择一个队列，使用公共资源池时必须指定规格，使用私有资源池时必须指定CPU和内存，是否使用GPU可以按需选择。公共资源池队列ID固定为&#x60;joybuilder-public-queue&#x60;。
- **镜像配置**: 支持公共镜像和自定义镜像，需要指定镜像来源(public/self)、镜像名称、镜像URL等信息。
- **存储空间**: 使用用户个人存储，第一块存储默认作为工作目录，挂载到Notebook实例中&#x60;/mnt/workspace&#x60;目录下。支持cfs、oss、jpfs三种存储类型。&#x60;cfs&#x60;和&#x60;jpfs&#x60;类型存储只能选择与队列同vpc下的资源，使用私有资源池时跨vpc需要打通(vpcPeering)对等连接。
- **数据集**: 可选择公共数据集或个人数据集。&#x60;cfs&#x60;和&#x60;jpfs&#x60;类型的数据集只能选择与队列同vpc下的数据集，使用私有资源池时跨vpc需要打通(vpcPeering)对等连接。
- **模型**: 可选择公共模型或个人模型。&#x60;cfs&#x60;和&#x60;jpfs&#x60;类型的模型只能选择与队列同vpc下的模型，使用私有资源池时跨vpc需要打通(vpcPeering)对等连接。
- **SSH连接**: 开启后需要选择一个与队列同vpc下的负载均衡(LB)，并设置一个未占用的监听端口，实例运行后可以通过LB的公网IP和端口进行SSH访问，使用私有资源池时跨vpc需要打通(vpcPeering)对等连接。
- **计费配置**: 在私有资源池中创建Notebook时不计费，在公共资源池中创建Notebook时默认为按规格配置计费。
- **资源权限**: 支持设置工作空间中的资源归属权限(public/private)，管理员可查看工作空间中全部资源，其他用户只能查看归属自己的private权限的资源或public权限的资源。

## Notebook环境说明
- Notebook通过(/home/.notebook_utils/notebook_start.sh)脚本启动，启动脚本不可更改。
- 系统通过探测Notebook容器内的8888端口探活。
- 平台默认使用conda配置(/opt/conda/.condarc), conda环境目录以及conda管理的pip存储都会配置为使用用户个人存储, 以便数据不会在下次启动时丢失。
- 平台默认配置pip配置文件/conf/pip/$BASE_ENV_NAME.conf，由conda激活时接管。
- 如果用户需要自定义配置，只需要在个人存储中编写脚本(/mnt/workspace/.custom/custom_config.sh)，之后再次启动时平台将不会做任何(conda和pip)配置。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} [opts.clientToken] - 用于保证请求的幂等性。由客户端生成，并确保不同请求中该参数唯一，长度不超过64个字符。  optional
      * @param {notebookSpec} opts.notebookSpec - notebook配置。
      * @param {chargeSpec} [opts.chargeSpec] - 计费配置。
在私有资源池中创建Notebook时不计费并且参数无效。
在公共资源池中创建Notebook时默认为按配置计费，并且只支持按配置计费。
  optional
      * @param {logCollectConfig} [opts.logCollectConfig] - 日志采集配置  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string notebookId  Notebook ID
      */

  createNotebook (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createNotebook"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createNotebook"
      )
    }
    if (opts.notebookSpec === undefined || opts.notebookSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookSpec' when calling createNotebook"
      )
    }

    let postBody = {}
    if (opts.clientToken !== undefined && opts.clientToken !== null) {
      postBody['clientToken'] = opts.clientToken
    }
    if (opts.notebookSpec !== undefined && opts.notebookSpec !== null) {
      postBody['notebookSpec'] = opts.notebookSpec
    }
    if (opts.chargeSpec !== undefined && opts.chargeSpec !== null) {
      postBody['chargeSpec'] = opts.chargeSpec
    }
    if (opts.logCollectConfig !== undefined && opts.logCollectConfig !== null) {
      postBody['logCollectConfig'] = opts.logCollectConfig
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createNotebook with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  统计查询工作空间下Notebook每种状态的数量。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param notebookStateCount stateCount
      */

  describeNotebookCount (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeNotebookCount"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeNotebookCount"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeNotebookCount with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook:count',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询Notebook实例的详细信息，包括资源配置、存储配置、数据集配置、模型配置、计费信息等完整信息。

## 返回信息
- 基础信息: 名称、描述、状态、创建时间等
- 资源配置: CPU、内存、GPU、队列信息
- 存储配置: 挂载的存储列表及挂载路径
- 数据集配置: 挂载的数据集列表
- 模型配置: 挂载的模型列表
- 访问信息: 访问令牌、控制台地址、VSCode地址
- 计费信息: 公共资源池的计费详情(私有资源池无计费信息)
- 关机策略: 自动关机策略配置

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param notebookDetail notebook  notebook详情
      */

  describeNotebook (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeNotebook"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeNotebook"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling describeNotebook"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeNotebook with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新Notebook实例的基础属性，不影响资源配置。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} [opts.name] - 名称。1~32字符，可以包含中文、数字、大小写字母、英文下划线“_”、中划线“-”或点“.”。传null表示不修改此字段。  optional
      * @param {string} [opts.description] - 描述。不超过256字符。传null表示不修改此字段。  optional
      * @param {string} [opts.permission] - 工作空间中的资源归属权限，支持(public,private)，只有管理员和拥有者可以修改。传null表示不修改此字段。
管理员可查看工作空间中全部资源，其他用户只能查看归属自己的private权限的资源或public权限的资源。  optional
      * @param {string} [opts.ownerUserPin] - 资源归属用户。传null表示不修改此字段。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateNotebook (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateNotebook"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateNotebook"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling updateNotebook"
      )
    }

    let postBody = {}
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (opts.permission !== undefined && opts.permission !== null) {
      postBody['permission'] = opts.permission
    }
    if (opts.ownerUserPin !== undefined && opts.ownerUserPin !== null) {
      postBody['ownerUserPin'] = opts.ownerUserPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateNotebook with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除Notebook实例。

## 接口说明
- 如果Notebook已经不存在了，则不会做任何操作。
- 其它状态都允许删除操作。
- 删除后数据保留在用户个人存储中，不会被清除。
- 删除操作不可恢复，请谨慎操作。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteNotebook (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteNotebook"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteNotebook"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling deleteNotebook"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteNotebook with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  启动Notebook实例。

## 接口说明
- 只允许停止状态(stopped)时操作。
- 如果Notebook已经是运行状态(running)，则不会做任何操作。
- 如果是其它状态，接口会返回错误。

## 资源配置变更
启动时支持更换资源配置(仅私有资源池):
- 在公共资源池中的Notebook不允许变更资源配置，workloadSpec参数只能传null。
- 在私有资源池中的Notebook不允许变更为公共资源池，但允许更换私有资源池中的其它队列。
- 支持在启动时更换公网访问配置(LB)，或移除公网访问配置。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {workloadSpec} [opts.workloadSpec] - 更换资源配置。不需要更换时需要传null。
限制条件:
在公共资源池中的Notebook不允许变更资源配置，只能传null。
在私有资源池中的Notebook不允许变更为公共资源池，但允许更换私有资源池中的其它队列。
  optional
      * @param {lbSpec} [opts.lbSpec] - 只支持私有资源池中的Notebook可以更换公网访问配置。不需要更换时需要传null。  optional
      * @param {boolean} [opts.removeLb] - 是否移除公网访问配置。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  startNotebook (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  startNotebook"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling startNotebook"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling startNotebook"
      )
    }

    let postBody = {}
    if (opts.workloadSpec !== undefined && opts.workloadSpec !== null) {
      postBody['workloadSpec'] = opts.workloadSpec
    }
    if (opts.lbSpec !== undefined && opts.lbSpec !== null) {
      postBody['lbSpec'] = opts.lbSpec
    }
    if (opts.removeLb !== undefined && opts.removeLb !== null) {
      postBody['removeLb'] = opts.removeLb
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call startNotebook with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}:start',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止Notebook实例。

## 接口说明
- 如果Notebook已经是停止状态(stopped)，则不会做任何操作。
- 除了删除中(deleting)、保存镜像中(saving)状态，其它状态都允许操作。
- 停止后实例不再占用计算资源，但存储数据保留在用户个人存储中。
- 再次启动时可以从上次停止的状态恢复。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  stopNotebook (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  stopNotebook"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling stopNotebook"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling stopNotebook"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call stopNotebook with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}:stop',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新Notebook的资源属性配置。

## 支持更新的属性
- **镜像配置**: 更换镜像来源和镜像ID
- **工作负载配置**: 更换资源队列、CPU、内存、GPU配置
- **存储配置**: 更新存储空间挂载配置
- **数据集配置**: 更新数据集挂载配置
- **模型配置**: 更新模型挂载配置
- **公网访问配置**: 更新或移除负载均衡配置(仅私有资源池)
- **代码配置**: 更新代码库挂载配置
- **节点亲和性配置**: 更新节点调度亲和性规则

## 接口说明
- 更新操作需要在Notebook停止状态下进行。
- 更新存储、数据集、模型配置时需要确保新配置与队列的网络连通性。
- 传null的字段表示不修改该属性。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {updateResourcePropertiesParam} opts.updateResourcePropertiesParam - 更新notebook的资源属性参数。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateResourceProperties (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateResourceProperties"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateResourceProperties"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling updateResourceProperties"
      )
    }
    if (
      opts.updateResourcePropertiesParam === undefined ||
      opts.updateResourcePropertiesParam === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.updateResourcePropertiesParam' when calling updateResourceProperties"
      )
    }

    let postBody = {}
    if (
      opts.updateResourcePropertiesParam !== undefined &&
      opts.updateResourcePropertiesParam !== null
    ) {
      postBody['updateResourcePropertiesParam'] =
        opts.updateResourcePropertiesParam
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateResourceProperties with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}:updateResourceProperties',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取Notebook实例的事件列表，用于排查问题和查看实例状态变更历史。

## 事件类型
- 启动事件: 记录启动过程中的各个阶段
- 停止事件: 记录停止操作的原因和时间
- 错误事件: 记录异常情况和错误原因
- 资源变更事件: 记录资源配置的变更操作

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {string} [opts.startTime] - 事件开始时间。  optional
      * @param {string} [opts.endTime] - 事件结束时间。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param notebookEvent events
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  getNotebookEvents (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getNotebookEvents"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getNotebookEvents"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling getNotebookEvents"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      queryParams['startTime'] = opts.startTime
    }
    if (opts.endTime !== undefined && opts.endTime !== null) {
      queryParams['endTime'] = opts.endTime
    }

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getNotebookEvents with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}:events',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取Notebook实例的Pod列表，用于查看底层Kubernetes资源的运行状态。

## Pod信息
- Pod名称、命名空间、IP地址
- Pod状态(Phase)
- 容器数量和就绪容器数量
- 重启次数
- 状态异常时的原因和描述

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param notebookPod pods
      */

  getNotebookPods (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getNotebookPods"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling getNotebookPods"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling getNotebookPods"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getNotebookPods with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}:pods',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  将运行中的Notebook实例保存为自定义镜像，保存成功后自动注册到用户的镜像资产中。

## 接口说明
- 镜像以快照的方式进行保存，会保存当前工作目录的所有内容。
- 镜像保存过程中Notebook无法停止和删除，但不影响正常连接使用。
- 不建议在保存镜像过程中在Notebook里操作大量磁盘IO(比如下载大文件)类操作，可能影响镜像保存质量。
- 保存完成后会生成一个镜像任务，可通过查询镜像任务接口查看保存进度和结果。

## 镜像配置说明
- **镜像名称**: 用于注册到我的镜像仓库，需要符合命名规范。
- **镜像用途**: 指定镜像可用于哪些场景(training,notebook,inference)。
- **镜像架构**: 指定CPU或GPU类型。
- **目标仓库**: 指定推送到的镜像仓库地址。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} [opts.clientToken] - 用于保证请求的幂等性。由客户端生成，并确保不同请求中该参数唯一，长度不超过64个字符。  optional
      * @param {imageSpec} opts.imageSpec - notebook配置。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string taskId  保存镜像任务ID
      */

  saveImage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  saveImage"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling saveImage"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling saveImage"
      )
    }
    if (opts.imageSpec === undefined || opts.imageSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.imageSpec' when calling saveImage"
      )
    }

    let postBody = {}
    if (opts.clientToken !== undefined && opts.clientToken !== null) {
      postBody['clientToken'] = opts.clientToken
    }
    if (opts.imageSpec !== undefined && opts.imageSpec !== null) {
      postBody['imageSpec'] = opts.imageSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call saveImage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}/image',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询Notebook的镜像保存任务列表，支持分页和过滤查询。

## 任务状态说明
- pending: 等待执行
- running: 正在执行
- success: 执行成功
- failed: 执行失败

## 支持的过滤条件
- taskId: 任务ID，精确匹配，支持多个
- imgName: 镜像名称，模糊匹配，支持单个
- state: 任务状态，精确匹配，支持多个
- createUser: 创建人名称，模糊匹配，支持单个

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;taskId&#x60;: 任务ID，精确匹配，支持多个。
&#x60;imgName&#x60;: 镜像名称，模糊匹配，支持单个。
&#x60;state&#x60;: 状态，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param imageTask imageTasks
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  describeImageTasks (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeImageTasks"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeImageTasks"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling describeImageTasks"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeImageTasks with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}/imageTask',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除镜像保存任务记录。

## 接口说明
- 只能删除已完成(成功或失败)的任务记录。
- 删除任务记录不会影响已保存成功的镜像。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} opts.taskId - 任务ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteImageTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteImageTask"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteImageTask"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling deleteImageTask"
      )
    }
    if (opts.taskId === undefined || opts.taskId === null) {
      throw new Error(
        "Missing the required parameter 'opts.taskId' when calling deleteImageTask"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId,
      taskId: opts.taskId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteImageTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}/imageTask/{taskId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  配置Notebook实例的日志采集规则，将容器日志采集到日志服务。

## 日志采集配置说明
- **日志集ID**: 指定日志采集的目标日志集。
- **日志主题ID**: 指定日志采集的目标日志主题。
- **标准日志配置**: 配置是否采集容器的标准输出(stdout/stderr)。
- **自定义日志配置**: 配置采集容器内的自定义日志文件路径，支持通配符。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {logCollectConfig} opts.logCollectConfig - 日志采集配置
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  createNotebookLogCollectConfig (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createNotebookLogCollectConfig"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createNotebookLogCollectConfig"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling createNotebookLogCollectConfig"
      )
    }
    if (opts.logCollectConfig === undefined || opts.logCollectConfig === null) {
      throw new Error(
        "Missing the required parameter 'opts.logCollectConfig' when calling createNotebookLogCollectConfig"
      )
    }

    let postBody = {}
    if (opts.logCollectConfig !== undefined && opts.logCollectConfig !== null) {
      postBody['logCollectConfig'] = opts.logCollectConfig
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createNotebookLogCollectConfig with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}/logCollectConfig',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除Notebook实例的日志采集配置。

## 接口说明
- 删除后停止采集该Notebook的日志。
- 已采集的历史日志不受影响，仍可在日志服务中查看。

      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.notebookId - notebook ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteNotebookLogCollectConfig (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteNotebookLogCollectConfig"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteNotebookLogCollectConfig"
      )
    }
    if (opts.notebookId === undefined || opts.notebookId === null) {
      throw new Error(
        "Missing the required parameter 'opts.notebookId' when calling deleteNotebookLogCollectConfig"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      notebookId: opts.notebookId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteNotebookLogCollectConfig with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/workspace/{workspaceId}/notebook/{notebookId}/logCollectConfig:delete',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  管理员查询子账号下有权限的所有Notebook实例，支持跨工作空间查询。

## 接口说明
- 需要管理员权限才能调用此接口。
- 返回当前账号下所有工作空间的Notebook实例。
- 支持通过filters参数进行多条件过滤查询。
- 支持按用户标签(userTags)和资源组(resourceGroupIds)筛选资源。

## 支持的过滤条件
- notebookId: 资源ID，精确匹配，支持多个
- name: 名称，模糊匹配，支持单个
- state: 状态，精确匹配，支持多个
- createUser: 创建人名称，模糊匹配，支持单个
- createUserPin: 创建人pin，精确匹配，支持多个
- appType: 应用类型，精确匹配，支持单个
- queue: 队列ID，精确匹配，支持单个
- workspaceId: 工作空间ID，精确匹配，支持单个

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;notebookId&#x60;: 资源ID，精确匹配，支持多个。
&#x60;name&#x60;: 名称，模糊匹配，支持单个。
&#x60;state&#x60;: 状态，精确匹配，支持多个。
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个。
&#x60;createUserPin&#x60;: 创建人pin，精确匹配，支持多个。
&#x60;appType&#x60;: 应用类型，精确匹配，支持单个。
&#x60;queue&#x60;: 队列ID，精确匹配，支持单个。
&#x60;workspaceId&#x60;: 工作空间ID，精确匹配，支持单个。
  optional
      * @param {tagFilter} [opts.userTags] - 用户自定义标签列表。  optional
      * @param {string} [opts.resourceGroupIds] - 资源组ID列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param notebookInfo notebooks
      * @param number totalCount  本次查询可匹配到的总记录数，使用者需要结合 &#x60;pageNumber&#x60; 和 &#x60;pageSize&#x60; 计算是否可以继续分页。
      */

  adminDescribeNotebooks (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeNotebooks"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeNotebooks"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))
    Object.assign(
      queryParams,
      super.buildTagFilterParam(opts.userTags, 'userTags')
    )
    Object.assign(
      queryParams,
      super.buildArrayParam(opts.resourceGroupIds, 'resourceGroupIds')
    )

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeNotebooks with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/api/{apiType}/notebook',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  管理员统计查询账号下所有Notebook按状态分类的数量，支持跨工作空间。

## 接口说明
- 需要管理员权限才能调用此接口。
- 按Notebook状态统计数量，返回各状态的实例数量。
- 支持通过filters参数过滤统计范围。

## 支持的过滤条件
- queueId: 队列ID，精确匹配，支持单个
- workspaceId: 工作空间ID，精确匹配，支持单个
- createUser: 创建人名称，模糊匹配，支持单个
- createUserPin: 创建人pin，精确匹配，支持多个
- ownerUser: 所有者名称，模糊匹配，支持单个
- ownerUserPin: 所有者pin，精确匹配，支持多个

## 状态说明
- queuing: 排队中
- pending: 启动中
- failed: 失败
- running: 运行中
- stopping: 停止中
- stopped: 已停止
- deleting: 删除中
- saving: 镜像保存中
- rolling-back: 回滚中
- rolled-back: 已回滚

      * @param {Object} opts - parameters
      * @param {string} opts.apiType - 接口类型(admin)
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;queueId&#x60;: 队列ID，精确匹配，支持单个。
&#x60;workspaceId&#x60;: 工作空间ID，精确匹配，支持单个。
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个。
&#x60;createUserPin&#x60;: 创建人pin，精确匹配，支持多个。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param notebookStateCount stateCount
      */

  adminDescribeNotebookCount (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  adminDescribeNotebookCount"
      )
    }

    opts = opts || {}

    if (opts.apiType === undefined || opts.apiType === null) {
      throw new Error(
        "Missing the required parameter 'opts.apiType' when calling adminDescribeNotebookCount"
      )
    }

    let postBody = null
    let queryParams = {}
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      apiType: opts.apiType
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call adminDescribeNotebookCount with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/nb-regions/{regionId}/api/{apiType}/notebook:count',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取代码仓配置列表
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {integer} [opts.pageNumber] - 页码；默认为1。  optional
      * @param {integer} [opts.pageSize] - 分页大小；&lt;br&gt;默认为10；取值范围[1, 100]。  optional
      * @param {filter} [opts.filters] - &lt;b&gt;filters 中支持使用以下关键字进行过滤&lt;/b&gt;
&#x60;repoName&#x60;: 代码仓配置名称，模糊匹配，支持单个
&#x60;createUser&#x60;: 创建人名称，模糊匹配，支持单个
&#x60;repoType&#x60;: 代码库类型[github,coding]，精确匹配，支持多个
&#x60;repoId&#x60;: 代码仓ID，精确匹配，支持多个
&#x60;permission&#x60;: 权限，精确匹配，支持单个，可选(public,private)。
&#x60;ownerUser&#x60;: 所有者名称，模糊匹配，支持单个。
&#x60;ownerUserPin&#x60;: 所有者pin，精确匹配，支持多个。
  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param number totalCount  总记录数
      * @param codeRepoConfigInfo data
      */

  describeCodeRepoConfigs (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeCodeRepoConfigs"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeCodeRepoConfigs"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.pageNumber !== undefined && opts.pageNumber !== null) {
      queryParams['pageNumber'] = opts.pageNumber
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      queryParams['pageSize'] = opts.pageSize
    }
    Object.assign(queryParams, super.buildFilterParam(opts.filters, 'filters'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeCodeRepoConfigs with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/repo-regions/{regionId}/workspace/{workspaceId}/codeRepoConfig',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加代码仓配置
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {createCodeRepoConfigParam} opts.createCodeRepoConfigParam - 创建代码仓配置的参数
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string repoId  代码仓配置ID
      */

  createCodeRepoConfig (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createCodeRepoConfig"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling createCodeRepoConfig"
      )
    }
    if (
      opts.createCodeRepoConfigParam === undefined ||
      opts.createCodeRepoConfigParam === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.createCodeRepoConfigParam' when calling createCodeRepoConfig"
      )
    }

    let postBody = {}
    if (
      opts.createCodeRepoConfigParam !== undefined &&
      opts.createCodeRepoConfigParam !== null
    ) {
      postBody['createCodeRepoConfigParam'] = opts.createCodeRepoConfigParam
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createCodeRepoConfig with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/repo-regions/{regionId}/workspace/{workspaceId}/codeRepoConfig',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取代码仓配置详情
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.repoId - 代码仓配置ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param codeRepoConfigInfo codeRepoConfig
      */

  describeCodeRepoConfig (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  describeCodeRepoConfig"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling describeCodeRepoConfig"
      )
    }
    if (opts.repoId === undefined || opts.repoId === null) {
      throw new Error(
        "Missing the required parameter 'opts.repoId' when calling describeCodeRepoConfig"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      repoId: opts.repoId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call describeCodeRepoConfig with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/repo-regions/{regionId}/workspace/{workspaceId}/codeRepoConfig/{repoId}',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改代码仓配置
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.repoId - 代码仓配置ID
      * @param {updateCodeRepoConfigParam} opts.updateCodeRepoConfigParam - 修改代码仓配置的参数
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateCodeRepoConfig (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateCodeRepoConfig"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateCodeRepoConfig"
      )
    }
    if (opts.repoId === undefined || opts.repoId === null) {
      throw new Error(
        "Missing the required parameter 'opts.repoId' when calling updateCodeRepoConfig"
      )
    }
    if (
      opts.updateCodeRepoConfigParam === undefined ||
      opts.updateCodeRepoConfigParam === null
    ) {
      throw new Error(
        "Missing the required parameter 'opts.updateCodeRepoConfigParam' when calling updateCodeRepoConfig"
      )
    }

    let postBody = {}
    if (
      opts.updateCodeRepoConfigParam !== undefined &&
      opts.updateCodeRepoConfigParam !== null
    ) {
      postBody['updateCodeRepoConfigParam'] = opts.updateCodeRepoConfigParam
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      repoId: opts.repoId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateCodeRepoConfig with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/repo-regions/{regionId}/workspace/{workspaceId}/codeRepoConfig/{repoId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除代码仓配置
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} opts.repoId - 代码仓配置ID
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  deleteCodeRepoConfig (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteCodeRepoConfig"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling deleteCodeRepoConfig"
      )
    }
    if (opts.repoId === undefined || opts.repoId === null) {
      throw new Error(
        "Missing the required parameter 'opts.repoId' when calling deleteCodeRepoConfig"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId,
      repoId: opts.repoId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteCodeRepoConfig with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/repo-regions/{regionId}/workspace/{workspaceId}/codeRepoConfig/{repoId}',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建工作空间。
      * @param {Object} opts - parameters
      * @param {workspaceSpec} opts.workspaceSpec - 工作空间配置。
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string workspaceId  工作空间ID。
      */

  createWorkspace (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createWorkspace"
      )
    }

    opts = opts || {}

    if (opts.workspaceSpec === undefined || opts.workspaceSpec === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceSpec' when calling createWorkspace"
      )
    }

    let postBody = {}
    if (opts.workspaceSpec !== undefined && opts.workspaceSpec !== null) {
      postBody['workspaceSpec'] = opts.workspaceSpec
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createWorkspace with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ws-regions/{regionId}/workspace',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新工作空间。
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} [opts.workspaceName] - 工作空间名称。  optional
      * @param {string} [opts.workspaceDesc] - 工作空间描述。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  updateWorkspace (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateWorkspace"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling updateWorkspace"
      )
    }

    let postBody = {}
    if (opts.workspaceName !== undefined && opts.workspaceName !== null) {
      postBody['workspaceName'] = opts.workspaceName
    }
    if (opts.workspaceDesc !== undefined && opts.workspaceDesc !== null) {
      postBody['workspaceDesc'] = opts.workspaceDesc
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateWorkspace with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ws-regions/{regionId}/workspace/{workspaceId}',
      'PUT',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  关联队列。
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {array} [opts.queueIds] - 关联的队列。【废弃】该字段即将下线，建议使用新的资源队列配置字段[queueConfigs]。  optional
      * @param {array} [opts.queueConfigs] - 关联队列配置  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string queueIds
      */

  addQueues (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  addQueues"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling addQueues"
      )
    }

    let postBody = {}
    if (opts.queueIds !== undefined && opts.queueIds !== null) {
      postBody['queueIds'] = opts.queueIds
    }
    if (opts.queueConfigs !== undefined && opts.queueConfigs !== null) {
      postBody['queueConfigs'] = opts.queueConfigs
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call addQueues with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ws-regions/{regionId}/workspace/{workspaceId}/queue',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  解绑队列
      * @param {Object} opts - parameters
      * @param {string} opts.workspaceId - 工作空间ID
      * @param {string} [opts.queueIds] - 队列ID。  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      */

  removeQueues (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  removeQueues"
      )
    }

    opts = opts || {}

    if (opts.workspaceId === undefined || opts.workspaceId === null) {
      throw new Error(
        "Missing the required parameter 'opts.workspaceId' when calling removeQueues"
      )
    }

    let postBody = null
    let queryParams = {}
    Object.assign(queryParams, super.buildArrayParam(opts.queueIds, 'queueIds'))

    let pathParams = {
      regionId: regionId,
      workspaceId: opts.workspaceId
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdaip/1.0.2'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call removeQueues with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/ws-regions/{regionId}/workspace/{workspaceId}/queue',
      'DELETE',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }
}
module.exports = JDAIP
