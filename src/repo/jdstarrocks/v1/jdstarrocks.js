/*
 * Copyright 2018 JDCLOUD.COM
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http:#www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * WarehouseController
 * 资源分组管理接口
 *
 * OpenAPI spec version: v1
 * Contact:
 *
 * NOTE: This class is auto generated by the jdcloud code generator program.
 */

require('../../../lib/node_loader')
var JDCloud = require('../../../lib/core')
var Service = JDCloud.Service
var serviceId = 'jdstarrocks'
Service._services[serviceId] = true

/**
 * jdstarrocks service.
 * @version 1.0.0
 */

class JDSTARROCKS extends Service {
  constructor (options = {}) {
    options._defaultEndpoint = {}
    options._defaultEndpoint.protocol =
      options._defaultEndpoint.protocol || 'https'
    options._defaultEndpoint.host =
      options._defaultEndpoint.host || 'jstarrocks.jdcloud-api.com'
    options.basePath = '/v1' // 默认要设为空""
    super(serviceId, options)
  }

  /**
      *  获取告警规则列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {projectAlarmRulesListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectAlarmRulesListResPageRespVo result
      */

  projectAlarmRulesList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectAlarmRulesList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectAlarmRulesList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectAlarmRulesList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectAlarmRulesList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建告警规则
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.rulesCode] - 规则code  optional
      * @param {string} [opts.alarmName] - 规则名称  optional
      * @param {boolean} [opts.alarmEnable] - 是否启用  optional
      * @param {string} [opts.metric] - 监控项  optional
      * @param {string} [opts.metricRole] - 指标类型 FE/BE/CN/QUEUE/PROJECT  optional
      * @param {boolean} [opts.isCustomMetric] - 是否自定义监控项  optional
      * @param {string} [opts.timeSpan] - 时间间隔  optional
      * @param {string} [opts.timeUnit] - 时间单位  optional
      * @param {string} [opts.functions] - 取值函数  optional
      * @param {string} [opts.operator] - 运算符  optional
      * @param {string} [opts.threshold] - 阈值  optional
      * @param {integer} [opts.cycles] - 周期  optional
      * @param {string} [opts.level] - 告警级别，一般-General,严重-serious,紧急-emergency;  optional
      * @param {boolean} [opts.ifEmail] - 是否发送邮件  optional
      * @param {boolean} [opts.ifMessage] - 是否发送短信  optional
      * @param {boolean} [opts.ifPhone] - 是否电话  optional
      * @param {boolean} [opts.ifRobot] - 是否机器人（可支持钉钉、飞书、企业微信）  optional
      * @param {string} [opts.contacts] - 联系人  optional
      * @param {string} [opts.sendFrequency] - 通知频率. EVERYTIME-每次都进行告警通知,THIRTYMINUTES-每30分钟告警通知一次,ONEHOUR-每1小时告警通知一次,TWOHOUR-每2小时告警通知一次,SIXHOUR-每6小时告警通知一次;  optional
      * @param {boolean} [opts.ifAllDay] - 是否全天  optional
      * @param {string} [opts.startTime] - 创建时间  optional
      * @param {string} [opts.stopTime] - 创建时间  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectCreateAlarmRules (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectCreateAlarmRules"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectCreateAlarmRules"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.rulesCode !== undefined && opts.rulesCode !== null) {
      postBody['rulesCode'] = opts.rulesCode
    }
    if (opts.alarmName !== undefined && opts.alarmName !== null) {
      postBody['alarmName'] = opts.alarmName
    }
    if (opts.alarmEnable !== undefined && opts.alarmEnable !== null) {
      postBody['alarmEnable'] = opts.alarmEnable
    }
    if (opts.metric !== undefined && opts.metric !== null) {
      postBody['metric'] = opts.metric
    }
    if (opts.metricRole !== undefined && opts.metricRole !== null) {
      postBody['metricRole'] = opts.metricRole
    }
    if (opts.isCustomMetric !== undefined && opts.isCustomMetric !== null) {
      postBody['isCustomMetric'] = opts.isCustomMetric
    }
    if (opts.timeSpan !== undefined && opts.timeSpan !== null) {
      postBody['timeSpan'] = opts.timeSpan
    }
    if (opts.timeUnit !== undefined && opts.timeUnit !== null) {
      postBody['timeUnit'] = opts.timeUnit
    }
    if (opts.functions !== undefined && opts.functions !== null) {
      postBody['functions'] = opts.functions
    }
    if (opts.operator !== undefined && opts.operator !== null) {
      postBody['operator'] = opts.operator
    }
    if (opts.threshold !== undefined && opts.threshold !== null) {
      postBody['threshold'] = opts.threshold
    }
    if (opts.cycles !== undefined && opts.cycles !== null) {
      postBody['cycles'] = opts.cycles
    }
    if (opts.level !== undefined && opts.level !== null) {
      postBody['level'] = opts.level
    }
    if (opts.ifEmail !== undefined && opts.ifEmail !== null) {
      postBody['ifEmail'] = opts.ifEmail
    }
    if (opts.ifMessage !== undefined && opts.ifMessage !== null) {
      postBody['ifMessage'] = opts.ifMessage
    }
    if (opts.ifPhone !== undefined && opts.ifPhone !== null) {
      postBody['ifPhone'] = opts.ifPhone
    }
    if (opts.ifRobot !== undefined && opts.ifRobot !== null) {
      postBody['ifRobot'] = opts.ifRobot
    }
    if (opts.contacts !== undefined && opts.contacts !== null) {
      postBody['contacts'] = opts.contacts
    }
    if (opts.sendFrequency !== undefined && opts.sendFrequency !== null) {
      postBody['sendFrequency'] = opts.sendFrequency
    }
    if (opts.ifAllDay !== undefined && opts.ifAllDay !== null) {
      postBody['ifAllDay'] = opts.ifAllDay
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      postBody['startTime'] = opts.startTime
    }
    if (opts.stopTime !== undefined && opts.stopTime !== null) {
      postBody['stopTime'] = opts.stopTime
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectCreateAlarmRules with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectCreateAlarmRules',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  告警规则详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.rulesCode] - 规则code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectAlarmRulesRes result  返回结果对象
      */

  projectAlarmRules (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectAlarmRules"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectAlarmRules"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.rulesCode !== undefined && opts.rulesCode !== null) {
      postBody['rulesCode'] = opts.rulesCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectAlarmRules with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectAlarmRules',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改告警规则
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.rulesCode] - 规则code  optional
      * @param {string} [opts.alarmName] - 规则名称  optional
      * @param {boolean} [opts.alarmEnable] - 是否启用  optional
      * @param {string} [opts.metric] - 监控项  optional
      * @param {string} [opts.metricRole] - 指标类型 FE/BE/CN/QUEUE/PROJECT  optional
      * @param {boolean} [opts.isCustomMetric] - 是否自定义监控项  optional
      * @param {string} [opts.timeSpan] - 时间间隔  optional
      * @param {string} [opts.timeUnit] - 时间单位  optional
      * @param {string} [opts.functions] - 取值函数  optional
      * @param {string} [opts.operator] - 运算符  optional
      * @param {string} [opts.threshold] - 阈值  optional
      * @param {integer} [opts.cycles] - 周期  optional
      * @param {string} [opts.level] - 告警级别，一般-General,严重-serious,紧急-emergency;  optional
      * @param {boolean} [opts.ifEmail] - 是否发送邮件  optional
      * @param {boolean} [opts.ifMessage] - 是否发送短信  optional
      * @param {boolean} [opts.ifPhone] - 是否电话  optional
      * @param {boolean} [opts.ifRobot] - 是否机器人（可支持钉钉、飞书、企业微信）  optional
      * @param {string} [opts.contacts] - 联系人  optional
      * @param {string} [opts.sendFrequency] - 通知频率. EVERYTIME-每次都进行告警通知,THIRTYMINUTES-每30分钟告警通知一次,ONEHOUR-每1小时告警通知一次,TWOHOUR-每2小时告警通知一次,SIXHOUR-每6小时告警通知一次;  optional
      * @param {boolean} [opts.ifAllDay] - 是否全天  optional
      * @param {string} [opts.startTime] - 创建时间  optional
      * @param {string} [opts.stopTime] - 创建时间  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectUpdateAlarmRules (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectUpdateAlarmRules"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectUpdateAlarmRules"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.rulesCode !== undefined && opts.rulesCode !== null) {
      postBody['rulesCode'] = opts.rulesCode
    }
    if (opts.alarmName !== undefined && opts.alarmName !== null) {
      postBody['alarmName'] = opts.alarmName
    }
    if (opts.alarmEnable !== undefined && opts.alarmEnable !== null) {
      postBody['alarmEnable'] = opts.alarmEnable
    }
    if (opts.metric !== undefined && opts.metric !== null) {
      postBody['metric'] = opts.metric
    }
    if (opts.metricRole !== undefined && opts.metricRole !== null) {
      postBody['metricRole'] = opts.metricRole
    }
    if (opts.isCustomMetric !== undefined && opts.isCustomMetric !== null) {
      postBody['isCustomMetric'] = opts.isCustomMetric
    }
    if (opts.timeSpan !== undefined && opts.timeSpan !== null) {
      postBody['timeSpan'] = opts.timeSpan
    }
    if (opts.timeUnit !== undefined && opts.timeUnit !== null) {
      postBody['timeUnit'] = opts.timeUnit
    }
    if (opts.functions !== undefined && opts.functions !== null) {
      postBody['functions'] = opts.functions
    }
    if (opts.operator !== undefined && opts.operator !== null) {
      postBody['operator'] = opts.operator
    }
    if (opts.threshold !== undefined && opts.threshold !== null) {
      postBody['threshold'] = opts.threshold
    }
    if (opts.cycles !== undefined && opts.cycles !== null) {
      postBody['cycles'] = opts.cycles
    }
    if (opts.level !== undefined && opts.level !== null) {
      postBody['level'] = opts.level
    }
    if (opts.ifEmail !== undefined && opts.ifEmail !== null) {
      postBody['ifEmail'] = opts.ifEmail
    }
    if (opts.ifMessage !== undefined && opts.ifMessage !== null) {
      postBody['ifMessage'] = opts.ifMessage
    }
    if (opts.ifPhone !== undefined && opts.ifPhone !== null) {
      postBody['ifPhone'] = opts.ifPhone
    }
    if (opts.ifRobot !== undefined && opts.ifRobot !== null) {
      postBody['ifRobot'] = opts.ifRobot
    }
    if (opts.contacts !== undefined && opts.contacts !== null) {
      postBody['contacts'] = opts.contacts
    }
    if (opts.sendFrequency !== undefined && opts.sendFrequency !== null) {
      postBody['sendFrequency'] = opts.sendFrequency
    }
    if (opts.ifAllDay !== undefined && opts.ifAllDay !== null) {
      postBody['ifAllDay'] = opts.ifAllDay
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      postBody['startTime'] = opts.startTime
    }
    if (opts.stopTime !== undefined && opts.stopTime !== null) {
      postBody['stopTime'] = opts.stopTime
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectUpdateAlarmRules with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectUpdateAlarmRules',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  告警规则启用禁用
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.rulesCode] - 规则code  optional
      * @param {boolean} [opts.alarmEnable] - 是否启用  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectEnableAlarmRules (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectEnableAlarmRules"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectEnableAlarmRules"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.rulesCode !== undefined && opts.rulesCode !== null) {
      postBody['rulesCode'] = opts.rulesCode
    }
    if (opts.alarmEnable !== undefined && opts.alarmEnable !== null) {
      postBody['alarmEnable'] = opts.alarmEnable
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectEnableAlarmRules with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectEnableAlarmRules',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  告警规则删除
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {array} [opts.rulesCodeList] - 规则code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectDelAlarmRules (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectDelAlarmRules"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectDelAlarmRules"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.rulesCodeList !== undefined && opts.rulesCodeList !== null) {
      postBody['rulesCodeList'] = opts.rulesCodeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectDelAlarmRules with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectDelAlarmRules',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取告警记录列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {projectAlarmRecordListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectAlarmRecordListResPageRespVo result
      */

  projectAlarmRecordList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectAlarmRecordList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectAlarmRecordList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectAlarmRecordList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectAlarmRecordList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  告警记录详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.recordCode] - 记录code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectAlarmRecordInfoRes result  返回结果对象
      */

  projectAlarmRecordInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectAlarmRecordInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectAlarmRecordInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.recordCode !== undefined && opts.recordCode !== null) {
      postBody['recordCode'] = opts.recordCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectAlarmRecordInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectAlarmRecordInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  绑定对象存储桶
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.ossBucketName] - 对象存储桶名称  optional
      * @param {string} [opts.ossBucketRegionId] - 对象存储桶地域ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupBindOssBucket (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupBindOssBucket"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupBindOssBucket"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.ossBucketName !== undefined && opts.ossBucketName !== null) {
      postBody['ossBucketName'] = opts.ossBucketName
    }
    if (
      opts.ossBucketRegionId !== undefined &&
      opts.ossBucketRegionId !== null
    ) {
      postBody['ossBucketRegionId'] = opts.ossBucketRegionId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupBindOssBucket with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupBindOssBucket',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  解绑对象存储桶
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.ossBucketName] - 对象存储桶名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupUnbindOssBucket (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupUnbindOssBucket"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupUnbindOssBucket"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.ossBucketName !== undefined && opts.ossBucketName !== null) {
      postBody['ossBucketName'] = opts.ossBucketName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupUnbindOssBucket with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupUnbindOssBucket',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询SR实例绑定的备份存储空间信息，如果没有绑定，则返回空
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param backupGetBoundOssBucketRes result  返回结果对象
      */

  backupGetBoundOssBucket (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetBoundOssBucket"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetBoundOssBucket"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetBoundOssBucket with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetBoundOssBucket',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取备份任务列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {backupGetTaskListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoBackupTaskRes result  返回结果对象
      */

  backupGetTaskList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetTaskList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetTaskList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetTaskList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetTaskList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取备份任务详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.taskCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param backupTaskDetailRes result  返回结果对象
      */

  backupGetTaskDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetTaskDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetTaskDetail"
      )
    }
    if (opts.taskCode === undefined || opts.taskCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.taskCode' when calling backupGetTaskDetail"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      queryParams['taskCode'] = opts.taskCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetTaskDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetTaskDetail',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建备份任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskName] - 任务名称  optional
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.taskType] - 任务类型  optional
      * @param {string} [opts.taskScheduleCycle] - 任务调度周期  optional
      * @param {integer} [opts.taskScheduleDay] - 任务调度日期  optional
      * @param {string} [opts.taskScheduleTimeRange] - 任务调度时间范围，格式：HH:mm-HH:mm  optional
      * @param {string} [opts.backupScope] - 备份范围  optional
      * @param {string} [opts.backupContent] - 备份内容详情，json格式  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupCreateTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupCreateTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupCreateTask"
      )
    }

    let postBody = {}
    if (opts.taskName !== undefined && opts.taskName !== null) {
      postBody['taskName'] = opts.taskName
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskType !== undefined && opts.taskType !== null) {
      postBody['taskType'] = opts.taskType
    }
    if (
      opts.taskScheduleCycle !== undefined &&
      opts.taskScheduleCycle !== null
    ) {
      postBody['taskScheduleCycle'] = opts.taskScheduleCycle
    }
    if (opts.taskScheduleDay !== undefined && opts.taskScheduleDay !== null) {
      postBody['taskScheduleDay'] = opts.taskScheduleDay
    }
    if (
      opts.taskScheduleTimeRange !== undefined &&
      opts.taskScheduleTimeRange !== null
    ) {
      postBody['taskScheduleTimeRange'] = opts.taskScheduleTimeRange
    }
    if (opts.backupScope !== undefined && opts.backupScope !== null) {
      postBody['backupScope'] = opts.backupScope
    }
    if (opts.backupContent !== undefined && opts.backupContent !== null) {
      postBody['backupContent'] = opts.backupContent
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupCreateTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupCreateTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新备份任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskCode] - 任务code  optional
      * @param {string} [opts.taskType] - 任务类型  optional
      * @param {string} [opts.taskScheduleCycle] - 任务调度周期  optional
      * @param {integer} [opts.taskScheduleDay] - 任务调度日期  optional
      * @param {string} [opts.taskScheduleTimeRange] - 任务调度时间范围，格式：HH:mm-HH:mm  optional
      * @param {string} [opts.backupScope] - 备份范围  optional
      * @param {string} [opts.backupContent] - 备份内容详情，json格式  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupUpdateTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupUpdateTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupUpdateTask"
      )
    }

    let postBody = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.taskType !== undefined && opts.taskType !== null) {
      postBody['taskType'] = opts.taskType
    }
    if (
      opts.taskScheduleCycle !== undefined &&
      opts.taskScheduleCycle !== null
    ) {
      postBody['taskScheduleCycle'] = opts.taskScheduleCycle
    }
    if (opts.taskScheduleDay !== undefined && opts.taskScheduleDay !== null) {
      postBody['taskScheduleDay'] = opts.taskScheduleDay
    }
    if (
      opts.taskScheduleTimeRange !== undefined &&
      opts.taskScheduleTimeRange !== null
    ) {
      postBody['taskScheduleTimeRange'] = opts.taskScheduleTimeRange
    }
    if (opts.backupScope !== undefined && opts.backupScope !== null) {
      postBody['backupScope'] = opts.backupScope
    }
    if (opts.backupContent !== undefined && opts.backupContent !== null) {
      postBody['backupContent'] = opts.backupContent
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupUpdateTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupUpdateTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  手动运行备份任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskCode] - 任务code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupRunTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupRunTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupRunTask"
      )
    }

    let postBody = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupRunTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupRunTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  暂停备份任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskCode] - 任务code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupPauseTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupPauseTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupPauseTask"
      )
    }

    let postBody = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupPauseTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupPauseTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  开启备份任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskCode] - 任务code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupStartTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupStartTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupStartTask"
      )
    }

    let postBody = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupStartTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupStartTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除备份任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskCode] - 任务code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupRemoveTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupRemoveTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupRemoveTask"
      )
    }

    let postBody = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupRemoveTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupRemoveTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取实例关联的仓库下的备份快照列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {backupGetRepoSnapshotListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoBackupSnapshotRes result  返回结果对象
      */

  backupGetSnapshotList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetSnapshotList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetSnapshotList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetSnapshotList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetSnapshotList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取备份快照详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.snapshotId] - 快照ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param backupSnapshotRes result  返回结果对象
      */

  backupGetSnapshotDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetSnapshotDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetSnapshotDetail"
      )
    }

    let postBody = {}
    if (opts.snapshotId !== undefined && opts.snapshotId !== null) {
      postBody['snapshotId'] = opts.snapshotId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetSnapshotDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetSnapshotDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  恢复快照
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {integer} [opts.snapshotId] - 快照ID  optional
      * @param {string} [opts.restoreScope] - 恢复范围  optional
      * @param {string} [opts.restoreContent] - 恢复内容详情，json格式  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupRestoreSnapshot (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupRestoreSnapshot"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupRestoreSnapshot"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.snapshotId !== undefined && opts.snapshotId !== null) {
      postBody['snapshotId'] = opts.snapshotId
    }
    if (opts.restoreScope !== undefined && opts.restoreScope !== null) {
      postBody['restoreScope'] = opts.restoreScope
    }
    if (opts.restoreContent !== undefined && opts.restoreContent !== null) {
      postBody['restoreContent'] = opts.restoreContent
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupRestoreSnapshot with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupRestoreSnapshot',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取给定备份任务的快照列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {backupGetTaskSnapshotListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoBackupSnapshotRes result  返回结果对象
      */

  backupGetTaskSnapshotList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetTaskSnapshotList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetTaskSnapshotList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetTaskSnapshotList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetTaskSnapshotList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  重跑备份快照
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.snapshotId] - 快照ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupRerunSnapshot (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupRerunSnapshot"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupRerunSnapshot"
      )
    }

    let postBody = {}
    if (opts.snapshotId !== undefined && opts.snapshotId !== null) {
      postBody['snapshotId'] = opts.snapshotId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupRerunSnapshot with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupRerunSnapshot',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止备份快照运行
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.snapshotId] - 快照ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupStopSnapshot (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupStopSnapshot"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupStopSnapshot"
      )
    }

    let postBody = {}
    if (opts.snapshotId !== undefined && opts.snapshotId !== null) {
      postBody['snapshotId'] = opts.snapshotId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupStopSnapshot with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupStopSnapshot',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  取消备份任务项实例
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.itemInstanceId] - 任务项实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupCancelItemInstance (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupCancelItemInstance"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupCancelItemInstance"
      )
    }

    let postBody = {}
    if (opts.itemInstanceId !== undefined && opts.itemInstanceId !== null) {
      postBody['itemInstanceId'] = opts.itemInstanceId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupCancelItemInstance with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupCancelItemInstance',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除备份快照
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.snapshotId] - 快照ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupDelSnapshot (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupDelSnapshot"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupDelSnapshot"
      )
    }

    let postBody = {}
    if (opts.snapshotId !== undefined && opts.snapshotId !== null) {
      postBody['snapshotId'] = opts.snapshotId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupDelSnapshot with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupDelSnapshot',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取给定SR实例下的快照恢复记录列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {backupGetProjRestoreLogListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoBackupSnapshotRestoreLogRes result  返回结果对象
      */

  backupGetProjRestoreLogList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetProjRestoreLogList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetProjRestoreLogList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetProjRestoreLogList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetProjRestoreLogList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  取消快照恢复任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.restoreLogId] - 恢复记录ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  backupCancelRestore (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupCancelRestore"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupCancelRestore"
      )
    }

    let postBody = {}
    if (opts.restoreLogId !== undefined && opts.restoreLogId !== null) {
      postBody['restoreLogId'] = opts.restoreLogId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupCancelRestore with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupCancelRestore',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取快照恢复任务详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.restoreLogId] - 恢复记录ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param backupSnapshotRestoreLogRes result  返回结果对象
      */

  backupGetRestoreLogDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  backupGetRestoreLogDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling backupGetRestoreLogDetail"
      )
    }

    let postBody = {}
    if (opts.restoreLogId !== undefined && opts.restoreLogId !== null) {
      postBody['restoreLogId'] = opts.restoreLogId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call backupGetRestoreLogDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/backupGetRestoreLogDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  库权限列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataDatabasePrivilegesListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataDatabasePrivilegesListResPageRespVo result  返回结果对象
      */

  dataDatabasePrivilegesList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabasePrivilegesList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabasePrivilegesList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabasePrivilegesList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabasePrivilegesList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  表、视图、物化视图权限列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataTablePrivilegesListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataTablePrivilegesListResPageRespVo result  返回结果对象
      */

  dataTablePrivilegesList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTablePrivilegesList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTablePrivilegesList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTablePrivilegesList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTablePrivilegesList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  单库用户权限详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataDatabaseUserInfoReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataDatabaseUserInfoResPageRespVo result  返回结果对象
      */

  dataDatabaseUserInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseUserInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseUserInfo"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseUserInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseUserInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  单库角色权限详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataDatabaseRoleInfoReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataDatabaseRoleInfoResPageRespVo result  返回结果对象
      */

  dataDatabaseRoleInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseRoleInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseRoleInfo"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseRoleInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseRoleInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加库授权用户接口
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {array} [opts.dataDatabaseUserInfoList] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataDatabaseUserAdd (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseUserAdd"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseUserAdd"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (
      opts.dataDatabaseUserInfoList !== undefined &&
      opts.dataDatabaseUserInfoList !== null
    ) {
      postBody['dataDatabaseUserInfoList'] = opts.dataDatabaseUserInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseUserAdd with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseUserAdd',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加库授权角色接口
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {array} [opts.dataDatabaseRoleInfoList] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataDatabaseRoleAdd (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseRoleAdd"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseRoleAdd"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (
      opts.dataDatabaseRoleInfoList !== undefined &&
      opts.dataDatabaseRoleInfoList !== null
    ) {
      postBody['dataDatabaseRoleInfoList'] = opts.dataDatabaseRoleInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseRoleAdd with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseRoleAdd',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  取消库用户权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {array} [opts.dataDatabaseUserInfoList] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataDatabaseUserDel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseUserDel"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseUserDel"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (
      opts.dataDatabaseUserInfoList !== undefined &&
      opts.dataDatabaseUserInfoList !== null
    ) {
      postBody['dataDatabaseUserInfoList'] = opts.dataDatabaseUserInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseUserDel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseUserDel',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  取消库角色权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {array} [opts.dataDatabaseRoleInfoList] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataDatabaseRoleDel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseRoleDel"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseRoleDel"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (
      opts.dataDatabaseRoleInfoList !== undefined &&
      opts.dataDatabaseRoleInfoList !== null
    ) {
      postBody['dataDatabaseRoleInfoList'] = opts.dataDatabaseRoleInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseRoleDel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseRoleDel',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑库用户权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {array} [opts.dataDatabaseUserInfoList] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataDatabaseUserUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseUserUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseUserUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (
      opts.dataDatabaseUserInfoList !== undefined &&
      opts.dataDatabaseUserInfoList !== null
    ) {
      postBody['dataDatabaseUserInfoList'] = opts.dataDatabaseUserInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseUserUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseUserUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑库角色权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {array} [opts.dataDatabaseRoleInfoList] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataDatabaseRoleUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataDatabaseRoleUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataDatabaseRoleUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (
      opts.dataDatabaseRoleInfoList !== undefined &&
      opts.dataDatabaseRoleInfoList !== null
    ) {
      postBody['dataDatabaseRoleInfoList'] = opts.dataDatabaseRoleInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataDatabaseRoleUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataDatabaseRoleUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  表、视图、物化视图用户权限详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataTableUserInfoReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataTableUserInfoResPageRespVo result  返回结果对象
      */

  dataTableUserInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableUserInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableUserInfo"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableUserInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableUserInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  表、视图、物化视图角色权限详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataTableRoleInfoReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataTableRoleInfoResPageRespVo result  返回结果对象
      */

  dataTableRoleInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableRoleInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableRoleInfo"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableRoleInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableRoleInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加表、视图、物化视图授权用户接口
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.dataTableUserInfoList] - 表权限信息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataTableUserAdd (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableUserAdd"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableUserAdd"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.dataTableUserInfoList !== undefined &&
      opts.dataTableUserInfoList !== null
    ) {
      postBody['dataTableUserInfoList'] = opts.dataTableUserInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableUserAdd with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableUserAdd',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加表、视图、物化视图授权角色接口
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.dataTableRoleInfoList] - 表权限信息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataTableRoleAdd (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableRoleAdd"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableRoleAdd"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.dataTableRoleInfoList !== undefined &&
      opts.dataTableRoleInfoList !== null
    ) {
      postBody['dataTableRoleInfoList'] = opts.dataTableRoleInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableRoleAdd with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableRoleAdd',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  取消表、视图、物化视图用户权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.dataTableUserInfoList] - 表权限信息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataTableUserDel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableUserDel"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableUserDel"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.dataTableUserInfoList !== undefined &&
      opts.dataTableUserInfoList !== null
    ) {
      postBody['dataTableUserInfoList'] = opts.dataTableUserInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableUserDel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableUserDel',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  取消表、视图、物化视图角色权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.dataTableRoleInfoList] - 表权限信息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataTableRoleDel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableRoleDel"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableRoleDel"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.dataTableRoleInfoList !== undefined &&
      opts.dataTableRoleInfoList !== null
    ) {
      postBody['dataTableRoleInfoList'] = opts.dataTableRoleInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableRoleDel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableRoleDel',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑表、视图、物化视图用户权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.dataTableUserInfoList] - 表权限信息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataTableUserUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableUserUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableUserUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.dataTableUserInfoList !== undefined &&
      opts.dataTableUserInfoList !== null
    ) {
      postBody['dataTableUserInfoList'] = opts.dataTableUserInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableUserUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableUserUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑表、视图、物化视图用户权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.dataTableRoleInfoList] - 表权限信息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dataTableRoleUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataTableRoleUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataTableRoleUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.dataTableRoleInfoList !== undefined &&
      opts.dataTableRoleInfoList !== null
    ) {
      postBody['dataTableRoleInfoList'] = opts.dataTableRoleInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataTableRoleUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataTableRoleUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取库有权限的IAM用户列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.userPin] - 用户名  optional
      * @param {string} [opts.role] - 角色  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result
      */

  ricGetDatabaseIAMUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricGetDatabaseIAMUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricGetDatabaseIAMUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricGetDatabaseIAMUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricGetDatabaseIAMUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取用户有权限的库列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.userPin] - 用户名  optional
      * @param {string} [opts.role] - 角色  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result
      */

  ricGetDatabaseByIAMUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricGetDatabaseByIAMUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricGetDatabaseByIAMUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricGetDatabaseByIAMUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricGetDatabaseByIAMUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询用户是否拥有db_admin角色
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.userPin] - 用户名  optional
      * @param {string} [opts.role] - 角色  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  ricGetUserHasDbAdminRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricGetUserHasDbAdminRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricGetUserHasDbAdminRole"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricGetUserHasDbAdminRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricGetUserHasDbAdminRole',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据用户名(三方/iam)，实例id，库名，权限类型，获取符合条件的表清单
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.userSource] - 用户分类，IMA, CUSTOM  optional
      * @param {string} [opts.tablePrivilege] - 表权限类型：SELECT，INSERT  optional
      * @param {string} [opts.userPin] - 用户名  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result
      */

  ricGetUserAllTableList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricGetUserAllTableList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricGetUserAllTableList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.userSource !== undefined && opts.userSource !== null) {
      postBody['userSource'] = opts.userSource
    }
    if (opts.tablePrivilege !== undefined && opts.tablePrivilege !== null) {
      postBody['tablePrivilege'] = opts.tablePrivilege
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricGetUserAllTableList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricGetUserAllTableList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询数据导出任务列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataExportTaskListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoDataExportTaskRes result  返回结果对象
      */

  dataExportTaskGetList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportTaskGetList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportTaskGetList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportTaskGetList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportTaskGetList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加一条数据导出任务记录，初始进入调度等待状态
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.taskName] - 任务名称  optional
      * @param {array} [opts.taskOwners] - 任务负责人列表  optional
      * @param {string} [opts.taskDesc] - 任务描述  optional
      * @param {string} [opts.taskType] - 任务类型：TEMPORARY-临时任务，PERIODIC-周期任务  optional
      * @param {string} [opts.executionMode] - 执行方式：IMMEDIATE-立即执行，SCHEDULED-指定时间  optional
      * @param {string} [opts.expectedExecTime] - 期望执行时间，executionMode&#x3D;SCHEDULED 时必填  optional
      * @param {string} [opts.exportFileName] - 导出文件名称（不含路径，含后缀可选）  optional
      * @param {string} [opts.exportFileFormat] - 导出文件格式  optional
      * @param {string} [opts.colDelim] - 导出文件格式选项，列分隔符，可选  optional
      * @param {string} [opts.fileEncoding] - 导出文件格式选项，文件编码格式，可选  optional
      * @param {integer} [opts.exportFileTtl] - 导出文件有效期（天），可选  optional
      * @param {string} [opts.executionSql] - 执行SQL  optional
      * @param {integer} [opts.taskTimeout] - 任务超时时间（秒），可选  optional
      * @param {integer} [opts.exeTimeout] - 导出语句执行超时时间（秒），可选  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataExportAddTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportAddTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportAddTask"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskName !== undefined && opts.taskName !== null) {
      postBody['taskName'] = opts.taskName
    }
    if (opts.taskOwners !== undefined && opts.taskOwners !== null) {
      postBody['taskOwners'] = opts.taskOwners
    }
    if (opts.taskDesc !== undefined && opts.taskDesc !== null) {
      postBody['taskDesc'] = opts.taskDesc
    }
    if (opts.taskType !== undefined && opts.taskType !== null) {
      postBody['taskType'] = opts.taskType
    }
    if (opts.executionMode !== undefined && opts.executionMode !== null) {
      postBody['executionMode'] = opts.executionMode
    }
    if (opts.expectedExecTime !== undefined && opts.expectedExecTime !== null) {
      postBody['expectedExecTime'] = opts.expectedExecTime
    }
    if (opts.exportFileName !== undefined && opts.exportFileName !== null) {
      postBody['exportFileName'] = opts.exportFileName
    }
    if (opts.exportFileFormat !== undefined && opts.exportFileFormat !== null) {
      postBody['exportFileFormat'] = opts.exportFileFormat
    }
    if (opts.colDelim !== undefined && opts.colDelim !== null) {
      postBody['colDelim'] = opts.colDelim
    }
    if (opts.fileEncoding !== undefined && opts.fileEncoding !== null) {
      postBody['fileEncoding'] = opts.fileEncoding
    }
    if (opts.exportFileTtl !== undefined && opts.exportFileTtl !== null) {
      postBody['exportFileTtl'] = opts.exportFileTtl
    }
    if (opts.executionSql !== undefined && opts.executionSql !== null) {
      postBody['executionSql'] = opts.executionSql
    }
    if (opts.taskTimeout !== undefined && opts.taskTimeout !== null) {
      postBody['taskTimeout'] = opts.taskTimeout
    }
    if (opts.exeTimeout !== undefined && opts.exeTimeout !== null) {
      postBody['exeTimeout'] = opts.exeTimeout
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportAddTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportAddTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑一条数据导出任务记录
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.id] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.taskName] - 任务名称  optional
      * @param {string} [opts.taskType] - 任务类型：TEMPORARY-临时任务，PERIODIC-周期任务  optional
      * @param {string} [opts.executionMode] - 执行方式：IMMEDIATE-立即执行，SCHEDULED-指定时间  optional
      * @param {array} [opts.taskOwners] - 任务负责人列表  optional
      * @param {string} [opts.taskDesc] - 任务描述  optional
      * @param {string} [opts.exportFileName] - 导出文件名称  optional
      * @param {string} [opts.exportFileFormat] - 导出文件格式  optional
      * @param {string} [opts.executionSql] - 执行SQL  optional
      * @param {string} [opts.expectedExecTime] - 期望执行时间，executionMode&#x3D;SCHEDULED 时必填  optional
      * @param {string} [opts.colDelim] - 导出文件格式选项，列分隔符，可选  optional
      * @param {string} [opts.fileEncoding] - 导出文件格式选项，文件编码格式，可选  optional
      * @param {integer} [opts.exportFileTtl] - 导出文件有效期（天），可选  optional
      * @param {integer} [opts.taskTimeout] - 任务超时时间（秒），可选  optional
      * @param {integer} [opts.exeTimeout] - 导出语句执行超时时间（秒），可选  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataExportEditTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportEditTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportEditTask"
      )
    }

    let postBody = {}
    if (opts.id !== undefined && opts.id !== null) {
      postBody['id'] = opts.id
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskName !== undefined && opts.taskName !== null) {
      postBody['taskName'] = opts.taskName
    }
    if (opts.taskType !== undefined && opts.taskType !== null) {
      postBody['taskType'] = opts.taskType
    }
    if (opts.executionMode !== undefined && opts.executionMode !== null) {
      postBody['executionMode'] = opts.executionMode
    }
    if (opts.taskOwners !== undefined && opts.taskOwners !== null) {
      postBody['taskOwners'] = opts.taskOwners
    }
    if (opts.taskDesc !== undefined && opts.taskDesc !== null) {
      postBody['taskDesc'] = opts.taskDesc
    }
    if (opts.exportFileName !== undefined && opts.exportFileName !== null) {
      postBody['exportFileName'] = opts.exportFileName
    }
    if (opts.exportFileFormat !== undefined && opts.exportFileFormat !== null) {
      postBody['exportFileFormat'] = opts.exportFileFormat
    }
    if (opts.executionSql !== undefined && opts.executionSql !== null) {
      postBody['executionSql'] = opts.executionSql
    }
    if (opts.expectedExecTime !== undefined && opts.expectedExecTime !== null) {
      postBody['expectedExecTime'] = opts.expectedExecTime
    }
    if (opts.colDelim !== undefined && opts.colDelim !== null) {
      postBody['colDelim'] = opts.colDelim
    }
    if (opts.fileEncoding !== undefined && opts.fileEncoding !== null) {
      postBody['fileEncoding'] = opts.fileEncoding
    }
    if (opts.exportFileTtl !== undefined && opts.exportFileTtl !== null) {
      postBody['exportFileTtl'] = opts.exportFileTtl
    }
    if (opts.taskTimeout !== undefined && opts.taskTimeout !== null) {
      postBody['taskTimeout'] = opts.taskTimeout
    }
    if (opts.exeTimeout !== undefined && opts.exeTimeout !== null) {
      postBody['exeTimeout'] = opts.exeTimeout
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportEditTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportEditTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据导出任务详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataExportTaskDetailRes result  返回结果对象
      */

  dataExportGetTaskDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportGetTaskDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportGetTaskDetail"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportGetTaskDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportGetTaskDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除数据导出任务，运行中的任务不支持删除
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataExportDeleteTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportDeleteTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportDeleteTask"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportDeleteTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportDeleteTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止数据导出任务，只有运行中的任务才支持停止
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataExportStopTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportStopTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportStopTask"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportStopTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportStopTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  重跑数据导出任务，只有已完成或失败的任务才支持重跑
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataExportRerunTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportRerunTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportRerunTask"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportRerunTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportRerunTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据导出文件的预签名下载链接，支持链接过期检查和重新生成
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.fileId] - 文件ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataExportFileDownloadRes result  返回结果对象
      */

  dataExportGetFileDownloadUrl (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportGetFileDownloadUrl"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportGetFileDownloadUrl"
      )
    }

    let postBody = {}
    if (opts.fileId !== undefined && opts.fileId !== null) {
      postBody['fileId'] = opts.fileId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportGetFileDownloadUrl with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportGetFileDownloadUrl',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据导出文件下载信息，包含对象存储桶信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.fileId] - 文件ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataExportFileDownloadRes result  返回结果对象
      */

  dataExportGetFileDownloadInfo (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportGetFileDownloadInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportGetFileDownloadInfo"
      )
    }

    let postBody = {}
    if (opts.fileId !== undefined && opts.fileId !== null) {
      postBody['fileId'] = opts.fileId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportGetFileDownloadInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportGetFileDownloadInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除数据导出文件，包括对象存储中的文件和数据库记录
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.fileId] - 文件ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataExportDeleteFile (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportDeleteFile"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportDeleteFile"
      )
    }

    let postBody = {}
    if (opts.fileId !== undefined && opts.fileId !== null) {
      postBody['fileId'] = opts.fileId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportDeleteFile with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportDeleteFile',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询数据导出文件列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataExportFileListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoDataExportFileRes result  返回结果对象
      */

  dataExportFileGetList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataExportFileGetList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataExportFileGetList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataExportFileGetList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataExportFileGetList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询数据上传任务列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataUploadTaskListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoDataUploadTaskRes result  返回结果对象
      */

  dataUploadTaskGetList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadTaskGetList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadTaskGetList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadTaskGetList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadTaskGetList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  生成对象存储文件路径
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.fileName] - 文件名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataUploadGenOssFilePathRes result  返回结果对象
      */

  dataUploadGenOssFilePath (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadGenOssFilePath"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadGenOssFilePath"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.fileName !== undefined && opts.fileName !== null) {
      postBody['fileName'] = opts.fileName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadGenOssFilePath with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadGenOssFilePath',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加对象存储文件元数据
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.fileName] - 文件名称  optional
      * @param {string} [opts.fullFileName] - 文件全路径名，包括路径+文件名  optional
      * @param {string} [opts.fileFormat] - 文件格式  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataUploadAddOssFileMeta (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadAddOssFileMeta"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadAddOssFileMeta"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.fileName !== undefined && opts.fileName !== null) {
      postBody['fileName'] = opts.fileName
    }
    if (opts.fullFileName !== undefined && opts.fullFileName !== null) {
      postBody['fullFileName'] = opts.fullFileName
    }
    if (opts.fileFormat !== undefined && opts.fileFormat !== null) {
      postBody['fileFormat'] = opts.fileFormat
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadAddOssFileMeta with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadAddOssFileMeta',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询数据上传文件列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {dataUploadFileListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoDataUploadFileRes result  返回结果对象
      */

  dataUploadFileGetList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadFileGetList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadFileGetList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadFileGetList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadFileGetList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取支持的列分隔符
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataUploadColDelimRes result
      */

  dataUploadGetColDelims (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadGetColDelims"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadGetColDelims"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadGetColDelims with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadGetColDelims',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  通过DESC FILES语句获取对象存储文件的字段信息，包括字段名、字段类型、是否可为空
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.fileName] - 对象存储文件名称  optional
      * @param {string} [opts.fileFormat] - 文件格式  optional
      * @param {string} [opts.colDelim] - 通用列分隔符  optional
      * @param {string} [opts.nonStdColDelim] - 非通用列分隔符  optional
      * @param {integer} [opts.dataBeginLineNum] - 数据起始行号  optional
      * @param {string} [opts.rowDelim] - 行分隔符  optional
      * @param {string} [opts.customRowDelim] - 自定义行隔符  optional
      * @param {string} [opts.enclosedBy] - 列值包裹符  optional
      * @param {string} [opts.customEnclosedBy] - 自定义列值包裹符  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataUploadFileFieldInfoRes result
      */

  dataUploadGetFileFieldInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadGetFileFieldInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadGetFileFieldInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.fileName !== undefined && opts.fileName !== null) {
      postBody['fileName'] = opts.fileName
    }
    if (opts.fileFormat !== undefined && opts.fileFormat !== null) {
      postBody['fileFormat'] = opts.fileFormat
    }
    if (opts.colDelim !== undefined && opts.colDelim !== null) {
      postBody['colDelim'] = opts.colDelim
    }
    if (opts.nonStdColDelim !== undefined && opts.nonStdColDelim !== null) {
      postBody['nonStdColDelim'] = opts.nonStdColDelim
    }
    if (opts.dataBeginLineNum !== undefined && opts.dataBeginLineNum !== null) {
      postBody['dataBeginLineNum'] = opts.dataBeginLineNum
    }
    if (opts.rowDelim !== undefined && opts.rowDelim !== null) {
      postBody['rowDelim'] = opts.rowDelim
    }
    if (opts.customRowDelim !== undefined && opts.customRowDelim !== null) {
      postBody['customRowDelim'] = opts.customRowDelim
    }
    if (opts.enclosedBy !== undefined && opts.enclosedBy !== null) {
      postBody['enclosedBy'] = opts.enclosedBy
    }
    if (opts.customEnclosedBy !== undefined && opts.customEnclosedBy !== null) {
      postBody['customEnclosedBy'] = opts.customEnclosedBy
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadGetFileFieldInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadGetFileFieldInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  通过Broker Load从对象存储导入数据到StarRocks表中
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.taskName] - 任务名称  optional
      * @param {array} [opts.taskOwners] - 任务负责人列表  optional
      * @param {string} [opts.taskDesc] - 任务描述  optional
      * @param {string} [opts.fileName] - 文件名称  optional
      * @param {string} [opts.uploadFileFormat] - 上传文件格式  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableName] - 目标表名  optional
      * @param {string} [opts.writeMode] - 写入方式  optional
      * @param {string} [opts.fileEncoding] - 文件编码格式  optional
      * @param {string} [opts.colDelim] - 通用列分隔符  optional
      * @param {string} [opts.nonStdColDelim] - 非通用列分隔符  optional
      * @param {integer} [opts.dataBeginLineNum] - 数据起始行号  optional
      * @param {string} [opts.rowDelim] - 行分隔符  optional
      * @param {string} [opts.customRowDelim] - 自定义行隔符  optional
      * @param {string} [opts.enclosedBy] - 列值包裹符  optional
      * @param {string} [opts.customEnclosedBy] - 自定义列值包裹符  optional
      * @param {string} [opts.fieldMapping] - 字段映射  optional
      * @param {integer} [opts.taskTimeout] - 任务超时时间，单位：秒  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataUploadAddTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadAddTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadAddTask"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskName !== undefined && opts.taskName !== null) {
      postBody['taskName'] = opts.taskName
    }
    if (opts.taskOwners !== undefined && opts.taskOwners !== null) {
      postBody['taskOwners'] = opts.taskOwners
    }
    if (opts.taskDesc !== undefined && opts.taskDesc !== null) {
      postBody['taskDesc'] = opts.taskDesc
    }
    if (opts.fileName !== undefined && opts.fileName !== null) {
      postBody['fileName'] = opts.fileName
    }
    if (opts.uploadFileFormat !== undefined && opts.uploadFileFormat !== null) {
      postBody['uploadFileFormat'] = opts.uploadFileFormat
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.writeMode !== undefined && opts.writeMode !== null) {
      postBody['writeMode'] = opts.writeMode
    }
    if (opts.fileEncoding !== undefined && opts.fileEncoding !== null) {
      postBody['fileEncoding'] = opts.fileEncoding
    }
    if (opts.colDelim !== undefined && opts.colDelim !== null) {
      postBody['colDelim'] = opts.colDelim
    }
    if (opts.nonStdColDelim !== undefined && opts.nonStdColDelim !== null) {
      postBody['nonStdColDelim'] = opts.nonStdColDelim
    }
    if (opts.dataBeginLineNum !== undefined && opts.dataBeginLineNum !== null) {
      postBody['dataBeginLineNum'] = opts.dataBeginLineNum
    }
    if (opts.rowDelim !== undefined && opts.rowDelim !== null) {
      postBody['rowDelim'] = opts.rowDelim
    }
    if (opts.customRowDelim !== undefined && opts.customRowDelim !== null) {
      postBody['customRowDelim'] = opts.customRowDelim
    }
    if (opts.enclosedBy !== undefined && opts.enclosedBy !== null) {
      postBody['enclosedBy'] = opts.enclosedBy
    }
    if (opts.customEnclosedBy !== undefined && opts.customEnclosedBy !== null) {
      postBody['customEnclosedBy'] = opts.customEnclosedBy
    }
    if (opts.fieldMapping !== undefined && opts.fieldMapping !== null) {
      postBody['fieldMapping'] = opts.fieldMapping
    }
    if (opts.taskTimeout !== undefined && opts.taskTimeout !== null) {
      postBody['taskTimeout'] = opts.taskTimeout
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadAddTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadAddTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑数据上传任务
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.taskName] - 任务名称  optional
      * @param {array} [opts.taskOwners] - 任务负责人列表  optional
      * @param {string} [opts.taskDesc] - 任务描述  optional
      * @param {string} [opts.fileName] - 文件名称  optional
      * @param {string} [opts.uploadFileFormat] - 上传文件格式  optional
      * @param {string} [opts.fileEncoding] - 文件编码格式  optional
      * @param {string} [opts.colDelim] - 文件格式选项，通用列分隔符  optional
      * @param {string} [opts.nonStdColDelim] - 文件格式选项，非通用列分隔符  optional
      * @param {integer} [opts.dataBeginLineNum] - 文件格式选项，数据起始行号  optional
      * @param {string} [opts.rowDelim] - 行分隔符  optional
      * @param {string} [opts.customRowDelim] - 自定义行隔符  optional
      * @param {string} [opts.enclosedBy] - 列值包裹符  optional
      * @param {string} [opts.customEnclosedBy] - 自定义列值包裹符  optional
      * @param {string} [opts.writeMode] - 写入方式：APPEND-追加，OVERWRITE-覆盖  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataUploadEditTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadEditTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadEditTask"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskName !== undefined && opts.taskName !== null) {
      postBody['taskName'] = opts.taskName
    }
    if (opts.taskOwners !== undefined && opts.taskOwners !== null) {
      postBody['taskOwners'] = opts.taskOwners
    }
    if (opts.taskDesc !== undefined && opts.taskDesc !== null) {
      postBody['taskDesc'] = opts.taskDesc
    }
    if (opts.fileName !== undefined && opts.fileName !== null) {
      postBody['fileName'] = opts.fileName
    }
    if (opts.uploadFileFormat !== undefined && opts.uploadFileFormat !== null) {
      postBody['uploadFileFormat'] = opts.uploadFileFormat
    }
    if (opts.fileEncoding !== undefined && opts.fileEncoding !== null) {
      postBody['fileEncoding'] = opts.fileEncoding
    }
    if (opts.colDelim !== undefined && opts.colDelim !== null) {
      postBody['colDelim'] = opts.colDelim
    }
    if (opts.nonStdColDelim !== undefined && opts.nonStdColDelim !== null) {
      postBody['nonStdColDelim'] = opts.nonStdColDelim
    }
    if (opts.dataBeginLineNum !== undefined && opts.dataBeginLineNum !== null) {
      postBody['dataBeginLineNum'] = opts.dataBeginLineNum
    }
    if (opts.rowDelim !== undefined && opts.rowDelim !== null) {
      postBody['rowDelim'] = opts.rowDelim
    }
    if (opts.customRowDelim !== undefined && opts.customRowDelim !== null) {
      postBody['customRowDelim'] = opts.customRowDelim
    }
    if (opts.enclosedBy !== undefined && opts.enclosedBy !== null) {
      postBody['enclosedBy'] = opts.enclosedBy
    }
    if (opts.customEnclosedBy !== undefined && opts.customEnclosedBy !== null) {
      postBody['customEnclosedBy'] = opts.customEnclosedBy
    }
    if (opts.writeMode !== undefined && opts.writeMode !== null) {
      postBody['writeMode'] = opts.writeMode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadEditTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadEditTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据上传任务详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataUploadTaskDetailRes result  返回结果对象
      */

  dataUploadGetTaskDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadGetTaskDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadGetTaskDetail"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadGetTaskDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadGetTaskDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  重跑数据上传任务，运行中的任务不支持重跑
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataUploadRerunTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadRerunTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadRerunTask"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadRerunTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadRerunTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除数据上传任务，运行中的任务不支持删除
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataUploadDeleteTask (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadDeleteTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadDeleteTask"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadDeleteTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadDeleteTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据任务ID获取数据上传任务的错误详情，通过执行trackingSQL获取详细错误信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.taskId] - 任务ID  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param dataUploadTaskErrDetailRes result  返回结果对象
      */

  dataUploadGetTaskErrDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadGetTaskErrDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadGetTaskErrDetail"
      )
    }

    let postBody = {}
    if (opts.taskId !== undefined && opts.taskId !== null) {
      postBody['taskId'] = opts.taskId
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadGetTaskErrDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadGetTaskErrDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  批量删除数据上传文件，同步删除对象存储中的文件和数据库中的文件记录
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {array} [opts.fileIdList] - 文件ID列表  optional
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  dataUploadBatchDeleteFile (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dataUploadBatchDeleteFile"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dataUploadBatchDeleteFile"
      )
    }

    let postBody = {}
    if (opts.fileIdList !== undefined && opts.fileIdList !== null) {
      postBody['fileIdList'] = opts.fileIdList
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dataUploadBatchDeleteFile with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dataUploadBatchDeleteFile',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  数据查询目录、作业列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {boolean} [opts.isOwner] - 是否是私有的  optional
      * @param {boolean} [opts.onlyMenu] - 是否单独查询目录目录  optional
      * @param {boolean} [opts.isManage] - 共享中是否我负责的，默认是否  optional
      * @param {string} [opts.name] - 查询名称  optional
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param developTreeRes result
      */

  developTree (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developTree"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developTree"
      )
    }

    let postBody = {}
    if (opts.isOwner !== undefined && opts.isOwner !== null) {
      postBody['isOwner'] = opts.isOwner
    }
    if (opts.onlyMenu !== undefined && opts.onlyMenu !== null) {
      postBody['onlyMenu'] = opts.onlyMenu
    }
    if (opts.isManage !== undefined && opts.isManage !== null) {
      postBody['isManage'] = opts.isManage
    }
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developTree with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developTree',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取当前登陆用户作业权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskCode] - 作业code  optional
      * @param {string} [opts.projectCode] - 项目Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param developPrivilegeTypeRes result  返回结果对象
      */

  developPrivilegeInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developPrivilegeInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developPrivilegeInfo"
      )
    }

    let postBody = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developPrivilegeInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developPrivilegeInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  节点创建，必须的参数：projectCode、developTaskType、isDir、name、developTaskType，返回 code
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.code] - 目录id，修改、移动、删除时必传  optional
      * @param {string} [opts.name] - 目录名称、创建、修改时必须传  optional
      * @param {string} [opts.parentMenuCode] - 父目录id，创建、移动时必须传  optional
      * @param {string} [opts.targetMenuCode] - 目标目录id，移动时必须传  optional
      * @param {boolean} [opts.isDir] - 是否是目录，true是目录，false是task  optional
      * @param {string} [opts.developTaskType] - 排序方向  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回结果
      */

  developMenuCreate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developMenuCreate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developMenuCreate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.code !== undefined && opts.code !== null) {
      postBody['code'] = opts.code
    }
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.parentMenuCode !== undefined && opts.parentMenuCode !== null) {
      postBody['parentMenuCode'] = opts.parentMenuCode
    }
    if (opts.targetMenuCode !== undefined && opts.targetMenuCode !== null) {
      postBody['targetMenuCode'] = opts.targetMenuCode
    }
    if (opts.isDir !== undefined && opts.isDir !== null) {
      postBody['isDir'] = opts.isDir
    }
    if (opts.developTaskType !== undefined && opts.developTaskType !== null) {
      postBody['developTaskType'] = opts.developTaskType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developMenuCreate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developMenuCreate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  节点更新,必须的参数：projectCode、code、developTaskType、isDir、name
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.code] - 目录id，修改、移动、删除时必传  optional
      * @param {string} [opts.name] - 目录名称、创建、修改时必须传  optional
      * @param {string} [opts.parentMenuCode] - 父目录id，创建、移动时必须传  optional
      * @param {string} [opts.targetMenuCode] - 目标目录id，移动时必须传  optional
      * @param {boolean} [opts.isDir] - 是否是目录，true是目录，false是task  optional
      * @param {string} [opts.developTaskType] - 排序方向  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  developMenuUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developMenuUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developMenuUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.code !== undefined && opts.code !== null) {
      postBody['code'] = opts.code
    }
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.parentMenuCode !== undefined && opts.parentMenuCode !== null) {
      postBody['parentMenuCode'] = opts.parentMenuCode
    }
    if (opts.targetMenuCode !== undefined && opts.targetMenuCode !== null) {
      postBody['targetMenuCode'] = opts.targetMenuCode
    }
    if (opts.isDir !== undefined && opts.isDir !== null) {
      postBody['isDir'] = opts.isDir
    }
    if (opts.developTaskType !== undefined && opts.developTaskType !== null) {
      postBody['developTaskType'] = opts.developTaskType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developMenuUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developMenuUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  节点删除，必须的参数：projectCode、code
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.code] - 目录id，修改、移动、删除时必传  optional
      * @param {string} [opts.name] - 目录名称、创建、修改时必须传  optional
      * @param {string} [opts.parentMenuCode] - 父目录id，创建、移动时必须传  optional
      * @param {string} [opts.targetMenuCode] - 目标目录id，移动时必须传  optional
      * @param {boolean} [opts.isDir] - 是否是目录，true是目录，false是task  optional
      * @param {string} [opts.developTaskType] - 排序方向  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  developMenuDelete (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developMenuDelete"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developMenuDelete"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.code !== undefined && opts.code !== null) {
      postBody['code'] = opts.code
    }
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.parentMenuCode !== undefined && opts.parentMenuCode !== null) {
      postBody['parentMenuCode'] = opts.parentMenuCode
    }
    if (opts.targetMenuCode !== undefined && opts.targetMenuCode !== null) {
      postBody['targetMenuCode'] = opts.targetMenuCode
    }
    if (opts.isDir !== undefined && opts.isDir !== null) {
      postBody['isDir'] = opts.isDir
    }
    if (opts.developTaskType !== undefined && opts.developTaskType !== null) {
      postBody['developTaskType'] = opts.developTaskType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developMenuDelete with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developMenuDelete',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  节点移动，必须的参数：projectCode、code、isDir、targetMenuCode
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.code] - 目录id，修改、移动、删除时必传  optional
      * @param {string} [opts.name] - 目录名称、创建、修改时必须传  optional
      * @param {string} [opts.parentMenuCode] - 父目录id，创建、移动时必须传  optional
      * @param {string} [opts.targetMenuCode] - 目标目录id，移动时必须传  optional
      * @param {boolean} [opts.isDir] - 是否是目录，true是目录，false是task  optional
      * @param {string} [opts.developTaskType] - 排序方向  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  developMenuMove (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developMenuMove"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developMenuMove"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.code !== undefined && opts.code !== null) {
      postBody['code'] = opts.code
    }
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.parentMenuCode !== undefined && opts.parentMenuCode !== null) {
      postBody['parentMenuCode'] = opts.parentMenuCode
    }
    if (opts.targetMenuCode !== undefined && opts.targetMenuCode !== null) {
      postBody['targetMenuCode'] = opts.targetMenuCode
    }
    if (opts.isDir !== undefined && opts.isDir !== null) {
      postBody['isDir'] = opts.isDir
    }
    if (opts.developTaskType !== undefined && opts.developTaskType !== null) {
      postBody['developTaskType'] = opts.developTaskType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developMenuMove with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developMenuMove',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  节点名称校验
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.name] - 目录名称、创建、修改时必须传  optional
      * @param {string} [opts.parentMenuCode] - 父目录id，创建、移动时必须传  optional
      * @param {boolean} [opts.isDir] - 是否是目录，true是目录，false是task  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  checkDevelopNodeName (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  checkDevelopNodeName"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling checkDevelopNodeName"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.name !== undefined && opts.name !== null) {
      postBody['name'] = opts.name
    }
    if (opts.parentMenuCode !== undefined && opts.parentMenuCode !== null) {
      postBody['parentMenuCode'] = opts.parentMenuCode
    }
    if (opts.isDir !== undefined && opts.isDir !== null) {
      postBody['isDir'] = opts.isDir
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call checkDevelopNodeName with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/checkDevelopNodeName',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  作业移交
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.taskCode] - 作业ID  optional
      * @param {string} [opts.pin] - 用户  optional
      * @param {string} [opts.developTaskType] - 排序方向  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  developTransfer (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developTransfer"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developTransfer"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.pin !== undefined && opts.pin !== null) {
      postBody['pin'] = opts.pin
    }
    if (opts.developTaskType !== undefined && opts.developTaskType !== null) {
      postBody['developTaskType'] = opts.developTaskType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developTransfer with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developTransfer',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  作业移交
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.taskCode] - 作业id  optional
      * @param {string} [opts.sql] - sql 加密  optional
      * @param {boolean} [opts.execMode] - true 是全量执行，false 是选中执行  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param syntaxRes result  返回结果对象
      */

  developSqlCheck (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developSqlCheck"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developSqlCheck"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.sql !== undefined && opts.sql !== null) {
      postBody['sql'] = opts.sql
    }
    if (opts.execMode !== undefined && opts.execMode !== null) {
      postBody['execMode'] = opts.execMode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developSqlCheck with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developSqlCheck',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  sql作业抢锁
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.taskCode] - 作业code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param developDraftSqlRes result  返回结果对象
      */

  developGetLock (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developGetLock"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developGetLock"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developGetLock with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developGetLock',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  sql作业保存
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.taskCode] - 作业id  optional
      * @param {string} [opts.sql] - sql 加密  optional
      * @param {boolean} [opts.execMode] - true 是全量执行，false 是选中执行  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  developSqlSave (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developSqlSave"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developSqlSave"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.sql !== undefined && opts.sql !== null) {
      postBody['sql'] = opts.sql
    }
    if (opts.execMode !== undefined && opts.execMode !== null) {
      postBody['execMode'] = opts.execMode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developSqlSave with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developSqlSave',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  sql作业执行
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.taskCode] - 作业id  optional
      * @param {string} [opts.sql] - sql 加密  optional
      * @param {boolean} [opts.execMode] - true 是全量执行，false 是选中执行  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回结果
      */

  developSqlStart (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developSqlStart"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developSqlStart"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.sql !== undefined && opts.sql !== null) {
      postBody['sql'] = opts.sql
    }
    if (opts.execMode !== undefined && opts.execMode !== null) {
      postBody['execMode'] = opts.execMode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developSqlStart with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developSqlStart',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  sql作业日志状态获取
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.runCode] - 运行code  optional
      * @param {string} [opts.projectCode] - 项目Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param developLogRes result  返回结果对象
      */

  getRunLog (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getRunLog"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getRunLog"
      )
    }

    let postBody = {}
    if (opts.runCode !== undefined && opts.runCode !== null) {
      postBody['runCode'] = opts.runCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getRunLog with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getRunLog',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  sql作业结果获取
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.runCode] - 运行code  optional
      * @param {string} [opts.projectCode] - 项目Code  optional
      * @param {integer} [opts.resultPosition] - 结果次序  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param developResultRes result  返回结果对象
      */

  getRunResult (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getRunResult"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getRunResult"
      )
    }

    let postBody = {}
    if (opts.runCode !== undefined && opts.runCode !== null) {
      postBody['runCode'] = opts.runCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.resultPosition !== undefined && opts.resultPosition !== null) {
      postBody['resultPosition'] = opts.resultPosition
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getRunResult with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getRunResult',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取sql作业内容
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.taskCode] - 作业code  optional
      * @param {string} [opts.projectCode] - 项目Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param developDraftSqlRes result  返回结果对象
      */

  getDevelopSql (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getDevelopSql"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getDevelopSql"
      )
    }

    let postBody = {}
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getDevelopSql with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getDevelopSql',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取sql作业运行记录
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {runListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageResRunLogListRes result  返回结果对象
      */

  scriptRunLogList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  scriptRunLogList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling scriptRunLogList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call scriptRunLogList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/scriptRunLogList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取sql作业运行快照
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.runCode] - 运行code  optional
      * @param {string} [opts.projectCode] - 项目Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param runInfoRes result  返回结果对象
      */

  getScriptRunSnapshot (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getScriptRunSnapshot"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getScriptRunSnapshot"
      )
    }

    let postBody = {}
    if (opts.runCode !== undefined && opts.runCode !== null) {
      postBody['runCode'] = opts.runCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getScriptRunSnapshot with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getScriptRunSnapshot',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取 catalog 列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.table] - 表名  optional
      * @param {boolean} [opts.owner] - 是否是我的  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  数据或者是分页
      */

  getCatalogList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getCatalogList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getCatalogList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.table !== undefined && opts.table !== null) {
      postBody['table'] = opts.table
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getCatalogList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getCatalogList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取 database 列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.table] - 表名  optional
      * @param {boolean} [opts.owner] - 是否是我的  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  数据或者是分页
      */

  getDatabaseList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getDatabaseList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getDatabaseList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.table !== undefined && opts.table !== null) {
      postBody['table'] = opts.table
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getDatabaseList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getDatabaseList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取 table 列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.table] - 表名  optional
      * @param {boolean} [opts.owner] - 是否是我的  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  数据或者是分页
      */

  getTableList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getTableList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getTableList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.table !== undefined && opts.table !== null) {
      postBody['table'] = opts.table
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getTableList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getTableList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取 table 列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.table] - 表名  optional
      * @param {boolean} [opts.owner] - 是否是我的  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param tableQueryRes result  返回结果对象
      */

  getColumList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getColumList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getColumList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.table !== undefined && opts.table !== null) {
      postBody['table'] = opts.table
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getColumList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getColumList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  脚本参数保存/更新
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.taskCode] - 作业Code  optional
      * @param {array} [opts.params]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  developScriptParams (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developScriptParams"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developScriptParams"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }
    if (opts.params !== undefined && opts.params !== null) {
      postBody['params'] = opts.params
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developScriptParams with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developScriptParams',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获得脚本参数
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.taskCode] - 作业Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param taskParam result
      */

  developScriptParamsQueries (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developScriptParamsQueries"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developScriptParamsQueries"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.taskCode !== undefined && opts.taskCode !== null) {
      postBody['taskCode'] = opts.taskCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developScriptParamsQueries with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developScriptParamsQueries',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  sql转化
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.sql] - sql  optional
      * @param {string} [opts.write] - 输出的sql语法  optional
      * @param {string} [opts.read] - 输入的sql语法  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回结果
      */

  developTranslateSql (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  developTranslateSql"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling developTranslateSql"
      )
    }

    let postBody = {}
    if (opts.sql !== undefined && opts.sql !== null) {
      postBody['sql'] = opts.sql
    }
    if (opts.write !== undefined && opts.write !== null) {
      postBody['write'] = opts.write
    }
    if (opts.read !== undefined && opts.read !== null) {
      postBody['read'] = opts.read
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call developTranslateSql with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/developTranslateSql',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询StarRocks集群的CPU和内存负载信息，只有管理员可以访问
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code，必填  optional
      * @param {string} [opts.warehouseName] - 仓库名称，可选。存算分离架构时可按仓库过滤  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param clusterLoadQueryRes result  返回结果对象
      */

  clusterQueryLoad (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  clusterQueryLoad"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling clusterQueryLoad"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call clusterQueryLoad with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/clusterQueryLoad',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取外部引擎列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param externalEnginRes result  返回结果对象
      */

  getExternalEnginList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getExternalEnginList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getExternalEnginList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getExternalEnginList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getExternalEnginList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加外部引擎
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} [opts.enginType] - 外部引擎类型，目前仅有JCW  optional
      * @param {string} [opts.restartTime] - 重启时间，如果没有立即重启  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  addExternalEngin (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  addExternalEngin"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling addExternalEngin"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.enginType !== undefined && opts.enginType !== null) {
      postBody['enginType'] = opts.enginType
    }
    if (opts.restartTime !== undefined && opts.restartTime !== null) {
      postBody['restartTime'] = opts.restartTime
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call addExternalEngin with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/addExternalEngin',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新外部引擎
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.enginCode] - enginCode  optional
      * @param {string} [opts.restartTime] - 重启时间  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  updateExternalEngin (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateExternalEngin"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling updateExternalEngin"
      )
    }

    let postBody = {}
    if (opts.enginCode !== undefined && opts.enginCode !== null) {
      postBody['enginCode'] = opts.enginCode
    }
    if (opts.restartTime !== undefined && opts.restartTime !== null) {
      postBody['restartTime'] = opts.restartTime
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateExternalEngin with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/updateExternalEngin',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上传外部引擎配置文件，支持hdfs-site.xml和core-site.xml配置文件内容的base64编码上传
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目编码  optional
      * @param {string} [opts.confType] - 配置文件类型，可选值：hdfs-site（HDFS配置文件）、core-site（Core配置文件）  optional
      * @param {string} [opts.fileContentBase64] - 配置文件内容的base64编码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  uploadExternalEnginConf (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  uploadExternalEnginConf"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling uploadExternalEnginConf"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.confType !== undefined && opts.confType !== null) {
      postBody['confType'] = opts.confType
    }
    if (
      opts.fileContentBase64 !== undefined &&
      opts.fileContentBase64 !== null
    ) {
      postBody['fileContentBase64'] = opts.fileContentBase64
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call uploadExternalEnginConf with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/uploadExternalEnginConf',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取hdfs-site文件
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  文件id
      */

  getHdfsSiteConf (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getHdfsSiteConf"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getHdfsSiteConf"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getHdfsSiteConf with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getHdfsSiteConf',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取core-site文件
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  文件id
      */

  getCoreSiteConf (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getCoreSiteConf"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getCoreSiteConf"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getCoreSiteConf with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getCoreSiteConf',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据源类型枚举
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param externalDatasourceEnumRes result  返回结果对象
      */

  externalGetDatasourceEnum (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetDatasourceEnum"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetDatasourceEnum"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling externalGetDatasourceEnum"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetDatasourceEnum with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetDatasourceEnum',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取云上实例
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例的Code  optional
      * @param {string} [opts.instanceId] - 实例id  optional
      * @param {string} [opts.instanceName] - 实例名称  optional
      * @param {string} [opts.catalogType] - 数据源类型  optional
      * @param {string} [opts.region] - 地域code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param cloudInstanceVo result
      */

  externalDatasourceGetCloudInstances (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalDatasourceGetCloudInstances"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalDatasourceGetCloudInstances"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.instanceName !== undefined && opts.instanceName !== null) {
      postBody['instanceName'] = opts.instanceName
    }
    if (opts.catalogType !== undefined && opts.catalogType !== null) {
      postBody['catalogType'] = opts.catalogType
    }
    if (opts.region !== undefined && opts.region !== null) {
      postBody['region'] = opts.region
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalDatasourceGetCloudInstances with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalDatasourceGetCloudInstances',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建catalog，DatasourceSave
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.datasourceMode] - 实例模式  optional
      * @param {string} [opts.catalogType] - catalog类型  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.catalogDesc] - catalog描述  optional
      * @param {string} [opts.connectConfig] - 链接信息，json模式，每段信息包含key，chKey，value  optional
      * @param {array} [opts.connectTestResultList] - 连通性测试结果  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  externalCreateCatalog (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalCreateCatalog"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalCreateCatalog"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.datasourceMode !== undefined && opts.datasourceMode !== null) {
      postBody['datasourceMode'] = opts.datasourceMode
    }
    if (opts.catalogType !== undefined && opts.catalogType !== null) {
      postBody['catalogType'] = opts.catalogType
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.catalogDesc !== undefined && opts.catalogDesc !== null) {
      postBody['catalogDesc'] = opts.catalogDesc
    }
    if (opts.connectConfig !== undefined && opts.connectConfig !== null) {
      postBody['connectConfig'] = opts.connectConfig
    }
    if (
      opts.connectTestResultList !== undefined &&
      opts.connectTestResultList !== null
    ) {
      postBody['connectTestResultList'] = opts.connectTestResultList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalCreateCatalog with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalCreateCatalog',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  连通性测试
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.datasourceMode] - 实例模式  optional
      * @param {string} [opts.catalogType] - catalog类型  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.catalogDesc] - catalog描述  optional
      * @param {string} [opts.connectConfig] - 链接信息，json模式，每段信息包含key，chKey，value  optional
      * @param {array} [opts.connectTestResultList] - 连通性测试结果  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param connectTestVo result
      */

  externalDatasourceConnectTest (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalDatasourceConnectTest"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalDatasourceConnectTest"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.datasourceMode !== undefined && opts.datasourceMode !== null) {
      postBody['datasourceMode'] = opts.datasourceMode
    }
    if (opts.catalogType !== undefined && opts.catalogType !== null) {
      postBody['catalogType'] = opts.catalogType
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.catalogDesc !== undefined && opts.catalogDesc !== null) {
      postBody['catalogDesc'] = opts.catalogDesc
    }
    if (opts.connectConfig !== undefined && opts.connectConfig !== null) {
      postBody['connectConfig'] = opts.connectConfig
    }
    if (
      opts.connectTestResultList !== undefined &&
      opts.connectTestResultList !== null
    ) {
      postBody['connectTestResultList'] = opts.connectTestResultList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalDatasourceConnectTest with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalDatasourceConnectTest',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取catalog列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {externalGetCatalogListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoExternalGetCatalogListRes result  返回结果对象
      */

  externalGetCatalogList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetCatalogList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetCatalogList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetCatalogList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetCatalogList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取catalog详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param externalGetCatalogRes result  返回结果对象
      */

  externalGetCatalogInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetCatalogInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetCatalogInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetCatalogInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetCatalogInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除catalog
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  externalDelCatalog (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalDelCatalog"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalDelCatalog"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalDelCatalog with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalDelCatalog',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {externalGetCatalogReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoString result  返回结果对象
      */

  externalGetTableList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetTableList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetTableList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetTableList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetTableList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取字段列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param externalGetColumnRes result
      */

  externalGetColumnList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetColumnList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetColumnList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetColumnList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetColumnList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  外部catalog权限列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param externalCatalogPrivilegeRes result
      */

  externalCatalogPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalCatalogPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalCatalogPrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalCatalogPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalCatalogPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改外部catalog权限列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.masterUserPin] - 负责人pin  optional
      * @param {array} [opts.userPinList] - 用户pin列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  externalCatalogPrivilegeUpdate (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalCatalogPrivilegeUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalCatalogPrivilegeUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.masterUserPin !== undefined && opts.masterUserPin !== null) {
      postBody['masterUserPin'] = opts.masterUserPin
    }
    if (opts.userPinList !== undefined && opts.userPinList !== null) {
      postBody['userPinList'] = opts.userPinList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalCatalogPrivilegeUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalCatalogPrivilegeUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取云上数据湖实例详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.region] - 地域code  optional
      * @param {string} [opts.projectCode] - 项目实例的Code  optional
      * @param {string} [opts.catalogType] - 数据源类型  optional
      * @param {string} [opts.instanceName] - 云上数据湖实例名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param externalCloudLakeDetailRes result  返回结果对象
      */

  externalGetCloudLakeDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetCloudLakeDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetCloudLakeDetail"
      )
    }

    let postBody = {}
    if (opts.region !== undefined && opts.region !== null) {
      postBody['region'] = opts.region
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalogType !== undefined && opts.catalogType !== null) {
      postBody['catalogType'] = opts.catalogType
    }
    if (opts.instanceName !== undefined && opts.instanceName !== null) {
      postBody['instanceName'] = opts.instanceName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetCloudLakeDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetCloudLakeDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取数据库列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  数据库名称
      */

  externalGetDbList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetDbList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetDbList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetDbList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetDbList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例的Code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param externalGetTableDetailRes result  返回结果对象
      */

  externalGetTableDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  externalGetTableDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling externalGetTableDetail"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call externalGetTableDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/externalGetTableDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  通过sql语句创建数据导入作业
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目code  optional
      * @param {string} [opts.jobName] - 作业名称  optional
      * @param {string} [opts.dbName] - 作业所属库  optional
      * @param {string} [opts.tableName] - 目标表  optional
      * @param {array} [opts.jobAdmin] - 作业负责人  optional
      * @param {string} [opts.jobSql] - sql语句  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  routineLoadCreate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  routineLoadCreate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling routineLoadCreate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.jobName !== undefined && opts.jobName !== null) {
      postBody['jobName'] = opts.jobName
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.jobAdmin !== undefined && opts.jobAdmin !== null) {
      postBody['jobAdmin'] = opts.jobAdmin
    }
    if (opts.jobSql !== undefined && opts.jobSql !== null) {
      postBody['jobSql'] = opts.jobSql
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call routineLoadCreate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/routineLoadCreate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  通过sql语句编辑作业
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.jobName] - 作业名称  optional
      * @param {string} [opts.dbName] - 作业所属库  optional
      * @param {string} [opts.jobSql] - sql语句  optional
      * @param {array} [opts.jobAdmin] - 负责人  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  routineLoadUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  routineLoadUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling routineLoadUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.jobName !== undefined && opts.jobName !== null) {
      postBody['jobName'] = opts.jobName
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.jobSql !== undefined && opts.jobSql !== null) {
      postBody['jobSql'] = opts.jobSql
    }
    if (opts.jobAdmin !== undefined && opts.jobAdmin !== null) {
      postBody['jobAdmin'] = opts.jobAdmin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call routineLoadUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/routineLoadUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  校验数据导入sql
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目code  optional
      * @param {string} [opts.jobName] - 作业名称  optional
      * @param {string} [opts.dbName] - 作业所属库  optional
      * @param {string} [opts.tableName] - 数据表  optional
      * @param {string} [opts.opType] - 操作类型：CREATE 、 ALTER  optional
      * @param {string} [opts.jobSql] - sql语句  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param syntaxRes result  返回结果对象
      */

  routineLoadCheckSql (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  routineLoadCheckSql"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling routineLoadCheckSql"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.jobName !== undefined && opts.jobName !== null) {
      postBody['jobName'] = opts.jobName
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.opType !== undefined && opts.opType !== null) {
      postBody['opType'] = opts.opType
    }
    if (opts.jobSql !== undefined && opts.jobSql !== null) {
      postBody['jobSql'] = opts.jobSql
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call routineLoadCheckSql with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/routineLoadCheckSql',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询数据导入
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {routineLoadQueryReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param routineLoadQueryResPageRespVo result  返回结果对象
      */

  routineLoadQuery (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  routineLoadQuery"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling routineLoadQuery"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call routineLoadQuery with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/routineLoadQuery',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询数据导入作业明细
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.dbName] - 作业所属库  optional
      * @param {string} [opts.jobName] - 作业名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param routineLoadGetDetailRes result  返回结果对象
      */

  routineLoadGetDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  routineLoadGetDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling routineLoadGetDetail"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.jobName !== undefined && opts.jobName !== null) {
      postBody['jobName'] = opts.jobName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call routineLoadGetDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/routineLoadGetDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改作业管理员
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目代码  optional
      * @param {string} [opts.jobName] - 作业名称  optional
      * @param {string} [opts.dbName] - 作业所属库  optional
      * @param {array} [opts.jobAdmin] - 作业管理员  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  routineLoadChangeJobAdmin (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  routineLoadChangeJobAdmin"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling routineLoadChangeJobAdmin"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.jobName !== undefined && opts.jobName !== null) {
      postBody['jobName'] = opts.jobName
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.jobAdmin !== undefined && opts.jobAdmin !== null) {
      postBody['jobAdmin'] = opts.jobAdmin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call routineLoadChangeJobAdmin with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/routineLoadChangeJobAdmin',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改作业状态
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目code  optional
      * @param {string} [opts.jobName] - 作业名称  optional
      * @param {string} [opts.dbName] - 作业所属库  optional
      * @param {string} [opts.jobStatus] - 将作业改变为的目标状态  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  routineLoadChangJobStatus (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  routineLoadChangJobStatus"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling routineLoadChangJobStatus"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.jobName !== undefined && opts.jobName !== null) {
      postBody['jobName'] = opts.jobName
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.jobStatus !== undefined && opts.jobStatus !== null) {
      postBody['jobStatus'] = opts.jobStatus
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call routineLoadChangJobStatus with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/routineLoadChangJobStatus',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取所有枚举
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param developEnumsVo result  返回结果对象
      */

  getEnum (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getEnum"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getEnum"
      )
    }

    let postBody = null
    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getEnum with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getEnum',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取catalog列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回结果
      */

  metaGetCatalogList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetCatalogList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetCatalogList"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling metaGetCatalogList"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetCatalogList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetCatalogList',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取database列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metaDatabaseInfoRes result
      */

  metaGetDatabaseList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetDatabaseList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetDatabaseList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetDatabaseList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetDatabaseList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  外部获取 database 列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.companyCode] - 公司ID  optional
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {boolean} [opts.owner] - 是否是我的  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 负责人密码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  数据或者是分页
      */

  ricGetDatabaseList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricGetDatabaseList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricGetDatabaseList"
      )
    }

    let postBody = {}
    if (opts.companyCode !== undefined && opts.companyCode !== null) {
      postBody['companyCode'] = opts.companyCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricGetDatabaseList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricGetDatabaseList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  外部获取 table 列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.companyCode] - 公司ID  optional
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {boolean} [opts.owner] - 是否是我的  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 负责人密码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  数据或者是分页
      */

  ricGetTableList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricGetTableList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricGetTableList"
      )
    }

    let postBody = {}
    if (opts.companyCode !== undefined && opts.companyCode !== null) {
      postBody['companyCode'] = opts.companyCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricGetTableList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricGetTableList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建据库
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.databaseDesc] - 数据库描述  optional
      * @param {string} [opts.masterPin] - 负责人  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaCreateDatabase (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaCreateDatabase"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaCreateDatabase"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.databaseDesc !== undefined && opts.databaseDesc !== null) {
      postBody['databaseDesc'] = opts.databaseDesc
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaCreateDatabase with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaCreateDatabase',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改数据库描述、负责人
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.databaseDesc] - 数据库描述  optional
      * @param {string} [opts.masterPin] - 负责人  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaUpdateDatabase (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaUpdateDatabase"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaUpdateDatabase"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.databaseDesc !== undefined && opts.databaseDesc !== null) {
      postBody['databaseDesc'] = opts.databaseDesc
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaUpdateDatabase with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaUpdateDatabase',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改表负责人
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} [opts.masterPin] - 负责人  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaUpdateTableMaster (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaUpdateTableMaster"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaUpdateTableMaster"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaUpdateTableMaster with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaUpdateTableMaster',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除数据库
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaDelDatabase (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaDelDatabase"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaDelDatabase"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaDelDatabase with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaDelDatabase',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  内部表、视图列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {metaGetTableListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoMetaTableInfoRes result  返回结果对象
      */

  metaGetTableList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTableList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTableList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTableList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTableList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  校验表名
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaCheckTableName (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaCheckTableName"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaCheckTableName"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaCheckTableName with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaCheckTableName',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表、视图名称  optional
      * @param {boolean} [opts.changeTableName] - 是否修改表名称  optional
      * @param {string} [opts.tableNameNew] - 表、视图名称  optional
      * @param {string} [opts.tableCommon] - 表中文名  optional
      * @param {string} [opts.tableModel] - 表类型：VIEW 视图，MATERIALIZED_VIEW 物化视图、不填：表、PRIMARY_KEYS 主键表、DUP_KEYS：明细表、AGGREGATE_KEY：聚合表  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 密码  optional
      * @param {array} [opts.metaColumnInfo] - 列信息  optional
      * @param {metaPartitionInfo} [opts.partitionInfo] - 分区信息  optional
      * @param {metaProperties} [opts.properties] - 表属性信息  optional
      * @param {metaIndexes} [opts.metaIndexes] - 索引列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaCreateTable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaCreateTable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaCreateTable"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.changeTableName !== undefined && opts.changeTableName !== null) {
      postBody['changeTableName'] = opts.changeTableName
    }
    if (opts.tableNameNew !== undefined && opts.tableNameNew !== null) {
      postBody['tableNameNew'] = opts.tableNameNew
    }
    if (opts.tableCommon !== undefined && opts.tableCommon !== null) {
      postBody['tableCommon'] = opts.tableCommon
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }
    if (opts.metaColumnInfo !== undefined && opts.metaColumnInfo !== null) {
      postBody['metaColumnInfo'] = opts.metaColumnInfo
    }
    if (opts.partitionInfo !== undefined && opts.partitionInfo !== null) {
      postBody['partitionInfo'] = opts.partitionInfo
    }
    if (opts.properties !== undefined && opts.properties !== null) {
      postBody['properties'] = opts.properties
    }
    if (opts.metaIndexes !== undefined && opts.metaIndexes !== null) {
      postBody['metaIndexes'] = opts.metaIndexes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaCreateTable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaCreateTable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表、视图名称  optional
      * @param {string} [opts.tableCommon] - 表中文名  optional
      * @param {string} [opts.tableModel] - 表类型：VIEW 视图，MATERIALIZED_VIEW 物化视图、不填：表、PRIMARY_KEYS 主键表、DUP_KEYS：明细表、AGGREGATE_KEY：聚合表  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 密码  optional
      * @param {array} [opts.metaColumnInfo] - 列信息  optional
      * @param {string} [opts.properties] - 配置  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  ricMetaCreateTable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricMetaCreateTable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricMetaCreateTable"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.tableCommon !== undefined && opts.tableCommon !== null) {
      postBody['tableCommon'] = opts.tableCommon
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }
    if (opts.metaColumnInfo !== undefined && opts.metaColumnInfo !== null) {
      postBody['metaColumnInfo'] = opts.metaColumnInfo
    }
    if (opts.properties !== undefined && opts.properties !== null) {
      postBody['properties'] = opts.properties
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricMetaCreateTable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricMetaCreateTable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 密码  optional
      * @param {string} [opts.createSql] - 建表sql  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  ricCreateTableSubmit (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricCreateTableSubmit"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricCreateTableSubmit"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }
    if (opts.createSql !== undefined && opts.createSql !== null) {
      postBody['createSql'] = opts.createSql
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricCreateTableSubmit with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricCreateTableSubmit',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  sql语法校验
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 密码  optional
      * @param {string} [opts.sql] - sql 加密  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param syntaxRes result  返回结果对象
      */

  ricSqlCheck (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricSqlCheck"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricSqlCheck"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }
    if (opts.sql !== undefined && opts.sql !== null) {
      postBody['sql'] = opts.sql
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricSqlCheck with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricSqlCheck',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取创建视图建表语句
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.viewName] - 视图名称  optional
      * @param {string} [opts.viewCommon] - 视图注释  optional
      * @param {string} [opts.viewModel] - 表类型：VIEW 视图，MATERIALIZED_VIEW 物化视图  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.queryStatement] - 查询语句  optional
      * @param {metaMaterializedView} [opts.metaMaterializedView] - 物化视图配置  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回创建视图语句
      */

  metaGetCreateViewSql (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetCreateViewSql"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetCreateViewSql"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.viewName !== undefined && opts.viewName !== null) {
      postBody['viewName'] = opts.viewName
    }
    if (opts.viewCommon !== undefined && opts.viewCommon !== null) {
      postBody['viewCommon'] = opts.viewCommon
    }
    if (opts.viewModel !== undefined && opts.viewModel !== null) {
      postBody['viewModel'] = opts.viewModel
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.queryStatement !== undefined && opts.queryStatement !== null) {
      postBody['queryStatement'] = opts.queryStatement
    }
    if (
      opts.metaMaterializedView !== undefined &&
      opts.metaMaterializedView !== null
    ) {
      postBody['metaMaterializedView'] = opts.metaMaterializedView
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetCreateViewSql with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetCreateViewSql',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建视图或者物化视图
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.viewName] - 视图名称  optional
      * @param {string} [opts.viewCommon] - 视图注释  optional
      * @param {string} [opts.viewModel] - 表类型：VIEW 视图，MATERIALIZED_VIEW 物化视图  optional
      * @param {string} [opts.masterPin] - 负责人  optional
      * @param {string} [opts.createViewSql] - 创建视图sql  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  是否创建成功
      */

  metaCreateView (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaCreateView"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaCreateView"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.viewName !== undefined && opts.viewName !== null) {
      postBody['viewName'] = opts.viewName
    }
    if (opts.viewCommon !== undefined && opts.viewCommon !== null) {
      postBody['viewCommon'] = opts.viewCommon
    }
    if (opts.viewModel !== undefined && opts.viewModel !== null) {
      postBody['viewModel'] = opts.viewModel
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.createViewSql !== undefined && opts.createViewSql !== null) {
      postBody['createViewSql'] = opts.createViewSql
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaCreateView with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaCreateView',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  DDL语句解析
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.sqlContent] - sql 不能为空  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metaCreateTablePartition result  返回结果对象
      */

  metaDdlAnalyze (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaDdlAnalyze"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaDdlAnalyze"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.sqlContent !== undefined && opts.sqlContent !== null) {
      postBody['sqlContent'] = opts.sqlContent
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaDdlAnalyze with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaDdlAnalyze',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表、视图详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metaTableInfoRes result  返回结果对象
      */

  metaGetTableInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTableInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTableInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTableInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTableInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表、视图结构
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metaColumnInfo result
      */

  metaGetTableColumnInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTableColumnInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTableColumnInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTableColumnInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTableColumnInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表索引
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metaIndexes result
      */

  metaGetTableMetaIndexes (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTableMetaIndexes"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTableMetaIndexes"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTableMetaIndexes with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTableMetaIndexes',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表、视图权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {metaGetTablePrivilegeReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageResMetaPrivilegeInfo result  返回结果对象
      */

  metaGetTablePrivilegeInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTablePrivilegeInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTablePrivilegeInfo"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTablePrivilegeInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTablePrivilegeInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  新增表、视图权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.metaPrivilegeInfoList] - 表权限明细  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaAddTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaAddTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaAddTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.metaPrivilegeInfoList !== undefined &&
      opts.metaPrivilegeInfoList !== null
    ) {
      postBody['metaPrivilegeInfoList'] = opts.metaPrivilegeInfoList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaAddTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaAddTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑表权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {metaPrivilegeInfo} [opts.metaPrivilegeInfo]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaUpdateTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaUpdateTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaUpdateTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.metaPrivilegeInfo !== undefined &&
      opts.metaPrivilegeInfo !== null
    ) {
      postBody['metaPrivilegeInfo'] = opts.metaPrivilegeInfo
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaUpdateTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaUpdateTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除表、视图权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {metaPrivilegeInfo} [opts.metaPrivilegeInfo]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaDelTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaDelTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaDelTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.metaPrivilegeInfo !== undefined &&
      opts.metaPrivilegeInfo !== null
    ) {
      postBody['metaPrivilegeInfo'] = opts.metaPrivilegeInfo
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaDelTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaDelTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  表详情获取建表、视图语句
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回结果
      */

  metaGetTableCreateSql (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTableCreateSql"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTableCreateSql"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTableCreateSql with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTableCreateSql',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建修改时获取建表语句
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表、视图名称  optional
      * @param {boolean} [opts.changeTableName] - 是否修改表名称  optional
      * @param {string} [opts.tableNameNew] - 表、视图名称  optional
      * @param {string} [opts.tableCommon] - 表中文名  optional
      * @param {string} [opts.tableModel] - 表类型：VIEW 视图，MATERIALIZED_VIEW 物化视图、不填：表、PRIMARY_KEYS 主键表、DUP_KEYS：明细表、AGGREGATE_KEY：聚合表  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 密码  optional
      * @param {array} [opts.metaColumnInfo] - 列信息  optional
      * @param {metaPartitionInfo} [opts.partitionInfo] - 分区信息  optional
      * @param {metaProperties} [opts.properties] - 表属性信息  optional
      * @param {metaIndexes} [opts.metaIndexes] - 索引列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回结果
      */

  metaGetCreateSql (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetCreateSql"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetCreateSql"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.changeTableName !== undefined && opts.changeTableName !== null) {
      postBody['changeTableName'] = opts.changeTableName
    }
    if (opts.tableNameNew !== undefined && opts.tableNameNew !== null) {
      postBody['tableNameNew'] = opts.tableNameNew
    }
    if (opts.tableCommon !== undefined && opts.tableCommon !== null) {
      postBody['tableCommon'] = opts.tableCommon
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }
    if (opts.metaColumnInfo !== undefined && opts.metaColumnInfo !== null) {
      postBody['metaColumnInfo'] = opts.metaColumnInfo
    }
    if (opts.partitionInfo !== undefined && opts.partitionInfo !== null) {
      postBody['partitionInfo'] = opts.partitionInfo
    }
    if (opts.properties !== undefined && opts.properties !== null) {
      postBody['properties'] = opts.properties
    }
    if (opts.metaIndexes !== undefined && opts.metaIndexes !== null) {
      postBody['metaIndexes'] = opts.metaIndexes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetCreateSql with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetCreateSql',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建修改时获取建表语句
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表、视图名称  optional
      * @param {string} [opts.tableCommon] - 表中文名  optional
      * @param {string} [opts.tableModel] - 表类型：VIEW 视图，MATERIALIZED_VIEW 物化视图、不填：表、PRIMARY_KEYS 主键表、DUP_KEYS：明细表、AGGREGATE_KEY：聚合表  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 密码  optional
      * @param {array} [opts.metaColumnInfo] - 列信息  optional
      * @param {string} [opts.properties] - 配置  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  返回结果
      */

  ricMetaGetCreateSql (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricMetaGetCreateSql"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricMetaGetCreateSql"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.tableCommon !== undefined && opts.tableCommon !== null) {
      postBody['tableCommon'] = opts.tableCommon
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }
    if (opts.metaColumnInfo !== undefined && opts.metaColumnInfo !== null) {
      postBody['metaColumnInfo'] = opts.metaColumnInfo
    }
    if (opts.properties !== undefined && opts.properties !== null) {
      postBody['properties'] = opts.properties
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricMetaGetCreateSql with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricMetaGetCreateSql',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  编辑表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表、视图名称  optional
      * @param {boolean} [opts.changeTableName] - 是否修改表名称  optional
      * @param {string} [opts.tableNameNew] - 表、视图名称  optional
      * @param {string} [opts.tableCommon] - 表中文名  optional
      * @param {string} [opts.tableModel] - 表类型：VIEW 视图，MATERIALIZED_VIEW 物化视图、不填：表、PRIMARY_KEYS 主键表、DUP_KEYS：明细表、AGGREGATE_KEY：聚合表  optional
      * @param {string} [opts.masterPin] - 负责人pin  optional
      * @param {string} [opts.masterPassWord] - 密码  optional
      * @param {array} [opts.metaColumnInfo] - 列信息  optional
      * @param {metaPartitionInfo} [opts.partitionInfo] - 分区信息  optional
      * @param {metaProperties} [opts.properties] - 表属性信息  optional
      * @param {metaIndexes} [opts.metaIndexes] - 索引列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaUpdateTable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaUpdateTable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaUpdateTable"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.changeTableName !== undefined && opts.changeTableName !== null) {
      postBody['changeTableName'] = opts.changeTableName
    }
    if (opts.tableNameNew !== undefined && opts.tableNameNew !== null) {
      postBody['tableNameNew'] = opts.tableNameNew
    }
    if (opts.tableCommon !== undefined && opts.tableCommon !== null) {
      postBody['tableCommon'] = opts.tableCommon
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }
    if (opts.metaColumnInfo !== undefined && opts.metaColumnInfo !== null) {
      postBody['metaColumnInfo'] = opts.metaColumnInfo
    }
    if (opts.partitionInfo !== undefined && opts.partitionInfo !== null) {
      postBody['partitionInfo'] = opts.partitionInfo
    }
    if (opts.properties !== undefined && opts.properties !== null) {
      postBody['properties'] = opts.properties
    }
    if (opts.metaIndexes !== undefined && opts.metaIndexes !== null) {
      postBody['metaIndexes'] = opts.metaIndexes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaUpdateTable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaUpdateTable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除表、视图
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaDelTable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaDelTable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaDelTable"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaDelTable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaDelTable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表属性
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metaProperties result  返回结果对象
      */

  metaGetTableProperties (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTableProperties"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTableProperties"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTableProperties with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTableProperties',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表分区设置
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metaPartitionInfo result  返回结果对象
      */

  metaGetTablePartitionInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTablePartitionInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTablePartitionInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTablePartitionInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTablePartitionInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  新增分区
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {array} [opts.metaPartitionsCreateList] - 分区列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaAddTablePartition (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaAddTablePartition"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaAddTablePartition"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (
      opts.metaPartitionsCreateList !== undefined &&
      opts.metaPartitionsCreateList !== null
    ) {
      postBody['metaPartitionsCreateList'] = opts.metaPartitionsCreateList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaAddTablePartition with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaAddTablePartition',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除分区
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} [opts.partitionName] - 分区名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaDelTablePartition (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaDelTablePartition"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaDelTablePartition"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.partitionName !== undefined && opts.partitionName !== null) {
      postBody['partitionName'] = opts.partitionName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaDelTablePartition with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaDelTablePartition',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表分区列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {metaGetPartitionsReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoMetaPartitions result  返回结果对象
      */

  metaGetTablePartitionList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetTablePartitionList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetTablePartitionList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetTablePartitionList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetTablePartitionList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取物化视图刷新记录
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {metaGetMaterializedViewRefreshTaskReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoMetaGetMaterializedViewRefreshTaskRes result  返回结果对象
      */

  metaGetMaterializedViewRefreshTask (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaGetMaterializedViewRefreshTask"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaGetMaterializedViewRefreshTask"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaGetMaterializedViewRefreshTask with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaGetMaterializedViewRefreshTask',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  手动刷新异步物化视图
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - database名称  optional
      * @param {string} [opts.tableName] - 视图名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  metaRefreshMaterializedView (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metaRefreshMaterializedView"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metaRefreshMaterializedView"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metaRefreshMaterializedView with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metaRefreshMaterializedView',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上报be指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目code  optional
      * @param {string} [opts.instanceId] - 实例ID  optional
      * @param {string} [opts.deployService] - 节点角色  optional
      * @param {beMetrics} [opts.beMetrics] - 指标详情  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectMetricPostBeMetrics (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricPostBeMetrics"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricPostBeMetrics"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.deployService !== undefined && opts.deployService !== null) {
      postBody['deployService'] = opts.deployService
    }
    if (opts.beMetrics !== undefined && opts.beMetrics !== null) {
      postBody['beMetrics'] = opts.beMetrics
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricPostBeMetrics with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricPostBeMetrics',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上报fe指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目code  optional
      * @param {string} [opts.instanceId] - 实例ID  optional
      * @param {string} [opts.deployService] - 节点角色  optional
      * @param {feMetrics} [opts.feMetrics] - 指标详情  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectMetricPostFeMetrics (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricPostFeMetrics"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricPostFeMetrics"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.deployService !== undefined && opts.deployService !== null) {
      postBody['deployService'] = opts.deployService
    }
    if (opts.feMetrics !== undefined && opts.feMetrics !== null) {
      postBody['feMetrics'] = opts.feMetrics
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricPostFeMetrics with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricPostFeMetrics',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上报节点状态是否存活
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目code  optional
      * @param {string} [opts.instanceId] - 实例ID  optional
      * @param {string} [opts.ip] - 实例Ip  optional
      * @param {integer} [opts.time]   optional
      * @param {string} [opts.deployService] - 节点FE\BE\CN  optional
      * @param {boolean} [opts.alive] - 是否存活  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectMetricPostAlive (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricPostAlive"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricPostAlive"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.ip !== undefined && opts.ip !== null) {
      postBody['ip'] = opts.ip
    }
    if (opts.time !== undefined && opts.time !== null) {
      postBody['time'] = opts.time
    }
    if (opts.deployService !== undefined && opts.deployService !== null) {
      postBody['deployService'] = opts.deployService
    }
    if (opts.alive !== undefined && opts.alive !== null) {
      postBody['alive'] = opts.alive
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricPostAlive with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricPostAlive',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上报需要前端展示的fe指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.time] - 时间  optional
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.instanceId] - 云主机id  optional
      * @param {integer} [opts.alive] - 是否存活  optional
      * @param {number} [opts.cpuUsage] - cpu 利用率  optional
      * @param {number} [opts.memUsage] - 内存利用率  optional
      * @param {integer} [opts.connectionTotal] - 连接总数  optional
      * @param {integer} [opts.queryTotal] - 总查询数量  optional
      * @param {integer} [opts.querySuccess] - 成功查询数量  optional
      * @param {integer} [opts.queryFail] - 失败查询数量  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectMetricPostFeMetricForFront (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricPostFeMetricForFront"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricPostFeMetricForFront"
      )
    }

    let postBody = {}
    if (opts.time !== undefined && opts.time !== null) {
      postBody['time'] = opts.time
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.alive !== undefined && opts.alive !== null) {
      postBody['alive'] = opts.alive
    }
    if (opts.cpuUsage !== undefined && opts.cpuUsage !== null) {
      postBody['cpuUsage'] = opts.cpuUsage
    }
    if (opts.memUsage !== undefined && opts.memUsage !== null) {
      postBody['memUsage'] = opts.memUsage
    }
    if (opts.connectionTotal !== undefined && opts.connectionTotal !== null) {
      postBody['connectionTotal'] = opts.connectionTotal
    }
    if (opts.queryTotal !== undefined && opts.queryTotal !== null) {
      postBody['queryTotal'] = opts.queryTotal
    }
    if (opts.querySuccess !== undefined && opts.querySuccess !== null) {
      postBody['querySuccess'] = opts.querySuccess
    }
    if (opts.queryFail !== undefined && opts.queryFail !== null) {
      postBody['queryFail'] = opts.queryFail
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricPostFeMetricForFront with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricPostFeMetricForFront',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上报需要前端展示的be指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.time] - 时间  optional
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.instanceId] - 云主机id  optional
      * @param {integer} [opts.alive] - 是否存活  optional
      * @param {number} [opts.cpuUsage] - cpu 利用率  optional
      * @param {number} [opts.memUsage] - 内存利用率  optional
      * @param {number} [opts.diskUsageTotal] - 磁盘使用量  optional
      * @param {number} [opts.diskUsageRate] - 磁盘使用率 (%)  optional
      * @param {number} [opts.ioRead] - io 读速率(MB/s)  optional
      * @param {number} [opts.ioWrite] - io 写速率(MB/s)  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectMetricPostBeMetricForFront (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricPostBeMetricForFront"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricPostBeMetricForFront"
      )
    }

    let postBody = {}
    if (opts.time !== undefined && opts.time !== null) {
      postBody['time'] = opts.time
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.alive !== undefined && opts.alive !== null) {
      postBody['alive'] = opts.alive
    }
    if (opts.cpuUsage !== undefined && opts.cpuUsage !== null) {
      postBody['cpuUsage'] = opts.cpuUsage
    }
    if (opts.memUsage !== undefined && opts.memUsage !== null) {
      postBody['memUsage'] = opts.memUsage
    }
    if (opts.diskUsageTotal !== undefined && opts.diskUsageTotal !== null) {
      postBody['diskUsageTotal'] = opts.diskUsageTotal
    }
    if (opts.diskUsageRate !== undefined && opts.diskUsageRate !== null) {
      postBody['diskUsageRate'] = opts.diskUsageRate
    }
    if (opts.ioRead !== undefined && opts.ioRead !== null) {
      postBody['ioRead'] = opts.ioRead
    }
    if (opts.ioWrite !== undefined && opts.ioWrite !== null) {
      postBody['ioWrite'] = opts.ioWrite
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricPostBeMetricForFront with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricPostBeMetricForFront',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取指标信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.metricType] - 指标类型 FE/BE/CN/QUEUE/PROJECT  optional
      * @param {string} [opts.projectCode] - SR实例code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param metricTypeResp result
      */

  projectMetricGetMetrics (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricGetMetrics"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricGetMetrics"
      )
    }

    let postBody = {}
    if (opts.metricType !== undefined && opts.metricType !== null) {
      postBody['metricType'] = opts.metricType
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricGetMetrics with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricGetMetrics',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取所有指标信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param allMetricTypeRes result
      */

  projectMetricGetAllMetrics (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricGetAllMetrics"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricGetAllMetrics"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricGetAllMetrics with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricGetAllMetrics',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取指标值
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.startTimestamp]   optional
      * @param {integer} [opts.endTimestamp]   optional
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.instanceId] - 云主机id  optional
      * @param {string} [opts.queueName] - 队列名称，如果查询队列信息必传  optional
      * @param {string} [opts.metrics] - 监控指标名  optional
      * @param {string} [opts.metricType] - 指标类型 FE/BE/CN/QUEUE/PROJECT  optional
      * @param {integer} [opts.intervalSecond] - 间隔多少秒一个数据点  optional
      * @param {boolean} [opts.isCustom] - 是否为自定义监控项  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param getFrontMetricValueResp result
      */

  projectMetricGetMetricValueForFront (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricGetMetricValueForFront"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricGetMetricValueForFront"
      )
    }

    let postBody = {}
    if (opts.startTimestamp !== undefined && opts.startTimestamp !== null) {
      postBody['startTimestamp'] = opts.startTimestamp
    }
    if (opts.endTimestamp !== undefined && opts.endTimestamp !== null) {
      postBody['endTimestamp'] = opts.endTimestamp
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.queueName !== undefined && opts.queueName !== null) {
      postBody['queueName'] = opts.queueName
    }
    if (opts.metrics !== undefined && opts.metrics !== null) {
      postBody['metrics'] = opts.metrics
    }
    if (opts.metricType !== undefined && opts.metricType !== null) {
      postBody['metricType'] = opts.metricType
    }
    if (opts.intervalSecond !== undefined && opts.intervalSecond !== null) {
      postBody['intervalSecond'] = opts.intervalSecond
    }
    if (opts.isCustom !== undefined && opts.isCustom !== null) {
      postBody['isCustom'] = opts.isCustom
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricGetMetricValueForFront with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricGetMetricValueForFront',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上报需要向用户展示的队列相关监控指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.time] - 时间  optional
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.instanceId] - 云主机id  optional
      * @param {array} [opts.queueMetricWithName] - 队列指标集合，需要一次性上报所有队列指标  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectMetricPostQueueMetricForFront (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricPostQueueMetricForFront"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricPostQueueMetricForFront"
      )
    }

    let postBody = {}
    if (opts.time !== undefined && opts.time !== null) {
      postBody['time'] = opts.time
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (
      opts.queueMetricWithName !== undefined &&
      opts.queueMetricWithName !== null
    ) {
      postBody['queueMetricWithName'] = opts.queueMetricWithName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricPostQueueMetricForFront with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricPostQueueMetricForFront',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  判断实例是否需要自动重启
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.instanceId] - 云主机id  optional
      * @param {string} [opts.deployService] - 主机角色类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectAutoStart (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectAutoStart"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectAutoStart"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.deployService !== undefined && opts.deployService !== null) {
      postBody['deployService'] = opts.deployService
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectAutoStart with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectAutoStart',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取监控指标项
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.tagKey] - tagKey  optional
      * @param {string} [opts.serviceCode] - serviceCode  optional
      * @param {string} [opts.resourceId] - tagKey  optional
      * @param {string} [opts.metric] - serviceCode  optional
      * @param {string} [opts.startTime] - tagKey  optional
      * @param {string} [opts.endTime] - serviceCode  optional
      * @param {string} [opts.timeInterval] - tagKey  optional
      * @param {array} [opts.tagFilter] - tagFilter  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectMetricTagValuesRes result
      */

  projectMetricTagValues (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricTagValues"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricTagValues"
      )
    }

    let postBody = {}
    if (opts.tagKey !== undefined && opts.tagKey !== null) {
      postBody['tagKey'] = opts.tagKey
    }
    if (opts.serviceCode !== undefined && opts.serviceCode !== null) {
      postBody['serviceCode'] = opts.serviceCode
    }
    if (opts.resourceId !== undefined && opts.resourceId !== null) {
      postBody['resourceId'] = opts.resourceId
    }
    if (opts.metric !== undefined && opts.metric !== null) {
      postBody['metric'] = opts.metric
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      postBody['startTime'] = opts.startTime
    }
    if (opts.endTime !== undefined && opts.endTime !== null) {
      postBody['endTime'] = opts.endTime
    }
    if (opts.timeInterval !== undefined && opts.timeInterval !== null) {
      postBody['timeInterval'] = opts.timeInterval
    }
    if (opts.tagFilter !== undefined && opts.tagFilter !== null) {
      postBody['tagFilter'] = opts.tagFilter
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricTagValues with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricTagValues',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取监控指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.orderBy] - orderBy  optional
      * @param {integer} [opts.topk] - topk  optional
      * @param {integer} [opts.bottomK] - bottomK  optional
      * @param {string} [opts.aggrType] - aggrType  optional
      * @param {string} [opts.downSampleType] - downSampleType  optional
      * @param {string} [opts.startTime] - startTime  optional
      * @param {string} [opts.endTime] - endTime  optional
      * @param {string} [opts.timeInterval] - timeInterval  optional
      * @param {array} [opts.tags] - tagFilter  optional
      * @param {boolean} [opts.groupBy] - groupBy  optional
      * @param {boolean} [opts.rate] - rate  optional
      * @param {integer} [opts.offset] - offset  optional
      * @param {string} [opts.serviceCode] - serviceCode  optional
      * @param {string} [opts.dimension] - dimension  optional
      * @param {string} [opts.resourceId] - resourceId  optional
      * @param {boolean} [opts.originalData] - originalData  optional
      * @param {boolean} [opts.multiResources] - multiResources  optional
      * @param {array} [opts.filters] - tagFilter  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectMetricDataRes result
      */

  projectMetricData (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricData"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricData"
      )
    }

    let postBody = {}
    if (opts.orderBy !== undefined && opts.orderBy !== null) {
      postBody['orderBy'] = opts.orderBy
    }
    if (opts.topk !== undefined && opts.topk !== null) {
      postBody['topk'] = opts.topk
    }
    if (opts.bottomK !== undefined && opts.bottomK !== null) {
      postBody['bottomK'] = opts.bottomK
    }
    if (opts.aggrType !== undefined && opts.aggrType !== null) {
      postBody['aggrType'] = opts.aggrType
    }
    if (opts.downSampleType !== undefined && opts.downSampleType !== null) {
      postBody['downSampleType'] = opts.downSampleType
    }
    if (opts.startTime !== undefined && opts.startTime !== null) {
      postBody['startTime'] = opts.startTime
    }
    if (opts.endTime !== undefined && opts.endTime !== null) {
      postBody['endTime'] = opts.endTime
    }
    if (opts.timeInterval !== undefined && opts.timeInterval !== null) {
      postBody['timeInterval'] = opts.timeInterval
    }
    if (opts.tags !== undefined && opts.tags !== null) {
      postBody['tags'] = opts.tags
    }
    if (opts.groupBy !== undefined && opts.groupBy !== null) {
      postBody['groupBy'] = opts.groupBy
    }
    if (opts.rate !== undefined && opts.rate !== null) {
      postBody['rate'] = opts.rate
    }
    if (opts.offset !== undefined && opts.offset !== null) {
      postBody['offset'] = opts.offset
    }
    if (opts.serviceCode !== undefined && opts.serviceCode !== null) {
      postBody['serviceCode'] = opts.serviceCode
    }
    if (opts.dimension !== undefined && opts.dimension !== null) {
      postBody['dimension'] = opts.dimension
    }
    if (opts.resourceId !== undefined && opts.resourceId !== null) {
      postBody['resourceId'] = opts.resourceId
    }
    if (opts.originalData !== undefined && opts.originalData !== null) {
      postBody['originalData'] = opts.originalData
    }
    if (opts.multiResources !== undefined && opts.multiResources !== null) {
      postBody['multiResources'] = opts.multiResources
    }
    if (opts.filters !== undefined && opts.filters !== null) {
      postBody['filters'] = opts.filters
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricData with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricData',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询自定义监控项
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {customMetricReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoMetricTypeResp result  返回结果对象
      */

  metricGetCustomMetricPage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  metricGetCustomMetricPage"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling metricGetCustomMetricPage"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call metricGetCustomMetricPage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/metricGetCustomMetricPage',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  SR实例添加自定义监控指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.metricItemCode] - 实例监控项code，实例自定义监控项的唯一标识  optional
      * @param {string} [opts.metricItemName] - 实例监控项名称，实例自定义监控项的名称  optional
      * @param {string} [opts.metric] - 监控项，关联自定义监控项表 custom_metric_item 的 metric 字段  optional
      * @param {string} [opts.metricRole] - 监控项所属角色  optional
      * @param {string} [opts.monitorDataQueryTpl] - 监控数据查询模板  optional
      * @param {string} [opts.alarmDataQueryTpl] - 告警数据查询模板  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  projectMetricAddCustom (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricAddCustom"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricAddCustom"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.metricItemCode !== undefined && opts.metricItemCode !== null) {
      postBody['metricItemCode'] = opts.metricItemCode
    }
    if (opts.metricItemName !== undefined && opts.metricItemName !== null) {
      postBody['metricItemName'] = opts.metricItemName
    }
    if (opts.metric !== undefined && opts.metric !== null) {
      postBody['metric'] = opts.metric
    }
    if (opts.metricRole !== undefined && opts.metricRole !== null) {
      postBody['metricRole'] = opts.metricRole
    }
    if (
      opts.monitorDataQueryTpl !== undefined &&
      opts.monitorDataQueryTpl !== null
    ) {
      postBody['monitorDataQueryTpl'] = opts.monitorDataQueryTpl
    }
    if (
      opts.alarmDataQueryTpl !== undefined &&
      opts.alarmDataQueryTpl !== null
    ) {
      postBody['alarmDataQueryTpl'] = opts.alarmDataQueryTpl
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricAddCustom with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricAddCustom',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询SR实例自定义监控指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {projectCustomMetricPageReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoProjectCustomMetricRes result  返回结果对象
      */

  projectMetricGetCustomPage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricGetCustomPage"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricGetCustomPage"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricGetCustomPage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricGetCustomPage',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除SR实例自定义监控指标
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.metricItemCode] - 实例监控项code，实例自定义监控项的唯一标识  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  projectMetricDeleteCustom (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectMetricDeleteCustom"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectMetricDeleteCustom"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.metricItemCode !== undefined && opts.metricItemCode !== null) {
      postBody['metricItemCode'] = opts.metricItemCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectMetricDeleteCustom with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectMetricDeleteCustom',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  购买产品实例
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.region] - 地域  optional
      * @param {string} [opts.az] - 可用区  optional
      * @param {string} [opts.payMethod] - 付费类型, 目前仅支持，MONTHLY 包年包月  optional
      * @param {integer} [opts.purchaseDuration] - 购买时长  optional
      * @param {boolean} [opts.autoRenewal] - 自动续费  optional
      * @param {array} [opts.bizTagInfos] - 标签  optional
      * @param {string} [opts.vpcId] - 私有网络 ID  optional
      * @param {string} [opts.subnetId] - 子网ID  optional
      * @param {string} [opts.loadBalanceId] - 负载均衡id  optional
      * @param {string} [opts.instanceType] - 实例类型，SEPARATE(存算分离)， INTEGRATED(存算一体)  optional
      * @param {string} [opts.deployType] - 开通类型：试用、标准，EXPERIMENT(试用配置)，STANDARD(正式配置)  optional
      * @param {string} [opts.version] - starRocks 版本  optional
      * @param {string} [opts.projectName] - 实例名称  optional
      * @param {vmInstanceInfo} [opts.be]   optional
      * @param {vmInstanceInfo} [opts.fe]   optional
      * @param {vmInstanceInfo} [opts.cn]   optional
      * @param {string} [opts.returnUrl] - 购买时跳转url  optional
      * @param {string} [opts.deployModel] - 部署模式：IAAS PAAS  optional
      * @param {string} [opts.adminPass] - admin账号密码，加密  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param submitOrderRes result  返回结果对象
      */

  createProduct (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  createProduct"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling createProduct"
      )
    }

    let postBody = {}
    if (opts.region !== undefined && opts.region !== null) {
      postBody['region'] = opts.region
    }
    if (opts.az !== undefined && opts.az !== null) {
      postBody['az'] = opts.az
    }
    if (opts.payMethod !== undefined && opts.payMethod !== null) {
      postBody['payMethod'] = opts.payMethod
    }
    if (opts.purchaseDuration !== undefined && opts.purchaseDuration !== null) {
      postBody['purchaseDuration'] = opts.purchaseDuration
    }
    if (opts.autoRenewal !== undefined && opts.autoRenewal !== null) {
      postBody['autoRenewal'] = opts.autoRenewal
    }
    if (opts.bizTagInfos !== undefined && opts.bizTagInfos !== null) {
      postBody['bizTagInfos'] = opts.bizTagInfos
    }
    if (opts.vpcId !== undefined && opts.vpcId !== null) {
      postBody['vpcId'] = opts.vpcId
    }
    if (opts.subnetId !== undefined && opts.subnetId !== null) {
      postBody['subnetId'] = opts.subnetId
    }
    if (opts.loadBalanceId !== undefined && opts.loadBalanceId !== null) {
      postBody['loadBalanceId'] = opts.loadBalanceId
    }
    if (opts.instanceType !== undefined && opts.instanceType !== null) {
      postBody['instanceType'] = opts.instanceType
    }
    if (opts.deployType !== undefined && opts.deployType !== null) {
      postBody['deployType'] = opts.deployType
    }
    if (opts.version !== undefined && opts.version !== null) {
      postBody['version'] = opts.version
    }
    if (opts.projectName !== undefined && opts.projectName !== null) {
      postBody['projectName'] = opts.projectName
    }
    if (opts.be !== undefined && opts.be !== null) {
      postBody['be'] = opts.be
    }
    if (opts.fe !== undefined && opts.fe !== null) {
      postBody['fe'] = opts.fe
    }
    if (opts.cn !== undefined && opts.cn !== null) {
      postBody['cn'] = opts.cn
    }
    if (opts.returnUrl !== undefined && opts.returnUrl !== null) {
      postBody['returnUrl'] = opts.returnUrl
    }
    if (opts.deployModel !== undefined && opts.deployModel !== null) {
      postBody['deployModel'] = opts.deployModel
    }
    if (opts.adminPass !== undefined && opts.adminPass !== null) {
      postBody['adminPass'] = opts.adminPass
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call createProduct with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/createProduct',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更改实例配置，扩容 BE、FE
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.nodeType] - 扩容的节点类型 FE_NODE(FE节点)，BE_NODE(BE节点), CN_NODE(CN节点)  optional
      * @param {string} [opts.az] - 可用区  optional
      * @param {integer} [opts.number] - 调整后节点数量  optional
      * @param {string} [opts.returnUrl] - 购买时跳转url  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param submitOrderRes result  返回结果对象
      */

  updateInstance (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateInstance"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling updateInstance"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.nodeType !== undefined && opts.nodeType !== null) {
      postBody['nodeType'] = opts.nodeType
    }
    if (opts.az !== undefined && opts.az !== null) {
      postBody['az'] = opts.az
    }
    if (opts.number !== undefined && opts.number !== null) {
      postBody['number'] = opts.number
    }
    if (opts.returnUrl !== undefined && opts.returnUrl !== null) {
      postBody['returnUrl'] = opts.returnUrl
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateInstance with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/updateInstance',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  指定节点缩容 FE、OBSERVER、BE、CN
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.nodeType] - 节点类型 FE_NODE(FE节点)，BE_NODE(BE节点)，CN_NODE(CN节点)，OBSERVER_NODE(OBSERVER节点)  optional
      * @param {array} [opts.instanceList] - 缩容的实例ID列表  optional
      * @param {string} [opts.returnUrl] - 购买时跳转url  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param submitOrderRes result  返回结果对象
      */

  reductionVmInstance (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  reductionVmInstance"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling reductionVmInstance"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.nodeType !== undefined && opts.nodeType !== null) {
      postBody['nodeType'] = opts.nodeType
    }
    if (opts.instanceList !== undefined && opts.instanceList !== null) {
      postBody['instanceList'] = opts.instanceList
    }
    if (opts.returnUrl !== undefined && opts.returnUrl !== null) {
      postBody['returnUrl'] = opts.returnUrl
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call reductionVmInstance with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/reductionVmInstance',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  扩缩容BE数据盘、CN缓存盘大小
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.nodeType] - 扩容的节点类型 FE，BE, CN  optional
      * @param {integer} [opts.diskSize] - 扩容后单个磁盘的容量大小  optional
      * @param {integer} [opts.diskIOPS] - be节点数据盘IPOS或cn缓存盘IOPS  optional
      * @param {string} [opts.returnUrl] - 购买时跳转url  optional
      * @param {integer} [opts.timeInterval] - 时间间隔  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param submitOrderRes result  返回结果对象
      */

  projectScaleDiskSize (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectScaleDiskSize"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectScaleDiskSize"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.nodeType !== undefined && opts.nodeType !== null) {
      postBody['nodeType'] = opts.nodeType
    }
    if (opts.diskSize !== undefined && opts.diskSize !== null) {
      postBody['diskSize'] = opts.diskSize
    }
    if (opts.diskIOPS !== undefined && opts.diskIOPS !== null) {
      postBody['diskIOPS'] = opts.diskIOPS
    }
    if (opts.returnUrl !== undefined && opts.returnUrl !== null) {
      postBody['returnUrl'] = opts.returnUrl
    }
    if (opts.timeInterval !== undefined && opts.timeInterval !== null) {
      postBody['timeInterval'] = opts.timeInterval
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectScaleDiskSize with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectScaleDiskSize',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改磁盘类型（系统盘或数据盘）。通过diskTarget参数指定变更目标，未指定时FE节点默认变更系统盘，BE/CN节点默认变更数据盘
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.nodeType] - 变更的节点类型 FE，BE, CN  optional
      * @param {string} [opts.diskTarget] - 磁盘变更目标：SYSTEM_DISK-系统盘、DATA_DISK-数据盘  optional
      * @param {string} [opts.diskType] - 变更后磁盘类型  optional
      * @param {integer} [opts.diskIops] - 变更后磁盘IOPS（仅PSSD/ESSD类型需要）  optional
      * @param {string} [opts.returnUrl] - 购买时跳转url  optional
      * @param {integer} [opts.timeInterval] - 时间间隔  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param submitOrderRes result  返回结果对象
      */

  projectChangeDiskType (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectChangeDiskType"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectChangeDiskType"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.nodeType !== undefined && opts.nodeType !== null) {
      postBody['nodeType'] = opts.nodeType
    }
    if (opts.diskTarget !== undefined && opts.diskTarget !== null) {
      postBody['diskTarget'] = opts.diskTarget
    }
    if (opts.diskType !== undefined && opts.diskType !== null) {
      postBody['diskType'] = opts.diskType
    }
    if (opts.diskIops !== undefined && opts.diskIops !== null) {
      postBody['diskIops'] = opts.diskIops
    }
    if (opts.returnUrl !== undefined && opts.returnUrl !== null) {
      postBody['returnUrl'] = opts.returnUrl
    }
    if (opts.timeInterval !== undefined && opts.timeInterval !== null) {
      postBody['timeInterval'] = opts.timeInterval
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectChangeDiskType with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectChangeDiskType',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  扩容BE数据盘/CN缓存盘/FE数据盘的个数。FE节点最多只支持1块数据盘
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.nodeType] - 扩容的节点类型 FE，BE, CN  optional
      * @param {integer} [opts.diskCount] - 扩容后单个云主机磁盘的数量（FE节点最大为1）  optional
      * @param {integer} [opts.feDiskSize] - 数据盘大小(GB)，仅FE节点需要  optional
      * @param {integer} [opts.feDiskIops] - 数据盘IOPS，仅FE节点需要  optional
      * @param {string} [opts.feDiskType] - 数据盘类型，仅FE节点需要。可选值：HDD/NSSD/PSSD/ESSD  optional
      * @param {string} [opts.returnUrl] - 购买时跳转url  optional
      * @param {integer} [opts.timeInterval] - 时间间隔  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param submitOrderRes result  返回结果对象
      */

  projectScaleDiskCount (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectScaleDiskCount"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectScaleDiskCount"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.nodeType !== undefined && opts.nodeType !== null) {
      postBody['nodeType'] = opts.nodeType
    }
    if (opts.diskCount !== undefined && opts.diskCount !== null) {
      postBody['diskCount'] = opts.diskCount
    }
    if (opts.feDiskSize !== undefined && opts.feDiskSize !== null) {
      postBody['feDiskSize'] = opts.feDiskSize
    }
    if (opts.feDiskIops !== undefined && opts.feDiskIops !== null) {
      postBody['feDiskIops'] = opts.feDiskIops
    }
    if (opts.feDiskType !== undefined && opts.feDiskType !== null) {
      postBody['feDiskType'] = opts.feDiskType
    }
    if (opts.returnUrl !== undefined && opts.returnUrl !== null) {
      postBody['returnUrl'] = opts.returnUrl
    }
    if (opts.timeInterval !== undefined && opts.timeInterval !== null) {
      postBody['timeInterval'] = opts.timeInterval
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectScaleDiskCount with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectScaleDiskCount',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  调整cu数量
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.nodeType] - 扩容的节点类型 FE，BE, CN  optional
      * @param {integer} [opts.cu] - 扩容后cu数量  optional
      * @param {string} [opts.returnUrl] - 购买时跳转url  optional
      * @param {integer} [opts.timeInterval] - 时间间隔  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param submitOrderRes result  返回结果对象
      */

  projectScaleCu (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectScaleCu"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectScaleCu"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.nodeType !== undefined && opts.nodeType !== null) {
      postBody['nodeType'] = opts.nodeType
    }
    if (opts.cu !== undefined && opts.cu !== null) {
      postBody['cu'] = opts.cu
    }
    if (opts.returnUrl !== undefined && opts.returnUrl !== null) {
      postBody['returnUrl'] = opts.returnUrl
    }
    if (opts.timeInterval !== undefined && opts.timeInterval !== null) {
      postBody['timeInterval'] = opts.timeInterval
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectScaleCu with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectScaleCu',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  校验产品是否过期
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectExpiredInfoRes result  返回结果对象
      */

  projectExpiredInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectExpiredInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectExpiredInfo"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling projectExpiredInfo"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectExpiredInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectExpiredInfo',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {searchProjectLIstReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoProjectInfoRes result  返回结果对象
      */

  getProjectList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据用户获取有权限的项目列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectName
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectListRes result
      */

  getProjectListByPin (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectListByPin"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectListByPin"
      )
    }
    if (opts.projectName === undefined || opts.projectName === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectName' when calling getProjectListByPin"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectName !== undefined && opts.projectName !== null) {
      queryParams['projectName'] = opts.projectName
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectListByPin with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectListByPin',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectInfoRes result  返回结果对象
      */

  getProjectInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectInfo"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling getProjectInfo"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectInfo',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取服务器实例节点列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {instanceInfoReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoVmInstanceInfoRes result  返回结果对象
      */

  getInstanceInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getInstanceInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getInstanceInfo"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getInstanceInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getInstanceInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取白名单
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {string} [opts.data] - 实例code，projectCode  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoWhiteInfoRes result  返回结果对象
      */

  getProjectWhite (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectWhite"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectWhite"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectWhite with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectWhite',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除白名单
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.whiteCode
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  deleteProjectWhite (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteProjectWhite"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling deleteProjectWhite"
      )
    }
    if (opts.whiteCode === undefined || opts.whiteCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.whiteCode' when calling deleteProjectWhite"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling deleteProjectWhite"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.whiteCode !== undefined && opts.whiteCode !== null) {
      queryParams['whiteCode'] = opts.whiteCode
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteProjectWhite with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/deleteProjectWhite',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加白名单
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.whiteCode] - 白名单ID  optional
      * @param {string} [opts.whiteName] - 白名单名称  optional
      * @param {string} [opts.whiteIp] - 白名单IP  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  addProjectWhite (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  addProjectWhite"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling addProjectWhite"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.whiteCode !== undefined && opts.whiteCode !== null) {
      postBody['whiteCode'] = opts.whiteCode
    }
    if (opts.whiteName !== undefined && opts.whiteName !== null) {
      postBody['whiteName'] = opts.whiteName
    }
    if (opts.whiteIp !== undefined && opts.whiteIp !== null) {
      postBody['whiteIp'] = opts.whiteIp
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call addProjectWhite with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/addProjectWhite',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取网络详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectNetworkReq result
      */

  getProjectNetWork (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectNetWork"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectNetWork"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling getProjectNetWork"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectNetWork with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectNetWork',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取网络详情V2 - 支持返回详细的域名及端口信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param projectNetworkResV2 projectNetworkResV2
      */

  getProjectNetWorkV2 (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectNetWorkV2"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectNetWorkV2"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling getProjectNetWorkV2"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectNetWorkV2 with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectNetWorkV2',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {string} [opts.data] - 实例code，projectCode  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoUserInfoRes result  返回结果对象
      */

  getProjectUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectUser"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取租户用户列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {userNameProjectCode} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoUserInfoRes result  返回结果对象
      */

  getCompanyUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getCompanyUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getCompanyUser"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getCompanyUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getCompanyUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加项目用户列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {array} [opts.userList] - 用户列表  optional
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {array} [opts.roleCodeList] - 要添加的角色code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  addProjectUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  addProjectUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling addProjectUser"
      )
    }

    let postBody = {}
    if (opts.userList !== undefined && opts.userList !== null) {
      postBody['userList'] = opts.userList
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.roleCodeList !== undefined && opts.roleCodeList !== null) {
      postBody['roleCodeList'] = opts.roleCodeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call addProjectUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/addProjectUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除项目用户列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {array} [opts.userList] - 用户列表  optional
      * @param {string} [opts.projectCode] - 项目实例code  optional
      * @param {array} [opts.roleCodeList] - 要添加的角色code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  deleteProjectUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteProjectUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling deleteProjectUser"
      )
    }

    let postBody = {}
    if (opts.userList !== undefined && opts.userList !== null) {
      postBody['userList'] = opts.userList
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.roleCodeList !== undefined && opts.roleCodeList !== null) {
      postBody['roleCodeList'] = opts.roleCodeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteProjectUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/deleteProjectUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目角色列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param userRole result
      */

  getProjectRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectRole"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling getProjectRole"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectRole',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改用户角色
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.pin] - 用户pin  optional
      * @param {array} [opts.roleList] - 角色列表  optional
      * @param {string} [opts.roleCodeList] - 角色Code列表，逗号隔开  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  updateUserRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateUserRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling updateUserRole"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.pin !== undefined && opts.pin !== null) {
      postBody['pin'] = opts.pin
    }
    if (opts.roleList !== undefined && opts.roleList !== null) {
      postBody['roleList'] = opts.roleList
    }
    if (opts.roleCodeList !== undefined && opts.roleCodeList !== null) {
      postBody['roleCodeList'] = opts.roleCodeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateUserRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/updateUserRole',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改实例名称
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.projectName] - 实例名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  updateInstanceName (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  updateInstanceName"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling updateInstanceName"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.projectName !== undefined && opts.projectName !== null) {
      postBody['projectName'] = opts.projectName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call updateInstanceName with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/updateInstanceName',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除产品实例
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  deleteProject (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteProject"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling deleteProject"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling deleteProject"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteProject with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/deleteProject',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取当前用户所有的项目和角色
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectAndRolesRes result
      */

  getProjectsAndRoles (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectsAndRoles"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectsAndRoles"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectsAndRoles with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectsAndRoles',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取当前用户的所有信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param userCompanyInfoRes result  返回结果对象
      */

  getCurrentUserInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getCurrentUserInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getCurrentUserInfo"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getCurrentUserInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getCurrentUserInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  实时计算调用接口-根据用户获取有权限的项目列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectName]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectListRes result
      */

  getProjectListByPinRic (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectListByPinRic"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectListByPinRic"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectName !== undefined && opts.projectName !== null) {
      queryParams['projectName'] = opts.projectName
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectListByPinRic with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectListByPinRic',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  实时计算调用接口-根据用户获取有权限的项目列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.companyCode] - 公司ID  optional
      * @param {string} [opts.projectName] - 项目名称  optional
      * @param {string} [opts.masterPin] - 负责人  optional
      * @param {string} [opts.masterPassWord] - 负责人密码  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectListRes result
      */

  getOwnerProjectListByPinRic (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getOwnerProjectListByPinRic"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getOwnerProjectListByPinRic"
      )
    }

    let postBody = {}
    if (opts.companyCode !== undefined && opts.companyCode !== null) {
      postBody['companyCode'] = opts.companyCode
    }
    if (opts.projectName !== undefined && opts.projectName !== null) {
      postBody['projectName'] = opts.projectName
    }
    if (opts.masterPin !== undefined && opts.masterPin !== null) {
      postBody['masterPin'] = opts.masterPin
    }
    if (opts.masterPassWord !== undefined && opts.masterPassWord !== null) {
      postBody['masterPassWord'] = opts.masterPassWord
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getOwnerProjectListByPinRic with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getOwnerProjectListByPinRic',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  实时计算调用接口-获取Fe节点信息-1
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param feNodeInfoRes result
      */

  getFeNodeInfoResRic (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getFeNodeInfoResRic"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getFeNodeInfoResRic"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getFeNodeInfoResRic with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getFeNodeInfoResRic',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  外部组件调用接口-获取SR集群连接信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param srConnectionInfoRes result  返回结果对象
      */

  getSrConnectionInfoRic (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getSrConnectionInfoRic"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getSrConnectionInfoRic"
      )
    }

    let postBody = null
    let queryParams = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      queryParams['projectCode'] = opts.projectCode
    }

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getSrConnectionInfoRic with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getSrConnectionInfoRic',
      'GET',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  启用产品实例
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  startInstance (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  startInstance"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling startInstance"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call startInstance with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/startInstance',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  重启启产品实例
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  restartInstance (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  restartInstance"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling restartInstance"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call restartInstance with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/restartInstance',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  重启实例节点
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.vmInstanceCode] - 重启的实例节点ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  restartInstanceNode (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  restartInstanceNode"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling restartInstanceNode"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.vmInstanceCode !== undefined && opts.vmInstanceCode !== null) {
      postBody['vmInstanceCode'] = opts.vmInstanceCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call restartInstanceNode with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/restartInstanceNode',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  下线实例节点
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.vmInstanceCode] - 重启的实例节点ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  dropInstanceNode (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  dropInstanceNode"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling dropInstanceNode"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.vmInstanceCode !== undefined && opts.vmInstanceCode !== null) {
      postBody['vmInstanceCode'] = opts.vmInstanceCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call dropInstanceNode with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/dropInstanceNode',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  上线实例节点
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.vmInstanceCode] - 重启的实例节点ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  addInstanceNode (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  addInstanceNode"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling addInstanceNode"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.vmInstanceCode !== undefined && opts.vmInstanceCode !== null) {
      postBody['vmInstanceCode'] = opts.vmInstanceCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call addInstanceNode with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/addInstanceNode',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  重启实例，支持可选的重启原因参数，未传时保持向后兼容
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} [opts.nodeType] - 节点类型 FE、BE、CN、ALL  optional
      * @param {integer} [opts.timeInterval] - 时间间隔  optional
      * @param {string} [opts.restartReason] - 重启原因，可选，兼容旧调用；枚举值 BIND_EXTERNAL_ENGIN、OTHER  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  projectRestart (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectRestart"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectRestart"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.nodeType !== undefined && opts.nodeType !== null) {
      postBody['nodeType'] = opts.nodeType
    }
    if (opts.timeInterval !== undefined && opts.timeInterval !== null) {
      postBody['timeInterval'] = opts.timeInterval
    }
    if (opts.restartReason !== undefined && opts.restartReason !== null) {
      postBody['restartReason'] = opts.restartReason
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectRestart with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectRestart',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停止产品实例
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例Code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  stopInstance (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  stopInstance"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling stopInstance"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call stopInstance with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/stopInstance',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取core-site文件
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  操作结果
      */

  enablePublicIp (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  enablePublicIp"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling enablePublicIp"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call enablePublicIp with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/enablePublicIp',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  禁用公网访问
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  操作结果
      */

  disablePublicIp (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  disablePublicIp"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling disablePublicIp"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call disablePublicIp with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/disablePublicIp',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取实例类型和最大副本数量
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param typeAndMaxRepRes result
      */

  getTypeAndMaxReplicationReq (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getTypeAndMaxReplicationReq"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getTypeAndMaxReplicationReq"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getTypeAndMaxReplicationReq with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getTypeAndMaxReplication',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取云主机id列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.deployService] - 服务器部署的服务：FE(FE节点)、BE(BE节点)、CN(CN节点)  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result
      */

  projectGetVmIdList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectGetVmIdList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectGetVmIdList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.deployService !== undefined && opts.deployService !== null) {
      postBody['deployService'] = opts.deployService
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectGetVmIdList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectGetVmIdList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取部署starrocks版本
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.instanceType] - 实例类型：SEPARATE(存算分离)，INTEGRATED(存算一体)，INTEGRATED_VM(存算一体纯享版)  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result
      */

  getStarRocksVersions (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getStarRocksVersions"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getStarRocksVersions"
      )
    }

    let postBody = {}
    if (opts.instanceType !== undefined && opts.instanceType !== null) {
      postBody['instanceType'] = opts.instanceType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getStarRocksVersions with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getStarRocksVersions',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  处理到期状态
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.pin] - 用户pin  optional
      * @param {string} [opts.region] - 地域  optional
      * @param {string} [opts.resourceId] - 资源id  optional
      * @param {string} [opts.serviceCode] - serviceCode  optional
      * @param {string} [opts.status] - 状态  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  billingStatusOnMessage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  billingStatusOnMessage"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling billingStatusOnMessage"
      )
    }

    let postBody = {}
    if (opts.pin !== undefined && opts.pin !== null) {
      postBody['pin'] = opts.pin
    }
    if (opts.region !== undefined && opts.region !== null) {
      postBody['region'] = opts.region
    }
    if (opts.resourceId !== undefined && opts.resourceId !== null) {
      postBody['resourceId'] = opts.resourceId
    }
    if (opts.serviceCode !== undefined && opts.serviceCode !== null) {
      postBody['serviceCode'] = opts.serviceCode
    }
    if (opts.status !== undefined && opts.status !== null) {
      postBody['status'] = opts.status
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call billingStatusOnMessage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/billingStatusOnMessage',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  订单主动取消或者超期被动取消
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.pin] - 用户pin  optional
      * @param {string} [opts.region] - 地域  optional
      * @param {string} [opts.orderNumber] - 订单好吗  optional
      * @param {string} [opts.serviceCodes] - 产品代码  optional
      * @param {integer} [opts.activityType] - 产品代码  optional
      * @param {string} [opts.success] - 是否成功  optional
      * @param {string} [opts.sourceId] - 源ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  cancelBillingOnMessage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  cancelBillingOnMessage"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling cancelBillingOnMessage"
      )
    }

    let postBody = {}
    if (opts.pin !== undefined && opts.pin !== null) {
      postBody['pin'] = opts.pin
    }
    if (opts.region !== undefined && opts.region !== null) {
      postBody['region'] = opts.region
    }
    if (opts.orderNumber !== undefined && opts.orderNumber !== null) {
      postBody['orderNumber'] = opts.orderNumber
    }
    if (opts.serviceCodes !== undefined && opts.serviceCodes !== null) {
      postBody['serviceCodes'] = opts.serviceCodes
    }
    if (opts.activityType !== undefined && opts.activityType !== null) {
      postBody['activityType'] = opts.activityType
    }
    if (opts.success !== undefined && opts.success !== null) {
      postBody['success'] = opts.success
    }
    if (opts.sourceId !== undefined && opts.sourceId !== null) {
      postBody['sourceId'] = opts.sourceId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call cancelBillingOnMessage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/cancelBillingOnMessage',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  接收付费结果
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.message] - 消息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  payResultOnMessage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  payResultOnMessage"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling payResultOnMessage"
      )
    }

    let postBody = {}
    if (opts.message !== undefined && opts.message !== null) {
      postBody['message'] = opts.message
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call payResultOnMessage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/payResultOnMessage',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  纯享模式转托管模式
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} [opts.user] - 实例ID  optional
      * @param {string} [opts.passwd] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  projectVmToIntegratedReq (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectVmToIntegratedReq"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectVmToIntegratedReq"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.user !== undefined && opts.user !== null) {
      postBody['user'] = opts.user
    }
    if (opts.passwd !== undefined && opts.passwd !== null) {
      postBody['passwd'] = opts.passwd
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectVmToIntegratedReq with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectVmToIntegrated',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  发送计费kafka消息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.deployResultTopic] - topic名称  optional
      * @param {string} [opts.deployResultMsgList] - 发送消息  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  sendBillingMessage (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  sendBillingMessage"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling sendBillingMessage"
      )
    }

    let postBody = {}
    if (
      opts.deployResultTopic !== undefined &&
      opts.deployResultTopic !== null
    ) {
      postBody['deployResultTopic'] = opts.deployResultTopic
    }
    if (
      opts.deployResultMsgList !== undefined &&
      opts.deployResultMsgList !== null
    ) {
      postBody['deployResultMsgList'] = opts.deployResultMsgList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call sendBillingMessage with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/sendBillingMessage',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取SR实例域名详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectDomainGetDetailRes result  返回结果对象
      */

  projectDomainGetDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectDomainGetDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectDomainGetDetail"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectDomainGetDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectDomainGetDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  更新域名信息
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.hostName] - 主机记录名称  optional
      * @param {string} [opts.domainPurpose] - 域名用途  optional
      * @param {string} [opts.domainScope] - 域名作用域  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  projectDomainUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectDomainUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectDomainUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.hostName !== undefined && opts.hostName !== null) {
      postBody['hostName'] = opts.hostName
    }
    if (opts.domainPurpose !== undefined && opts.domainPurpose !== null) {
      postBody['domainPurpose'] = opts.domainPurpose
    }
    if (opts.domainScope !== undefined && opts.domainScope !== null) {
      postBody['domainScope'] = opts.domainScope
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectDomainUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectDomainUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  绑定对象存储桶
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.ossBucketName] - 对象存储桶名称  optional
      * @param {string} [opts.ossBucketRegionId] - 对象存储桶地域id  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  projectBindOssBucket (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectBindOssBucket"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectBindOssBucket"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.ossBucketName !== undefined && opts.ossBucketName !== null) {
      postBody['ossBucketName'] = opts.ossBucketName
    }
    if (
      opts.ossBucketRegionId !== undefined &&
      opts.ossBucketRegionId !== null
    ) {
      postBody['ossBucketRegionId'] = opts.ossBucketRegionId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectBindOssBucket with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectBindOssBucket',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  解绑对象存储桶
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.ossBucketName] - 对象存储桶名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  projectUnbindOssBucket (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectUnbindOssBucket"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectUnbindOssBucket"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.ossBucketName !== undefined && opts.ossBucketName !== null) {
      postBody['ossBucketName'] = opts.ossBucketName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectUnbindOssBucket with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectUnbindOssBucket',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询实例绑定的对象存储桶信息，如果没有绑定，则返回空
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.ossBucketName] - 对象存储桶名称  optional
      * @param {string} [opts.ossBucketRegionId] - 对象存储桶地域id  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param projectGetBoundOssBucketRes result  返回结果对象
      */

  projectGetBoundOssBucket (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  projectGetBoundOssBucket"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling projectGetBoundOssBucket"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.ossBucketName !== undefined && opts.ossBucketName !== null) {
      postBody['ossBucketName'] = opts.ossBucketName
    }
    if (
      opts.ossBucketRegionId !== undefined &&
      opts.ossBucketRegionId !== null
    ) {
      postBody['ossBucketRegionId'] = opts.ossBucketRegionId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call projectGetBoundOssBucket with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/projectGetBoundOssBucket',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询表存储统计信息，默认查询BASE TABLE类型的表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {tableStorageStatsReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoTableStorageStatsRes result  返回结果对象
      */

  storageStatsGetTableList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageStatsGetTableList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageStatsGetTableList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageStatsGetTableList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageStatsGetTableList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询实例存储量库表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目编码，必填  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param storageStatsGetProjectStatsRes result  返回结果对象
      */

  storageStatsGetProjectStats (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageStatsGetProjectStats"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageStatsGetProjectStats"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageStatsGetProjectStats with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageStatsGetProjectStats',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目库权限用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {userProjectUserByDbReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param userProjectUserResPageRespVo result  返回结果对象
      */

  getProjectUserByDB (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectUserByDB"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectUserByDB"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectUserByDB with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectUserByDB',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目表权限用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {userProjectUserByTableReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param userProjectUserResPageRespVo result  返回结果对象
      */

  getProjectUserByTable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectUserByTable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectUserByTable"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectUserByTable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectUserByTable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目库角色列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.roleName] - 角色名  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param userProjectRoleRes result
      */

  getProjectRoleByDb (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectRoleByDb"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectRoleByDb"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.roleName !== undefined && opts.roleName !== null) {
      postBody['roleName'] = opts.roleName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectRoleByDb with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectRoleByDb',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取项目表角色列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalog] - catalog名称  optional
      * @param {string} [opts.database] - 数据库名称  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} [opts.roleName] - 角色名  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param userProjectRoleRes result
      */

  getProjectRoleByTable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getProjectRoleByTable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getProjectRoleByTable"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalog !== undefined && opts.catalog !== null) {
      postBody['catalog'] = opts.catalog
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.roleName !== undefined && opts.roleName !== null) {
      postBody['roleName'] = opts.roleName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getProjectRoleByTable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getProjectRoleByTable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取实例用户名密码
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode]   optional
      * @param {string} [opts.userName]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param ricGetProjectUserNamePwdRes result
      */

  ricGetProjectUserNamePwd (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  ricGetProjectUserNamePwd"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling ricGetProjectUserNamePwd"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userName !== undefined && opts.userName !== null) {
      postBody['userName'] = opts.userName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call ricGetProjectUserNamePwd with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/ricricGetProjectUserNamePwd',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取用户类型，对内、普通用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  用户类型：general（外部用户），jd（内部用户）
      */

  getUserResourceType (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getUserResourceType"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getUserResourceType"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getUserResourceType with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getUserResourceType',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取用户地域
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  地域，regionId
      */

  getUserRegionId (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getUserRegionId"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getUserRegionId"
      )
    }

    let postBody = {}

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getUserRegionId with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getUserRegionId',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  全部查询
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {queryAllQueryReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param queryAllQueryResPageRespVo result  返回结果对象
      */

  querySlowQuery (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  querySlowQuery"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling querySlowQuery"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call querySlowQuery with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/querySlowQuery',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  活跃查询
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {queryActiveQueryReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param queryAllQueryResPageRespVo result  返回结果对象
      */

  activeQuery (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  activeQuery"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling activeQuery"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call activeQuery with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/activeQuery',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  全部查询详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.queryId] - 查询ID  optional
      * @param {string} [opts.instanceId] - instanceId  optional
      * @param {string} [opts.connectionId] - connectionId  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param queryQueryInfoRes result  返回结果对象
      */

  queryQueryInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  queryQueryInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling queryQueryInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.queryId !== undefined && opts.queryId !== null) {
      postBody['queryId'] = opts.queryId
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.connectionId !== undefined && opts.connectionId !== null) {
      postBody['connectionId'] = opts.connectionId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call queryQueryInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/queryQueryInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  查询停止
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.queryId] - 查询ID  optional
      * @param {string} [opts.instanceId] - instanceId  optional
      * @param {string} [opts.connectionId] - connectionId  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  queryQueryStop (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  queryQueryStop"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling queryQueryStop"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.queryId !== undefined && opts.queryId !== null) {
      postBody['queryId'] = opts.queryId
    }
    if (opts.instanceId !== undefined && opts.instanceId !== null) {
      postBody['instanceId'] = opts.instanceId
    }
    if (opts.connectionId !== undefined && opts.connectionId !== null) {
      postBody['connectionId'] = opts.connectionId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call queryQueryStop with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/queryQueryStop',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  资源组列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {resourceGroupListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageResourceGroupListRes result  返回结果对象
      */

  resourceGroupList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGroupList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGroupList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGroupList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGroupList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建资源组
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.resourceGroupName] - 资源队列名称  optional
      * @param {string} [opts.resourceGroupDesc] - 资源队列描述  optional
      * @param {string} [opts.masterUserPin] - 负责人Pin  optional
      * @param {integer} [opts.cpuCoreLimit] - cpu使用上线  optional
      * @param {integer} [opts.memLimit] - memLimit  optional
      * @param {integer} [opts.bigQueryCpuSecondLimit] - 大查询任务可以使用 CPU 的时间上限  optional
      * @param {integer} [opts.bigQueryMemLimit] - 大查询任务可以使用的内存上限  optional
      * @param {integer} [opts.bigQueryScanRowsLimit] - 大查询任务可以扫描的行数上限  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  resourceGroupCreate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGroupCreate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGroupCreate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.resourceGroupName !== undefined &&
      opts.resourceGroupName !== null
    ) {
      postBody['resourceGroupName'] = opts.resourceGroupName
    }
    if (
      opts.resourceGroupDesc !== undefined &&
      opts.resourceGroupDesc !== null
    ) {
      postBody['resourceGroupDesc'] = opts.resourceGroupDesc
    }
    if (opts.masterUserPin !== undefined && opts.masterUserPin !== null) {
      postBody['masterUserPin'] = opts.masterUserPin
    }
    if (opts.cpuCoreLimit !== undefined && opts.cpuCoreLimit !== null) {
      postBody['cpuCoreLimit'] = opts.cpuCoreLimit
    }
    if (opts.memLimit !== undefined && opts.memLimit !== null) {
      postBody['memLimit'] = opts.memLimit
    }
    if (
      opts.bigQueryCpuSecondLimit !== undefined &&
      opts.bigQueryCpuSecondLimit !== null
    ) {
      postBody['bigQueryCpuSecondLimit'] = opts.bigQueryCpuSecondLimit
    }
    if (opts.bigQueryMemLimit !== undefined && opts.bigQueryMemLimit !== null) {
      postBody['bigQueryMemLimit'] = opts.bigQueryMemLimit
    }
    if (
      opts.bigQueryScanRowsLimit !== undefined &&
      opts.bigQueryScanRowsLimit !== null
    ) {
      postBody['bigQueryScanRowsLimit'] = opts.bigQueryScanRowsLimit
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGroupCreate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGroupCreate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  资源组修改
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.resourceGroupName] - 资源队列名称  optional
      * @param {string} [opts.resourceGroupDesc] - 资源队列描述  optional
      * @param {string} [opts.masterUserPin] - 负责人Pin  optional
      * @param {integer} [opts.cpuCoreLimit] - cpu使用上线  optional
      * @param {integer} [opts.memLimit] - memLimit  optional
      * @param {integer} [opts.bigQueryCpuSecondLimit] - 大查询任务可以使用 CPU 的时间上限  optional
      * @param {integer} [opts.bigQueryMemLimit] - 大查询任务可以使用的内存上限  optional
      * @param {integer} [opts.bigQueryScanRowsLimit] - 大查询任务可以扫描的行数上限  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  resourceGroupUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGroupUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGroupUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.resourceGroupName !== undefined &&
      opts.resourceGroupName !== null
    ) {
      postBody['resourceGroupName'] = opts.resourceGroupName
    }
    if (
      opts.resourceGroupDesc !== undefined &&
      opts.resourceGroupDesc !== null
    ) {
      postBody['resourceGroupDesc'] = opts.resourceGroupDesc
    }
    if (opts.masterUserPin !== undefined && opts.masterUserPin !== null) {
      postBody['masterUserPin'] = opts.masterUserPin
    }
    if (opts.cpuCoreLimit !== undefined && opts.cpuCoreLimit !== null) {
      postBody['cpuCoreLimit'] = opts.cpuCoreLimit
    }
    if (opts.memLimit !== undefined && opts.memLimit !== null) {
      postBody['memLimit'] = opts.memLimit
    }
    if (
      opts.bigQueryCpuSecondLimit !== undefined &&
      opts.bigQueryCpuSecondLimit !== null
    ) {
      postBody['bigQueryCpuSecondLimit'] = opts.bigQueryCpuSecondLimit
    }
    if (opts.bigQueryMemLimit !== undefined && opts.bigQueryMemLimit !== null) {
      postBody['bigQueryMemLimit'] = opts.bigQueryMemLimit
    }
    if (
      opts.bigQueryScanRowsLimit !== undefined &&
      opts.bigQueryScanRowsLimit !== null
    ) {
      postBody['bigQueryScanRowsLimit'] = opts.bigQueryScanRowsLimit
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGroupUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGroupUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  资源组权限列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.resourceGroupName] - 资源队列名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param resourceGroupPrivilegeListRes result  返回结果对象
      */

  resourceGroupPrivilegeList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGroupPrivilegeList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGroupPrivilegeList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.resourceGroupName !== undefined &&
      opts.resourceGroupName !== null
    ) {
      postBody['resourceGroupName'] = opts.resourceGroupName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGroupPrivilegeList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGroupPrivilegeList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  资源组权限修改
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.resourceGroupName] - 资源队列名称  optional
      * @param {string} [opts.masterUserPin] - 负责人Pin  optional
      * @param {string} [opts.masterUserName] - 负责人名称  optional
      * @param {array} [opts.userPinList]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  resourceGroupPrivilegeUpdate (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGroupPrivilegeUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGroupPrivilegeUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.resourceGroupName !== undefined &&
      opts.resourceGroupName !== null
    ) {
      postBody['resourceGroupName'] = opts.resourceGroupName
    }
    if (opts.masterUserPin !== undefined && opts.masterUserPin !== null) {
      postBody['masterUserPin'] = opts.masterUserPin
    }
    if (opts.masterUserName !== undefined && opts.masterUserName !== null) {
      postBody['masterUserName'] = opts.masterUserName
    }
    if (opts.userPinList !== undefined && opts.userPinList !== null) {
      postBody['userPinList'] = opts.userPinList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGroupPrivilegeUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGroupPrivilegeUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  资源组权限删除
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.resourceGroupName] - 资源队列名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果对象
      */

  resourceGroupDrop (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGroupDrop"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGroupDrop"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.resourceGroupName !== undefined &&
      opts.resourceGroupName !== null
    ) {
      postBody['resourceGroupName'] = opts.resourceGroupName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGroupDrop with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGroupDrop',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  资源组剩余资源量
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目实例ID  optional
      * @param {string} [opts.resourceGroupName] - 资源队列名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param resourceGroupAvailableRes result  返回结果对象
      */

  resourceGroupAvailable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGroupAvailable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGroupAvailable"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.resourceGroupName !== undefined &&
      opts.resourceGroupName !== null
    ) {
      postBody['resourceGroupName'] = opts.resourceGroupName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGroupAvailable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGroupAvailable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取资源组名称列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result
      */

  resourceGetResourceNameList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  resourceGetResourceNameList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling resourceGetResourceNameList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call resourceGetResourceNameList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/resourceGetResourceNameList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取角色列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {roleGetListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoRoleGetListRes result  返回结果对象
      */

  roleGetList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleGetList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleGetList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleGetList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleGetList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建角色
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} [opts.roleDes] - 角色描述  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleCreate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleCreate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleCreate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.roleDes !== undefined && opts.roleDes !== null) {
      postBody['roleDes'] = opts.roleDes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleCreate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleCreate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改角色
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} [opts.roleDes] - 角色描述  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.roleDes !== undefined && opts.roleDes !== null) {
      postBody['roleDes'] = opts.roleDes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取角色详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} [opts.roleDes] - 角色描述  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param roleGetInfoRes result  返回结果对象
      */

  roleGetRoleInfo (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleGetRoleInfo"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleGetRoleInfo"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.roleDes !== undefined && opts.roleDes !== null) {
      postBody['roleDes'] = opts.roleDes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleGetRoleInfo with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleGetRoleInfo',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取角色表、视图、物化视图权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {roleGetTablePrivilegeReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoRoleGetTablePrivilegeRes result  返回结果对象
      */

  roleGetTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleGetTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleGetTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleGetTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleGetTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取库列表,role 为 null 返回所有库，role 不为 null 返回该角色下没有赋权的库
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.database] - 库名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param string result  数据或者是分页
      */

  roleGetDBList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleGetDBList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleGetDBList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleGetDBList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleGetDBList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取表列表,role 为 null 返回所有表，role 不为 null 返回该角色下没有赋权的表。
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} [opts.database] - 库名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param roleGetTableRes result
      */

  roleGetTableList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleGetTableList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleGetTableList"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleGetTableList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleGetTableList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加表、视图、物化视图权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {array} [opts.roleTablePrivilegeList] - 权限明细  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleAddTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleAddTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleAddTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (
      opts.roleTablePrivilegeList !== undefined &&
      opts.roleTablePrivilegeList !== null
    ) {
      postBody['roleTablePrivilegeList'] = opts.roleTablePrivilegeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleAddTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleAddTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改表、视图、物化视图权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.id] - id  optional
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.metaTablePrivilegeType] - 库权限类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleUpdateTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleUpdateTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleUpdateTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.id !== undefined && opts.id !== null) {
      postBody['id'] = opts.id
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.metaTablePrivilegeType !== undefined &&
      opts.metaTablePrivilegeType !== null
    ) {
      postBody['metaTablePrivilegeType'] = opts.metaTablePrivilegeType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleUpdateTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleUpdateTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改表、视图、物化视图权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.role] - 角色名  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.database] - 库名  optional
      * @param {string} [opts.tableName] - 表名  optional
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.metaTablePrivilegeType] - 库权限类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleModifyTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleModifyTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleModifyTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.metaTablePrivilegeType !== undefined &&
      opts.metaTablePrivilegeType !== null
    ) {
      postBody['metaTablePrivilegeType'] = opts.metaTablePrivilegeType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleModifyTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleModifyTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取角色库权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {roleGetDBPrivilegeReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoRoleGetDBPrivilegeRes result  返回结果对象
      */

  roleGetDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleGetDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleGetDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleGetDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleGetDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加库权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {array} [opts.roleDBPrivilegeList] - 权限明细  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleAddDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleAddDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleAddDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (
      opts.roleDBPrivilegeList !== undefined &&
      opts.roleDBPrivilegeList !== null
    ) {
      postBody['roleDBPrivilegeList'] = opts.roleDBPrivilegeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleAddDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleAddDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改库权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.id] - id  optional
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {array} [opts.metaDBPrivilegeType] - 库权限类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleUpdateDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleUpdateDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleUpdateDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.id !== undefined && opts.id !== null) {
      postBody['id'] = opts.id
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.metaDBPrivilegeType !== undefined &&
      opts.metaDBPrivilegeType !== null
    ) {
      postBody['metaDBPrivilegeType'] = opts.metaDBPrivilegeType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleUpdateDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleUpdateDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改库权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.database] - 库名称  optional
      * @param {string} [opts.role] - 角色名  optional
      * @param {array} [opts.metaDBPrivilegeType] - 库权限类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleModifyDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleModifyDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleModifyDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (
      opts.metaDBPrivilegeType !== undefined &&
      opts.metaDBPrivilegeType !== null
    ) {
      postBody['metaDBPrivilegeType'] = opts.metaDBPrivilegeType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleModifyDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleModifyDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除角色库权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {array} [opts.roleDBPrivilegeList] - 权限明细  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleDelDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleDelDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleDelDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (
      opts.roleDBPrivilegeList !== undefined &&
      opts.roleDBPrivilegeList !== null
    ) {
      postBody['roleDBPrivilegeList'] = opts.roleDBPrivilegeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleDelDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleDelDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除角色表权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {array} [opts.roleTablePrivilegeList] - 权限明细  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleDelTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleDelTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleDelTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (
      opts.roleTablePrivilegeList !== undefined &&
      opts.roleTablePrivilegeList !== null
    ) {
      postBody['roleTablePrivilegeList'] = opts.roleTablePrivilegeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleDelTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleDelTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  角色成员列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {roleGetUserReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoRoleGetUserRes result  返回结果对象
      */

  roleGetUserList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleGetUserList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleGetUserList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleGetUserList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleGetUserList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  角色添加成员
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {array} [opts.userPinList] - 用户pin  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleAddUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleAddUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleAddUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.userPinList !== undefined && opts.userPinList !== null) {
      postBody['userPinList'] = opts.userPinList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleAddUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleAddUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  角色移除成员
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {array} [opts.userPinList] - 用户pin  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleDelUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleDelUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleDelUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }
    if (opts.userPinList !== undefined && opts.userPinList !== null) {
      postBody['userPinList'] = opts.userPinList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleDelUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleDelUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  角色删除
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.role] - 角色名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  roleDel (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  roleDel"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling roleDel"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call roleDel with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/roleDel',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据角色获取可添加的表列表，并标记上是否已添加
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.dbName] - 库名  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {string} [opts.role] - 角色  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param getAllTablesByUserRes result
      */

  getAllTablesByRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllTablesByRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllTablesByRole"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllTablesByRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllTablesByRole',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据角色获取可添加的库列表，并标记上是否已添加
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.role] - role  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param getAllDBByUserRes result
      */

  getAllDBByRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllDBByRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllDBByRole"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllDBByRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllDBByRole',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据角色获取可添加的用户列表，并标记上是否已添加
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.role] - role  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param getRoleAddableUserRes result
      */

  getAllUserByRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllUserByRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllUserByRole"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.role !== undefined && opts.role !== null) {
      postBody['role'] = opts.role
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllUserByRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllUserByRole',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  创建实例的自定义用户，独立于IAM同名用户之外，这个用户和实例操作无关，只是一个starRocks数据库的用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode - 实例code
      * @param {string} opts.userName - 用户名
      * @param {string} opts.passWord - 密码
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  addCustomStarRocksUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  addCustomStarRocksUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling addCustomStarRocksUser"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling addCustomStarRocksUser"
      )
    }
    if (opts.userName === undefined || opts.userName === null) {
      throw new Error(
        "Missing the required parameter 'opts.userName' when calling addCustomStarRocksUser"
      )
    }
    if (opts.passWord === undefined || opts.passWord === null) {
      throw new Error(
        "Missing the required parameter 'opts.passWord' when calling addCustomStarRocksUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userName !== undefined && opts.userName !== null) {
      postBody['userName'] = opts.userName
    }
    if (opts.passWord !== undefined && opts.passWord !== null) {
      postBody['passWord'] = opts.passWord
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call addCustomStarRocksUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/addCustomStarRocksUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取自定义实例用户列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {string} [opts.data] - 查询数据  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoCustomUserListRes result  返回结果对象
      */

  listCustomStarRocksUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  listCustomStarRocksUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling listCustomStarRocksUser"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call listCustomStarRocksUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/listCustomStarRocksUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改自定义用户密码
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode - 实例code
      * @param {string} opts.userName - 用户名
      * @param {string} opts.passWord - 密码
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  changeCustomStarRocksUserPassWord (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  changeCustomStarRocksUserPassWord"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling changeCustomStarRocksUserPassWord"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling changeCustomStarRocksUserPassWord"
      )
    }
    if (opts.userName === undefined || opts.userName === null) {
      throw new Error(
        "Missing the required parameter 'opts.userName' when calling changeCustomStarRocksUserPassWord"
      )
    }
    if (opts.passWord === undefined || opts.passWord === null) {
      throw new Error(
        "Missing the required parameter 'opts.passWord' when calling changeCustomStarRocksUserPassWord"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userName !== undefined && opts.userName !== null) {
      postBody['userName'] = opts.userName
    }
    if (opts.passWord !== undefined && opts.passWord !== null) {
      postBody['passWord'] = opts.passWord
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call changeCustomStarRocksUserPassWord with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/changeCustomStarRocksUserPassWord',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除自定义用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode - 实例code
      * @param {string} opts.userName - 用户名
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  deleteCustomStarRocksUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  deleteCustomStarRocksUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling deleteCustomStarRocksUser"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling deleteCustomStarRocksUser"
      )
    }
    if (opts.userName === undefined || opts.userName === null) {
      throw new Error(
        "Missing the required parameter 'opts.userName' when calling deleteCustomStarRocksUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userName !== undefined && opts.userName !== null) {
      postBody['userName'] = opts.userName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call deleteCustomStarRocksUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/deleteCustomStarRocksUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取所有的starRocks用户权限列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {getStarRocksUserReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoStarRocksUserResp result  返回结果
      */

  getAllStarRocksUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllStarRocksUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllStarRocksUser"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllStarRocksUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllStarRocksUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取starRocks所有用户列表，不包括权限数量
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {string} [opts.data] - 查询数据  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoAllStarRocksUserNameAndPinResp result  返回结果
      */

  getAllStarRocksUserNameAndPin (
    opts,
    regionId = this.config.regionId,
    callback
  ) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllStarRocksUserNameAndPin"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllStarRocksUserNameAndPin"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllStarRocksUserNameAndPin with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllStarRocksUserNameAndPin',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取库权限列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {userGetDBPrivilegeReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoUserDBPrivilegeResp result  返回结果
      */

  userGetDbPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userGetDbPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userGetDbPrivilege"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userGetDbPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userGetDbPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  用户添加库权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} opts.projectCode - 实例ID
      * @param {string} opts.userPin - 用户pin
      * @param {array} [opts.dbprivilegeList]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userAddDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userAddDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userAddDBPrivilege"
      )
    }
    if (opts.projectCode === undefined || opts.projectCode === null) {
      throw new Error(
        "Missing the required parameter 'opts.projectCode' when calling userAddDBPrivilege"
      )
    }
    if (opts.userPin === undefined || opts.userPin === null) {
      throw new Error(
        "Missing the required parameter 'opts.userPin' when calling userAddDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.dbprivilegeList !== undefined && opts.dbprivilegeList !== null) {
      postBody['dbprivilegeList'] = opts.dbprivilegeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userAddDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userAddDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userUpdateDBPrivilege
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.database] - 库名  optional
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {array} [opts.metaDBPrivilegeType] - 库权限类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userUpdateDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userUpdateDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userUpdateDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.database !== undefined && opts.database !== null) {
      postBody['database'] = opts.database
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (
      opts.metaDBPrivilegeType !== undefined &&
      opts.metaDBPrivilegeType !== null
    ) {
      postBody['metaDBPrivilegeType'] = opts.metaDBPrivilegeType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userUpdateDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userUpdateDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userDelDBPrivilege
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {array} [opts.dbList] - 库名列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userDelDBPrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userDelDBPrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userDelDBPrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.dbList !== undefined && opts.dbList !== null) {
      postBody['dbList'] = opts.dbList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userDelDBPrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userDelDBPrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userGetTablePrivilege
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {userGetTablePrivilegeReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoUserGetTablePrivilegeRes result  返回结果
      */

  userGetTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userGetTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userGetTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userGetTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userGetTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userAddTablePrivilege
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {string} [opts.tableModel] - 表类型  optional
      * @param {array} [opts.tablePrivilegeList] - 权限明细  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userAddTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userAddTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userAddTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (
      opts.tablePrivilegeList !== undefined &&
      opts.tablePrivilegeList !== null
    ) {
      postBody['tablePrivilegeList'] = opts.tablePrivilegeList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userAddTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userAddTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userUpdateTablePrivilege
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.tableName] - 表名称  optional
      * @param {string} [opts.dbName] - 库名  optional
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {string} [opts.metaTablePrivilegeType] - 表权限类型  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userUpdateTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userUpdateTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userUpdateTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.tableName !== undefined && opts.tableName !== null) {
      postBody['tableName'] = opts.tableName
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.metaTablePrivilegeType !== undefined &&
      opts.metaTablePrivilegeType !== null
    ) {
      postBody['metaTablePrivilegeType'] = opts.metaTablePrivilegeType
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userUpdateTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userUpdateTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userDelTablePrivilege
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {string} [opts.dbName] - 库名  optional
      * @param {string} [opts.projectCode] - 项目ID  optional
      * @param {array} [opts.dbTableList] - 表名列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userDelTablePrivilege (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userDelTablePrivilege"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userDelTablePrivilege"
      )
    }

    let postBody = {}
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.dbTableList !== undefined && opts.dbTableList !== null) {
      postBody['dbTableList'] = opts.dbTableList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userDelTablePrivilege with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userDelTablePrivilege',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userGetRoleList
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {userGetRoleListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoUserGetRoleListResp result  返回结果
      */

  userGetRoleList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userGetRoleList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userGetRoleList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userGetRoleList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userGetRoleList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userAddRole
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode]   optional
      * @param {string} [opts.userPin]   optional
      * @param {array} [opts.roleList]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userAddRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userAddRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userAddRole"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.roleList !== undefined && opts.roleList !== null) {
      postBody['roleList'] = opts.roleList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userAddRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userAddRole',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  userDeleteRole
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode]   optional
      * @param {string} [opts.userPin]   optional
      * @param {array} [opts.roleList]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result
      */

  userDeleteRole (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  userDeleteRole"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling userDeleteRole"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.roleList !== undefined && opts.roleList !== null) {
      postBody['roleList'] = opts.roleList
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call userDeleteRole with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/userDeleteRole',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据用户获取所有角色列表，并标记上已添加角色是否已添加用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode]   optional
      * @param {string} [opts.userPin]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param getAddableRoleRes result
      */

  getAllRoleByUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllRoleByUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllRoleByUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllRoleByUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllRoleByUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据用户获取所有表，并标记上已添加角色是否已添加用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.dbName] - 库名  optional
      * @param {string} [opts.tableModel] - 表/视图/物化视图  optional
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param getAllTablesByUserRes result
      */

  getAllTablesByUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllTablesByUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllTablesByUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.dbName !== undefined && opts.dbName !== null) {
      postBody['dbName'] = opts.dbName
    }
    if (opts.tableModel !== undefined && opts.tableModel !== null) {
      postBody['tableModel'] = opts.tableModel
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllTablesByUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllTablesByUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据用户获取所有库，并标记上已添加角色是否已添加用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.catalogName] - Catalog名称，可选  optional
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param getAllDBByUserRes result
      */

  getAllDBByUser (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  getAllDBByUser"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling getAllDBByUser"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.catalogName !== undefined && opts.catalogName !== null) {
      postBody['catalogName'] = opts.catalogName
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call getAllDBByUser with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/getAllDBByUser',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  根据用户获取所有库，并标记上已添加角色是否已添加用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.userPin] - 用户pin  optional
      * @param {string} [opts.tableString] - 库表名  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应吗
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param checkBatchTablesRes result
      */

  checkBatchTables (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  checkBatchTables"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling checkBatchTables"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.userPin !== undefined && opts.userPin !== null) {
      postBody['userPin'] = opts.userPin
    }
    if (opts.tableString !== undefined && opts.tableString !== null) {
      postBody['tableString'] = opts.tableString
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call checkBatchTables with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/checkBatchTables',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询存储卷列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {storageVolumeListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoStorageVolumeRes result  返回结果对象
      */

  storageVolumeGetList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageVolumeGetList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageVolumeGetList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageVolumeGetList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageVolumeGetList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加存储卷
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.storageVolumeName] - 存储卷名称  optional
      * @param {string} [opts.comment] - 存储卷注释  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  storageVolumeAdd (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageVolumeAdd"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageVolumeAdd"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (
      opts.storageVolumeName !== undefined &&
      opts.storageVolumeName !== null
    ) {
      postBody['storageVolumeName'] = opts.storageVolumeName
    }
    if (opts.comment !== undefined && opts.comment !== null) {
      postBody['comment'] = opts.comment
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageVolumeAdd with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageVolumeAdd',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  启用存储卷
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.storageVolumeId] - 存储卷ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  storageVolumeEnable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageVolumeEnable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageVolumeEnable"
      )
    }

    let postBody = {}
    if (opts.storageVolumeId !== undefined && opts.storageVolumeId !== null) {
      postBody['storageVolumeId'] = opts.storageVolumeId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageVolumeEnable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageVolumeEnable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  停用存储卷
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.storageVolumeId] - 存储卷ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  storageVolumeDisable (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageVolumeDisable"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageVolumeDisable"
      )
    }

    let postBody = {}
    if (opts.storageVolumeId !== undefined && opts.storageVolumeId !== null) {
      postBody['storageVolumeId'] = opts.storageVolumeId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageVolumeDisable with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageVolumeDisable',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改存储卷注释
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.storageVolumeId] - 存储卷ID  optional
      * @param {string} [opts.comment] - 存储卷注释  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  storageVolumeUpdateComment (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageVolumeUpdateComment"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageVolumeUpdateComment"
      )
    }

    let postBody = {}
    if (opts.storageVolumeId !== undefined && opts.storageVolumeId !== null) {
      postBody['storageVolumeId'] = opts.storageVolumeId
    }
    if (opts.comment !== undefined && opts.comment !== null) {
      postBody['comment'] = opts.comment
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageVolumeUpdateComment with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageVolumeUpdateComment',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  设置默认存储卷
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.storageVolumeId] - 存储卷ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  storageVolumeSetDefault (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageVolumeSetDefault"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageVolumeSetDefault"
      )
    }

    let postBody = {}
    if (opts.storageVolumeId !== undefined && opts.storageVolumeId !== null) {
      postBody['storageVolumeId'] = opts.storageVolumeId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageVolumeSetDefault with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageVolumeSetDefault',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除存储卷
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.storageVolumeId] - 存储卷ID  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  storageVolumeDelete (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  storageVolumeDelete"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling storageVolumeDelete"
      )
    }

    let postBody = {}
    if (opts.storageVolumeId !== undefined && opts.storageVolumeId !== null) {
      postBody['storageVolumeId'] = opts.storageVolumeId
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call storageVolumeDelete with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/storageVolumeDelete',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询资源分组列表
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {warehouseListReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoWarehouseRes result  返回结果对象
      */

  warehouseGetList (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseGetList"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseGetList"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseGetList with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseGetList',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  添加资源分组
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.warehouseName] - 资源分组名称  optional
      * @param {string} [opts.description] - 资源分组描述  optional
      * @param {string} [opts.owner] - 资源分组负责人  optional
      * @param {array} [opts.nodeCodes] - 初始分配的节点编码列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  warehouseAdd (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseAdd"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseAdd"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }
    if (opts.nodeCodes !== undefined && opts.nodeCodes !== null) {
      postBody['nodeCodes'] = opts.nodeCodes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseAdd with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseAdd',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取资源分组详情
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.warehouseName] - 资源分组名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param warehouseDetailRes result  返回结果对象
      */

  warehouseGetDetail (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseGetDetail"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseGetDetail"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseGetDetail with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseGetDetail',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  修改资源分组配置
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.warehouseName] - 资源分组名称  optional
      * @param {string} [opts.description] - 资源分组描述  optional
      * @param {string} [opts.owner] - 资源分组负责人  optional
      * @param {array} [opts.nodeCodes] - 计算节点编码列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  warehouseUpdate (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseUpdate"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseUpdate"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }
    if (opts.description !== undefined && opts.description !== null) {
      postBody['description'] = opts.description
    }
    if (opts.owner !== undefined && opts.owner !== null) {
      postBody['owner'] = opts.owner
    }
    if (opts.nodeCodes !== undefined && opts.nodeCodes !== null) {
      postBody['nodeCodes'] = opts.nodeCodes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseUpdate with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseUpdate',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  删除资源分组
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.warehouseName] - 资源分组名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  warehouseDelete (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseDelete"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseDelete"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseDelete with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseDelete',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分配计算节点
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.warehouseName] - 资源分组名称  optional
      * @param {array} [opts.nodeCodes] - 节点编码列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  warehouseAssignNodes (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseAssignNodes"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseAssignNodes"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }
    if (opts.nodeCodes !== undefined && opts.nodeCodes !== null) {
      postBody['nodeCodes'] = opts.nodeCodes
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseAssignNodes with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseAssignNodes',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分配用户权限
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.warehouseName] - 资源分组名称  optional
      * @param {array} [opts.users] - 用户权限分配列表  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param boolean result  返回结果
      */

  warehouseAssignUsers (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseAssignUsers"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseAssignUsers"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }
    if (opts.users !== undefined && opts.users !== null) {
      postBody['users'] = opts.users
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseAssignUsers with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseAssignUsers',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  分页查询未分配的计算节点
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {integer} [opts.currentPage] - 分页页码  optional
      * @param {integer} [opts.pageSize] - 分页大小  optional
      * @param {string} [opts.sort] - 排序字段  optional
      * @param {string} [opts.order] - 排序方向  optional
      * @param {unassignedNodeReq} [opts.data]   optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param pageRespVoNodeRes result  返回结果对象
      */

  warehouseGetUnassignedNodes (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseGetUnassignedNodes"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseGetUnassignedNodes"
      )
    }

    let postBody = {}
    if (opts.currentPage !== undefined && opts.currentPage !== null) {
      postBody['currentPage'] = opts.currentPage
    }
    if (opts.pageSize !== undefined && opts.pageSize !== null) {
      postBody['pageSize'] = opts.pageSize
    }
    if (opts.sort !== undefined && opts.sort !== null) {
      postBody['sort'] = opts.sort
    }
    if (opts.order !== undefined && opts.order !== null) {
      postBody['order'] = opts.order
    }
    if (opts.data !== undefined && opts.data !== null) {
      postBody['data'] = opts.data
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseGetUnassignedNodes with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseGetUnassignedNodes',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }

  /**
      *  获取对给定资源分组有权限的所有用户
      * @param {Object} opts - parameters
      * @param {string} opts.appName - 应用名称
      * @param {string} [opts.projectCode] - 实例code  optional
      * @param {string} [opts.warehouseName] - 资源分组名称  optional
      * @param {string} regionId - ID of the region
      * @param {string} callback - callback
      @return {Object} result
      * @param string code  业务响应码
      * @param string message  响应消息
      * @param string requestId  请求id
      * @param warehouseUserRes result
      */

  warehouseGetUsers (opts, regionId = this.config.regionId, callback) {
    if (typeof regionId === 'function') {
      callback = regionId
      regionId = this.config.regionId
    }

    if (regionId === undefined || regionId === null) {
      throw new Error(
        "Missing the required parameter 'regionId' when calling  warehouseGetUsers"
      )
    }

    opts = opts || {}

    if (opts.appName === undefined || opts.appName === null) {
      throw new Error(
        "Missing the required parameter 'opts.appName' when calling warehouseGetUsers"
      )
    }

    let postBody = {}
    if (opts.projectCode !== undefined && opts.projectCode !== null) {
      postBody['projectCode'] = opts.projectCode
    }
    if (opts.warehouseName !== undefined && opts.warehouseName !== null) {
      postBody['warehouseName'] = opts.warehouseName
    }

    let queryParams = {}

    let pathParams = {
      regionId: regionId,
      appName: opts.appName
    }

    let headerParams = {
      'User-Agent': 'JdcloudSdkNode/1.0.0  jdstarrocks/1.0.0'
    }

    let contentTypes = ['application/json']
    let accepts = ['application/json']

    // 扩展自定义头
    if (opts['x-extra-header']) {
      for (let extraHeader in opts['x-extra-header']) {
        headerParams[extraHeader] = opts['x-extra-header'][extraHeader]
      }

      if (Array.isArray(opts['x-extra-header']['content-type'])) {
        contentTypes = opts['x-extra-header']['content-type']
      } else if (typeof opts['x-extra-header']['content-type'] === 'string') {
        contentTypes = opts['x-extra-header']['content-type'].split(',')
      }

      if (Array.isArray(opts['x-extra-header']['accept'])) {
        accepts = opts['x-extra-header']['accept']
      } else if (typeof opts['x-extra-header']['accept'] === 'string') {
        accepts = opts['x-extra-header']['accept'].split(',')
      }
    }

    let formParams = {}

    let returnType = null

    this.config.logger(
      `call warehouseGetUsers with params:\npathParams:${JSON.stringify(
        pathParams
      )},\nqueryParams:${JSON.stringify(
        queryParams
      )}, \nheaderParams:${JSON.stringify(
        headerParams
      )}, \nformParams:${JSON.stringify(
        formParams
      )}, \npostBody:${JSON.stringify(postBody)}`,
      'DEBUG'
    )

    let request = super.makeRequest(
      '/regions/{regionId}/apps/{appName}/warehouseGetUsers',
      'POST',
      pathParams,
      queryParams,
      headerParams,
      formParams,
      postBody,
      contentTypes,
      accepts,
      returnType,
      callback
    )

    return request.then(
      function (result) {
        if (callback && typeof callback === 'function') {
          return callback(null, result)
        }
        return result
      },
      function (error) {
        if (callback && typeof callback === 'function') {
          return callback(error)
        }
        return Promise.reject(error)
      }
    )
  }
}
module.exports = JDSTARROCKS
